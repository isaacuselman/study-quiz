// ╔══════════════════════════════════════════════════════════════════╗
// ║  CS50 (Introduction to Computer Science) lectures               ║
// ╚══════════════════════════════════════════════════════════════════╝

export const CS50_LECTURES = [

// ─── LECTURE 1 ───────────────────────────────────────────────────────
{
  course: "CS50",
  title: "CS50 Week 1: C Fundamentals",
  questions: [
    // Compilation & build process
    { q: "What is the role of a compiler?", options: ["Runs source code line by line as an interpreter would", "Converts source code into machine code", "Checks code style and formatting conventions", "Manages memory allocation during execution"], answer: 1, explanation: "A compiler translates human-readable source code into machine code (binary) that the CPU can execute directly. This is different from an interpreter, which runs code line by line." },
    { q: "When you change source code, what must you do before running it again?", options: ["Save the file and close the editor first", "Recompile it", "Restart the terminal to refresh the environment", "Delete the old binary so the system picks up changes"], answer: 1 },
    { q: "If compiling produces no output, what does that typically mean?", options: ["The program crashed during compilation", "Compilation succeeded", "The file is empty and has no code to compile", "You need to install missing dependencies first"], answer: 1 },
    { q: "What is the relationship between 'make hello' and 'hello.c'?", options: ["make runs hello.c directly as a script", "make infers the .c filename and compiles it", "make creates a new file called hello.c from a template", "They are unrelated commands that serve different purposes"], answer: 1 },

    // Types & data representation
    { q: "Why does C require you to declare a variable's type (int, float, char)?", options: ["So the editor can apply syntax highlighting correctly", "So the compiler knows how much memory to allocate", "It's optional but recommended for clarity", "So the linker can optimize the final binary"], answer: 1 },
    { q: "What is the fundamental difference between an int and a float?", options: ["ints are stored in registers, floats in memory", "floats can represent decimal values, ints cannot", "ints use more memory than floats per value", "floats are unsigned while ints can be negative"], answer: 1 },
    { q: "What happens when you divide two integers in C (e.g., 1 / 3)?", options: ["You get 0.333... as a floating-point result", "The decimal is truncated — you get 0", "It throws a divide-by-fraction error at runtime", "It rounds to the nearest whole integer automatically"], answer: 1, explanation: "Integer division in C discards the fractional part entirely (truncation toward zero). To get a decimal result, cast one operand to float first: (float)1 / 3 gives 0.333..." },
    { q: "What is integer overflow?", options: ["Using more variables than the stack can hold", "Storing a value too large for the allocated bits, causing wraparound", "Dividing by zero and getting an undefined result", "Running out of heap memory during allocation"], answer: 1 },
    { q: "A 32-bit integer can represent roughly how many distinct values?", options: ["32 thousand distinct values", "About 1 million distinct values", "4 billion", "An unlimited number of values"], answer: 2 },
    { q: "What is floating-point imprecision?", options: ["Floats use twice the cycles of ints per operation", "Finite bits can't represent all real numbers exactly", "Floats can only store positive decimal values", "A known bug in C's standard math library functions"], answer: 1 },

    // Scope & variables
    { q: "If you declare a variable inside a for loop's braces, where can you use it?", options: ["Anywhere in the file after declaration", "Anywhere in the enclosing function body", "Only inside that loop's braces", "Only on the exact line where it's declared"], answer: 2 },
    { q: "What does 'scope' mean in programming?", options: ["The speed at which a variable can be accessed in memory", "The region of code where a variable exists and is accessible", "The data type and bit-width assigned to a variable", "Whether a variable is allocated on the stack or heap"], answer: 1 },
    { q: "What does the 'const' keyword signal to the compiler?", options: ["The variable should be visible across all source files", "The value must never be modified after initialization", "The variable is private to the current function only", "It should allocate extra memory for overflow protection"], answer: 1 },

    // Control flow
    { q: "Why is 'else if' preferable to multiple independent 'if' statements for mutually exclusive conditions?", options: ["It's the only valid syntax C allows for chained conditions", "It short-circuits — once a condition is true, the rest are skipped", "It uses significantly less stack memory at runtime", "There is no meaningful difference in behavior between them"], answer: 1 },
    { q: "What does a do-while loop guarantee that a while loop does not?", options: ["It runs exactly once and then always terminates", "The body executes at least once before the condition is checked", "It can never become an infinite loop under any input", "It evaluates the condition twice per iteration for safety"], answer: 1, explanation: "A do-while loop checks its condition after executing the body, so the body always runs at least once. A while loop checks first, so it may never run if the condition is initially false." },
    { q: "In 'for (int i = 0; i < n; i++)', what are the three parts separated by semicolons?", options: ["Declaration, condition, increment", "Input, process, output", "Start, stop, step", "Initialize, execute, terminate"], answer: 0 },
    { q: "What does 'break' do inside a loop?", options: ["Pauses execution until the user presses a key", "Exits the loop immediately", "Skips to the next iteration of the loop body", "Ends the program and returns an exit code"], answer: 1 },
    { q: "What does 'continue' do inside a loop?", options: ["Exits the loop and jumps to the next statement after it", "Restarts the program from the beginning of main", "Skips the rest of the current iteration and goes to the next", "Pauses the loop and waits for user input"], answer: 2 },

    // Functions & abstraction
    { q: "What is the main benefit of extracting repeated logic into a function?", options: ["It makes the compiled binary use less memory overall", "You write it once, use it many times — reducing duplication", "Functions always run faster than the equivalent inline code", "It's required by the compiler for programs over 50 lines"], answer: 1 },
    { q: "What does 'void' mean as a function's return type?", options: ["It returns zero as an integer value", "It returns an empty string to the caller", "It returns nothing", "It returns a boolean true on success"], answer: 2 },
    { q: "Why does C require a function prototype above main if the function is defined below?", options: ["It's a style choice with no impact on compilation", "The compiler reads top-to-bottom and needs to know the function exists", "It improves the runtime performance of function calls", "It's only needed for functions that call themselves recursively"], answer: 1 },
    { q: "If function A defines variable 'n' and passes it to function B, can B see A's 'n' directly?", options: ["Yes, all variables are shared globally between functions", "No — B receives its own copy via the parameter", "Only if both functions are defined in the same source file", "Only if n is declared with the const keyword first"], answer: 1 },

    // Terminal & tooling
    { q: "What does Ctrl+C do in a terminal?", options: ["Copies the selected text to the clipboard", "Interrupts/kills the running process", "Clears the screen and resets the prompt", "Closes the terminal window and all processes"], answer: 1 },
    { q: "What does './' before a program name mean when running it?", options: ["Run the program with elevated admin privileges", "Look in the current directory for this program", "Compile the source and then run the output", "Run the program in step-by-step debug mode"], answer: 1 },
    { q: "What is the purpose of the 'cd' command?", options: ["Compile and deploy a project to the server", "Change directory", "Create a new directory in the file system", "Copy data from one location to another"], answer: 1 },
    { q: "What does 'ls' show you?", options: ["All currently running processes on the system", "Files and folders in the current directory", "Recent system log entries from the kernel", "All environment variables set in the shell"], answer: 1 },

    // Debugging mindset
    { q: "When a compiler error says 'undeclared identifier', what does it typically mean?", options: ["The variable name is misspelled or not yet defined in scope", "The program has a runtime error that crashed execution", "You forgot a semicolon at the end of the previous line", "The file doesn't exist in the current working directory"], answer: 0 },
    { q: "Why is it best practice to fix the FIRST compiler error before addressing later ones?", options: ["Later errors are usually warnings that can be safely ignored", "One early error can cascade and cause many false errors below it", "The compiler only shows one error at a time in its output", "It's just a convention with no real technical benefit"], answer: 1 },

    // Design principles
    { q: "What are the three axes CS50 uses to evaluate code quality?", options: ["Speed, memory, style", "Correctness, design, style", "Syntax, logic, performance", "Readability, efficiency, testing"], answer: 1 },
    { q: "Why is hard-coding a value in multiple places (like '3' in two loops) considered bad design?", options: ["It causes compiler errors when values don't match", "If you need to change it, you must find and update every instance", "It uses more memory because each copy is stored separately", "It's fine — repeating constants has no design drawbacks"], answer: 1 },
  ],
},

// ─── LECTURE 2 ───────────────────────────────────────────────────────
{
  course: "CS50",
  title: "CS50 Week 2: Arrays, Strings & Debugging",
  questions: [
    // Debugging methodology
    { q: "What is the difference between a syntax error and a logical error?", options: ["Syntax errors are harder to find because they only appear at runtime", "Syntax errors prevent compilation; logical errors compile but produce wrong results", "Logical errors crash the program with a segmentation fault every time", "There is no meaningful difference — both are caught by the compiler"], answer: 1, explanation: "Syntax errors violate the language grammar and are caught by the compiler. Logical errors are valid code that does the wrong thing \u2014 the compiler can't detect them because the code is technically well-formed." },
    { q: "What is the most effective first use of printf when debugging?", options: ["Print 'hello world' to confirm the program compiles and runs correctly", "Print the values of variables at key points to see what the program actually has vs. what you expect", "Print the entire source code to the terminal for visual inspection", "Print error codes returned by each function call as you go"], answer: 1 },
    { q: "What does a breakpoint do in a debugger?", options: ["Permanently stops the program and frees all allocated memory", "Pauses execution at that line so you can inspect variables and step through code", "Marks a line for deletion from the source file", "Creates a backup snapshot of the program's current memory state"], answer: 1 },
    { q: "What is the difference between 'step over' and 'step into' in a debugger?", options: ["Step over skips the line entirely; step into executes it normally", "Step over executes a function call without entering it; step into enters the function line by line", "They are identical commands with different keyboard shortcuts", "Step into only works with the main() function, not helpers"], answer: 1 },
    { q: "Why is 'rubber duck debugging' effective?", options: ["Ducks are a visual metaphor for pattern-matching in code", "Explaining the problem aloud forces you to articulate your assumptions, often revealing the flaw", "It's a myth — there's no evidence it actually helps programmers", "The duck represents a second pair of eyes reviewing the code"], answer: 1 },
    { q: "You add a printf to debug a loop and find i goes 0, 1, 2, 3 when you expected 0, 1, 2. What is the likely bug?", options: ["i starts at the wrong initial value before the loop begins", "The loop condition allows one extra iteration (e.g., <= instead of <)", "printf is displaying incorrect values due to a format specifier bug", "The loop body is executing twice per iteration due to a missing brace"], answer: 1 },

    // Compilation pipeline
    { q: "What are the four steps that happen when you 'compile' a C program?", options: ["Parse, optimize, link, run", "Preprocessing, compiling, assembling, linking", "Tokenize, analyze, generate, execute", "Read, evaluate, print, loop"], answer: 1 },
    { q: "What does the preprocessing step do?", options: ["Converts C source code into assembly language instructions", "Replaces #include directives with the actual contents of header files", "Links external library functions together into one binary", "Optimizes the code by removing unused variables and dead code"], answer: 1 },
    { q: "What does the linking step do?", options: ["Converts assembly language instructions into raw machine code bytes", "Combines your compiled code with library code (e.g., printf's implementation) into one executable", "Checks all source files for syntax errors before compilation", "Formats the output file with the correct platform-specific headers"], answer: 1 },
    { q: "Why can't you typically take a compiled binary from a Mac and run it on a different CPU architecture?", options: ["The file extension and binary format are specific to each OS", "Different CPUs use different instruction sets, so the same C code compiles to different machine code", "Mac binaries are encrypted with a platform-specific signing key", "You can — compiled binaries are universal across all platforms"], answer: 1 },
    { q: "What is 'make' actually doing when you type 'make hello'?", options: ["Interpreting hello.c directly as if it were a scripting language", "Running clang with the correct flags and library links so you don't have to type them manually", "Downloading the hello program's source code from the internet", "Creating a new file called hello.c from a built-in template"], answer: 1 },
    { q: "What does '-lcs50' do when passed to the compiler?", options: ["Sets the language standard to the CS50 variant of C", "Tells the linker to include the CS50 library's compiled code", "Enables the CS50-specific interactive debugging mode", "Changes the output filename to include a cs50 prefix"], answer: 1 },

    // Memory model & data types
    { q: "How many bytes does an int typically use on modern systems?", options: ["1", "2", "4", "8"], answer: 2 },
    { q: "How many bytes does a char use?", options: ["1", "2", "4", "It depends on the character"], answer: 0 },
    { q: "What is a 'garbage value' in a variable?", options: ["A value the compiler explicitly initializes to zero", "Whatever bit pattern was left in that memory from previous use, before you assign a value", "A random number generated by the OS for security purposes", "An error code the compiler inserts to help with debugging"], answer: 1 },
    { q: "Why is it important that array elements are stored contiguously in memory?", options: ["It's a compiler requirement with no practical effect on performance", "It allows indexing by arithmetic — element i is at a predictable offset from the start", "It makes the array easier to print to the terminal in order", "Arrays wouldn't compile if elements were stored in separate locations"], answer: 1 },
    { q: "If you cast an int division to float (e.g., (float) sum / count), what problem does this solve?", options: ["It prevents integer overflow when the result is large", "It avoids integer truncation so you get a decimal result", "It makes the division operation run faster on modern CPUs", "It has no effect — the cast is ignored by the compiler"], answer: 1 },

    // Strings as arrays & null termination
    { q: "In C, what is a string at the memory level?", options: ["A special built-in data structure with a length field", "An array of characters terminated by a null character (\\0)", "A linked list of chars connected by pointers to each other", "A pointer to a file containing the text on disk"], answer: 1, explanation: "C has no built-in string type. A 'string' is just a contiguous array of chars in memory, with a special \\0 byte at the end to mark where it stops. Functions like strlen() rely on this sentinel." },
    { q: "What is the null character (\\0) and why does it exist?", options: ["It represents the letter O in the ASCII encoding table", "It's a sentinel byte of 8 zero-bits that marks where a string ends in memory", "It separates individual words within a single string value", "It's optional padding the compiler adds for memory alignment"], answer: 1 },
    { q: "A string containing 'hi!' takes how many bytes in memory?", options: ["3 bytes, one per visible character", "4", "5 bytes, including spaces between characters", "It depends on the system architecture"], answer: 1 },
    { q: "If you index one position past the end of a string (e.g., s[3] on a 3-char string), what value do you find?", options: ["A random character left over from previous memory use", "The null character (\\0), which is the value 0", "An error is thrown by the runtime bounds checker", "The first character of the next variable in memory"], answer: 1 },
    { q: "Why can't you ask a C array for its own length, unlike in Python or Java?", options: ["C arrays are immutable and can't be inspected at runtime", "C doesn't store the length — you must track it separately or use a sentinel like \\0", "You can, using the .length property available on all arrays", "It's a deliberate bug in C that was never fixed"], answer: 1 },
    { q: "How does strlen() determine the length of a string?", options: ["It reads a length field stored alongside the array in memory", "It counts characters from the start until it hits the null terminator", "It divides the total allocated memory by the size of a char", "It's calculated and hard-coded by the compiler at compile time"], answer: 1 },

    // Arrays
    { q: "What does 'int scores[3];' do?", options: ["Creates 3 separate int variables each with their own name", "Allocates a contiguous block of memory for 3 ints, accessible via scores[0], scores[1], scores[2]", "Creates an int variable and initializes it with the value 3", "Declares a function that takes and returns an array of 3 ints"], answer: 1 },
    { q: "When passing an array to a function in C, what else must you typically pass?", options: ["The array's memory address, since C passes arrays by value", "The length of the array, since C arrays don't know their own size", "A full copy of the array so the function has its own version", "The data type of the elements so the function can interpret them"], answer: 1 },
    { q: "Why do arrays in C use 0-based indexing?", options: ["It's an arbitrary convention inherited from early languages", "The index represents the offset from the start of the array in memory", "It prevents off-by-one errors when iterating through elements", "It saves memory by not needing a separate index variable"], answer: 1 },

    // Design & efficiency
    { q: "What is wrong with calling strlen(s) in a for loop condition like 'for (int i = 0; i < strlen(s); i++)'?", options: ["strlen doesn't work correctly when called inside loop conditions", "strlen is recalculated every iteration even though the string length doesn't change — wasteful", "It causes an infinite loop because strlen returns a different value each time", "It's actually fine — the compiler automatically optimizes it away"], answer: 1 },
    { q: "How do you fix the strlen-in-loop inefficiency?", options: ["Use a while loop instead of a for loop for string iteration", "Calculate the length once before the loop and store it in a variable", "Use a shorter string to reduce the number of length calculations", "Call strlen only on even iterations to cut the overhead in half"], answer: 1 },
    { q: "What is a 'magic number' in code and why is it bad?", options: ["A number that causes the program to crash when used in arithmetic", "An unexplained literal value hard-coded in multiple places — fragile and unclear", "A number used in encryption algorithms to generate hash values", "Any number greater than 100 that should be stored as a long instead"], answer: 1 },

    // Command line arguments
    { q: "What does 'int main(int argc, string argv[])' allow that 'int main(void)' does not?", options: ["It makes the program compile and execute faster overall", "It lets the program accept words typed after its name at the command line", "It enables the debugger to attach to the running process", "It allows the function to return multiple values to the caller"], answer: 1 },
    { q: "If you run './greet David', what is argv[0] and argv[1]?", options: ["argv[0] is 'David', argv[1] is './greet'", "argv[0] is './greet', argv[1] is 'David'", "argv[0] is 'David', argv[1] is null", "argv[0] is 0, argv[1] is 1"], answer: 1 },
    { q: "What does argc contain?", options: ["The number of characters typed in the full command", "The total count of command-line words, including the program name", "The program's exit code from the last run", "The number of functions defined in the program"], answer: 1 },

    // Exit status
    { q: "What does 'return 0' from main signify?", options: ["The program found zero results in its output", "The program completed successfully", "The program had zero errors but may have produced warnings", "Nothing specific — it's an optional return value"], answer: 1 },
    { q: "If main returns 1 (or any non-zero value), what does that signal?", options: ["Success, with minor warnings to review", "An error occurred", "The program needs to restart from the beginning", "Exactly one result was found by the program"], answer: 1 },

    // Character manipulation & ASCII
    { q: "Why can you subtract 32 from a lowercase ASCII letter to get its uppercase equivalent?", options: ["32 is the number of letters in the English alphabet", "In ASCII, every lowercase letter is exactly 32 positions above its uppercase counterpart", "It's a coincidence that only works for certain English letters", "You can't — subtracting 32 produces an undefined result"], answer: 1 },
    { q: "What is the advantage of using library functions like toupper() instead of manual ASCII math?", options: ["toupper() is faster because it uses hardware instructions directly", "It handles edge cases (non-letters pass through unchanged) and is more readable", "There is no advantage — manual arithmetic is equally robust", "toupper() works with Unicode while subtraction only handles ASCII"], answer: 1 },
    { q: "Why do single quotes ('a') and double quotes (\"a\") mean different things in C?", options: ["They're interchangeable — both produce a single character value", "Single quotes denote a single char (1 byte); double quotes denote a string (array + null terminator)", "Single quotes are used for numeric constants like '0' through '9'", "Double quotes are deprecated and should be replaced with single quotes"], answer: 1 },

    // Reverse engineering & security intuition
    { q: "Why is it impractical to perfectly reverse-engineer source code from a compiled binary?", options: ["Binaries are encrypted with a key that only the compiler knows", "Different source constructs (for vs while) compile to identical machine code — the original style is lost", "It's illegal under international copyright law to decompile any code", "Binaries delete the original source file as part of the compilation process"], answer: 1 },
  ],
},

// ─── PASTE NEW LECTURES BELOW THIS LINE ─────────────────────────────
// Format: { title: "Lecture Name", questions: [ { q, options, answer }, ... ] },

// ─── LECTURE 3 ───────────────────────────────────────────────────────
{
  course: "CS50",
  title: "CS50 Week 3: Algorithms, Sorting, and Recursion",
  questions: [
    // Big O and Asymptotic Notation
    { q: "Why does Big O notation ignore lower-order terms like 'divide by 2' or constant factors?", options: ["They are too small to calculate accurately on real hardware", "As N grows very large, only the dominant term meaningfully affects the total", "Computers handle constants automatically during execution", "Lower-order terms only matter for negative or zero inputs"], answer: 1, explanation: "Big O describes growth rate as input approaches infinity. At massive N, the difference between N\u00b2 and N\u00b2/2 is insignificant compared to the difference between N\u00b2 and N log N." },
    { q: "An algorithm runs in O(N/2). How would a computer scientist more precisely classify it?", options: ["O(N/2) — the constant matters for real-world performance", "O(1) — dividing by 2 makes it effectively constant time", "O(N) — constants are dropped in Big O notation", "O(log N) — division by a constant implies logarithmic behavior"], answer: 2 },
    { q: "What does Omega notation describe that Big O does not?", options: ["The worst-case running time of an algorithm", "The average-case running time across all inputs", "Best-case (lower bound) running time", "The exact step count for a given input size"], answer: 2 },
    { q: "When can you use Theta notation to describe an algorithm's running time?", options: ["When the algorithm uses recursive function calls", "When Big O and Omega are the same value", "When the input is already sorted before the algorithm begins", "When the algorithm runs in constant time regardless of input"], answer: 1 },
    { q: "Which of the following correctly orders these running times from fastest to slowest as N grows large?", options: ["O(N²), O(N log N), O(N), O(log N), O(1)", "O(1), O(log N), O(N), O(N log N), O(N²)", "O(log N), O(1), O(N), O(N²), O(N log N)", "O(N), O(1), O(log N), O(N log N), O(N²)"], answer: 1 },

    // Linear and Binary Search
    { q: "In the worst case, what is the Big O running time of linear search on an array of N elements?", options: ["O(1)", "O(log N)", "O(N)", "O(N²)"], answer: 2 },
    { q: "What is the Omega (best case) running time of both linear search and binary search?", options: ["Ω(N)", "Ω(log N)", "Ω(N²)", "Ω(1)"], answer: 3 },
    { q: "Why is binary search incorrect to use on an unsorted array?", options: ["It always checks the wrong half of the array first", "It requires too much extra memory to store indices", "Decisions to go left or right are based on ordering assumptions that don't hold", "It can only work correctly on arrays with an even number of elements"], answer: 2 },
    { q: "In the pseudocode 'for i from 0 to N-1', why N-1 instead of N?", options: ["N-1 avoids checking the middle element a second time", "Arrays are 0-indexed, so N elements occupy indices 0 through N-1", "The last element is always already sorted by convention", "It's a convention with no functional or technical reason"], answer: 1 },
    { q: "In binary search on 7 elements, how do you find the middle index?", options: ["7 / 2 = 3.5, round up to index 4", "7 / 2 = 3.5, round down to 3", "Always use index 0 as the starting comparison point", "Pick randomly between index 0 and index 6"], answer: 1 },
    { q: "In a linear search implementation, why is this pseudocode wrong: 'for each door: if 50 is here return true; else return false'?", options: ["'return false' should be replaced with 'break' to exit the loop", "The else causes immediate false return without checking remaining doors", "You should search from right to left instead of left to right", "True should be returned at the end of the loop, not first"], answer: 1 },
    { q: "If you only need to search a dataset once and it is unsorted, what is generally the best approach?", options: ["Sort it first then use binary search for better performance", "Use linear search directly", "Use merge sort then run binary search on the sorted result", "Use selection sort then run binary search on the sorted result"], answer: 1 },

    // Structs and Data Design
    { q: "What is the main design problem with storing parallel arrays (e.g., names[] and numbers[]) for a phone book?", options: ["Arrays can only store one data type per program", "The arrays cannot be guaranteed to be the same length", "Correctness depends on manually keeping both arrays in sync", "C doesn't support declaring two arrays in the same function"], answer: 2 },
    { q: "What does 'typedef struct' allow you to do in C?", options: ["Import external libraries into your source file", "Define a custom data type that groups multiple fields together", "Declare a function that returns multiple values at once", "Resize an array at runtime without copying the data"], answer: 1 },
    { q: "How do you access the 'name' field of the first element in an array called 'people'?", options: ["people[0]->name", "people.name[0]", "people[0].name", "people->name[0]"], answer: 2 },
    { q: "Why are phone numbers stored as strings rather than integers in C?", options: ["Integers can't be stored inside struct arrays in C", "Phone numbers may contain non-digit characters like '+' and '-', and leading zeros have no integer value", "String comparison is faster than integer comparison for lookups", "C has no integer type large enough to hold a 10-digit phone number"], answer: 1 },

    // String Comparison
    { q: "Why can't you use '==' to compare two strings in C?", options: ["Strings are not declared with the 'int' type, so == doesn't apply", "Strings must be compared character by character; == doesn't do that automatically", "== only works on variables declared within the exact same scope", "C reserves the == operator exclusively for boolean expressions"], answer: 1 },
    { q: "strcmp() returns 0 when strings are equal and a non-zero value otherwise. Why is a non-zero return value also useful beyond equality checking?", options: ["It tells you the length difference between the two strings", "It indicates which string comes first alphabetically, enabling sorting", "It reveals how many individual characters differ between the strings", "It signals a memory allocation error that occurred during the comparison"], answer: 1 },
    { q: "If you forget to '#include <string.h>' before using strcmp(), what happens?", options: ["The program runs but produces garbage output from the comparison", "The compiler reports an 'undeclared function' error", "The program compiles successfully but crashes when strcmp is called", "strcmp defaults to using == behavior on the pointer addresses"], answer: 1 },

    // Selection Sort
    { q: "What is the core operation that selection sort performs on each pass through the array?", options: ["Swap adjacent elements whenever they are out of order", "Find the smallest remaining element and swap it into position", "Merge two sorted halves into a single sorted sequence", "Divide the array into equal parts and sort each independently"], answer: 1 },
    { q: "What is the Big O running time of selection sort?", options: ["O(N)", "O(N log N)", "O(N²)", "O(log N)"], answer: 2 },
    { q: "Why is selection sort's Omega (best case) also O(N²), even if the array is already sorted?", options: ["It has to re-sort the entire array after each individual swap", "The pseudocode makes no check for whether the array is already sorted", "It needs N² memory locations to track element positions during sorting", "Swapping identical elements still takes the same number of CPU cycles"], answer: 1 },
    { q: "When selection sort swaps the minimum element into position, why can't it simply insert it and shift others over?", options: ["C doesn't support insertion operations on arrays natively", "Arrays are contiguous in memory with a fixed size; you can't create new space without overwriting something", "Insertion would change the array's length and invalidate the bounds", "The compiler would optimize away the insert operation entirely"], answer: 1 },

    // Bubble Sort
    { q: "In bubble sort, why does the inner loop run from index 0 to N-2, not N-1?", options: ["The last element is already sorted from the previous pass through the array", "Comparing index i with i+1 would go out of bounds at i = N-1", "The first element is always the largest and needs no further comparison", "N-1 iterations are needed for the outer loop to complete all passes"], answer: 1 },
    { q: "What optimization can reduce bubble sort's best-case running time from O(N²) to Ω(N)?", options: ["Start from the middle of the array instead of the left end", "Track whether any swaps were made; if none, the list is sorted — stop early", "Sort the array descending first, then reverse it to ascending order", "Skip elements that are already known to be in their final position"], answer: 1 },
    { q: "After the first full pass of bubble sort, what is guaranteed about the array?", options: ["The smallest element has been placed into the correct first position", "The largest element has bubbled to the last position", "All adjacent pairs are now in sorted order relative to each other", "The array is approximately half sorted across all positions"], answer: 1 },
    { q: "Both selection sort and bubble sort have O(N²) worst-case running time. What makes bubble sort potentially better in practice?", options: ["Bubble sort uses less memory because it avoids temporary variables", "Bubble sort can terminate early if no swaps occur, achieving Ω(N) best case", "Bubble sort makes fewer total comparisons across all passes of the array", "Bubble sort is easier to parallelize across multiple CPU cores"], answer: 1 },

    // Recursion
    { q: "What is a base case in a recursive function?", options: ["The first call to the function from main or another caller", "A condition that stops the recursion and returns immediately without a recursive call", "A parameter with a default value assigned in the function signature", "A call to a different function from within the recursive function body"], answer: 1, explanation: "Without a base case, recursion would continue infinitely (until stack overflow). The base case is the simplest version of the problem that can be answered directly without further recursion." },
    { q: "Why doesn't a well-written recursive function cause an infinite loop?", options: ["Recursion is limited by the compiler to a maximum of 100 calls", "Each recursive call receives a strictly smaller version of the problem, eventually hitting the base case", "The OS terminates any functions that attempt to call themselves", "Return statements exit the entire program and stop all recursion"], answer: 1 },
    { q: "What error occurs when recursion goes too deep (no base case or base case never reached)?", options: ["Segmentation fault from stack overflow", "Infinite loop displayed in the terminal output", "Compiler error caught at build time before running", "Variable overflow in the CPU's return register"], answer: 0 },
    { q: "In the recursive draw() function for a pyramid of height N, what does the recursive call draw(N-1) accomplish?", options: ["It prints the bottom row of the pyramid first before anything else", "It draws the entire pyramid minus the last row, which the current call then adds", "It calculates the total number of hashes needed for all rows combined", "It resets the loop counter i to 0 for the next iteration of the outer loop"], answer: 1 },
    { q: "A recursive function that calls itself without making the problem smaller will:", options: ["Return the correct answer, just more slowly than iteration", "Run forever or crash due to stack overflow", "Automatically be optimized by the compiler into an iterative loop", "Produce a compile-time error that prevents the code from building"], answer: 1 },

    // Merge Sort
    { q: "What is the Big O running time of merge sort?", options: ["O(N²)", "O(N)", "O(N log N)", "O(log N)"], answer: 2 },
    { q: "Why does merge sort require more memory than selection or bubble sort?", options: ["It stores the entire array twice in memory for comparison purposes", "Merging two sorted halves requires a second temporary array to hold the result", "Recursive calls each allocate a full independent copy of the entire input", "It needs to store every comparison result in a separate lookup table"], answer: 1 },
    { q: "During the merge step, how many total comparisons are needed to merge two sorted halves of total size N?", options: ["N²", "N log N", "N", "log N"], answer: 2 },
    { q: "Why does merge sort produce O(N log N) rather than O(N²)?", options: ["It avoids comparing already-sorted elements carried over from previous passes", "It sorts both halves in parallel across multiple CPU threads", "Each element is touched only once per level, and there are log N levels", "It skips elements that are already in the correct sorted position"], answer: 2 },
    { q: "Merge sort is described as Θ(N log N). What does this tell you that just saying O(N log N) does not?", options: ["The algorithm uses recursion rather than iteration internally", "Both the best and worst case are N log N — it can't do better or worse", "The algorithm is asymptotically faster than O(N) linear time", "The base case of the recursion runs in constant O(1) time"], answer: 1 },
  ],

},
{
  course: "CS50",
  title: "CS50 Week 4: Memory, Pointers & File I/O",
  questions: [
    // Hexadecimal & memory addresses
    { q: "Why do programmers use hexadecimal notation for memory addresses instead of decimal?", options: ["It uses fewer characters than decimal for large values", "One hex digit represents exactly 4 bits, making byte-level values compact and readable", "Computers store addresses in base-16 internally in hardware", "It's required by the C standard for all pointer operations"], answer: 1, explanation: "Each hex digit maps to exactly 4 binary bits (a nibble), so two hex digits represent one byte. This makes it easy to read memory values at the byte level \u2014 0xFF = 11111111 in binary = 255 in decimal." },
    { q: "What does the '0x' prefix before a number signify?", options: ["The value is zero and the x is ignored", "The number that follows is in hexadecimal notation", "The variable being assigned is a pointer type", "The value is negative in two's complement form"], answer: 1 },
    { q: "What is 0xFF in decimal?", options: ["15", "16", "255", "256"], answer: 2 },
    { q: "Why can two hex digits represent a full byte (8 bits)?", options: ["Each hex digit represents exactly 8 binary bits on its own", "Two hex digits can represent values 0–255, which is the same range as 8 bits", "Hex digits are physically larger than binary digits in memory", "It's a convention with no underlying technical reason behind it"], answer: 1 },

    // The & and * operators
    { q: "What does the '&' (ampersand) operator do when applied to a variable?", options: ["Performs a bitwise AND operation on the value", "Returns the memory address of that variable", "Dereferences the variable to get its stored value", "Declares the variable as a pointer to another type"], answer: 1 },
    { q: "What does the '*' operator mean when used in a variable declaration like 'int *p'?", options: ["Multiply the value of p by the integer that follows", "p stores the address of an integer, not an integer itself", "p is a constant integer that cannot be reassigned later", "p is a global variable accessible from any function"], answer: 1 },
    { q: "What does the '*' operator mean when used as a prefix on an existing pointer, like '*p'?", options: ["Declare a new pointer variable in the current scope", "Multiply the value of p by itself and return the result", "Go to the memory address stored in p and access the value there", "Get the address of the variable that p is stored in"], answer: 2 },
    { q: "If 'int *p = &n', what does '*p' give you?", options: ["The address where the pointer p itself is stored", "The address where the variable n is stored in memory", "The value stored at n's address", "The size of n measured in bytes on this system"], answer: 2 },

    // Pointers — mental model
    { q: "Why is a pointer on a modern 64-bit system 8 bytes (64 bits) rather than 4?", options: ["64-bit pointers are faster to read from the memory bus", "Modern computers have more than 4GB of RAM, so addresses need more than 32 bits to reach all of it", "4-byte pointers are deprecated in the latest C standard revision", "The C standard requires all pointers to be exactly 8 bytes"], answer: 1 },
    { q: "What is a pointer, conceptually?", options: ["A copy of a variable's value stored in a separate location", "A variable that stores a memory address", "A special integer type with extra range for large values", "A constant that cannot be changed once initialized"], answer: 1 },
    { q: "In memory diagrams, why do we draw pointers as arrows rather than showing the raw hex address?", options: ["Arrows are required by C syntax when declaring pointer types", "The actual address value is usually irrelevant — what matters is which thing it points to", "Hex addresses can't be drawn legibly in a small diagram", "It's a CS50-specific convention not used outside the course"], answer: 1 },

    // Strings as char* (the white lie)
    { q: "What is a C string at the memory level?", options: ["A special built-in type with a stored length and capacity", "An array of characters ending with a null terminator, referenced by the address of its first character", "A linked list of characters connected by next pointers", "A sequence of integers representing Unicode code points"], answer: 1 },
    { q: "What has the CS50 'string' type always actually been under the hood?", options: ["int[]", "char[]", "char *", "void *"], answer: 2 },
    { q: "Why doesn't a string variable in C need an ampersand when passed to printf with %s?", options: ["printf doesn't use addresses for any of its format specifiers", "The string variable is already a pointer (address of the first character), so passing it directly is correct", "Strings are passed by value in C, not by reference like other types", "The compiler adds the & operator automatically before compilation"], answer: 1 },
    { q: "If 's' is a char* pointing to \"hi!\", what value does the expression 's + 1' represent?", options: ["The length of the string as an integer value", "The address of the second character 'i'", "The integer value of 'h' plus the number 1", "A compile error because you cannot add to pointers"], answer: 1 },
    { q: "What is 'pointer arithmetic' and why does adding 1 to a char* move exactly one byte?", options: ["It's undefined behavior according to the C standard", "The compiler scales the offset by the size of the pointed-to type; a char is 1 byte, so +1 moves 1 byte", "It rounds the resulting address to the nearest word boundary", "Pointers can only be incremented by powers of 2 at a time"], answer: 1 },
    { q: "What does array bracket notation like 's[2]' actually translate to in memory terms?", options: ["A hash lookup on the array's internal table", "Go to address s + 2 and read the value there — syntactic sugar for *(s + 2)", "A bounds-checked access that throws an error if out of range", "A copy of the element placed into a temporary variable"], answer: 1 },

    // malloc, free & memory layout
    { q: "What does malloc() do?", options: ["Declares a local variable on the stack frame", "Asks the OS for a chunk of heap memory and returns its address, or NULL on failure", "Copies a value into a specific memory address you provide", "Frees previously allocated memory and returns it to the OS"], answer: 1 },
    { q: "What is a memory leak?", options: ["A variable going out of scope and becoming inaccessible", "Allocating memory with malloc and never calling free(), so the program holds memory it no longer uses", "Reading from an uninitialized pointer and getting garbage data", "A stack overflow caused by too many recursive function calls"], answer: 1 },
    { q: "What is the rule of thumb for when you must call free()?", options: ["Every time a function returns to its caller", "Only for global variables allocated at program start", "If you malloc'd it, you must free it", "Only if the program runs longer than a few seconds"], answer: 2 },
    { q: "Where in memory does malloc allocate from, and where do local variables live?", options: ["Both live on the stack alongside function frames", "malloc uses the heap; local variables and function frames use the stack", "Both live on the heap managed by the OS allocator", "malloc uses the data segment; locals use CPU registers directly"], answer: 1 },
    { q: "What happens to the stack frame of a function after it returns?", options: ["It is immediately zeroed out for security purposes", "It is freed from the heap by the garbage collector", "The memory is conceptually reclaimed and may be reused, leaving garbage values", "It persists in memory until the entire program exits"], answer: 2 },
    { q: "Why can heap overflow and stack overflow collide in a long-running program?", options: ["They share the same underlying memory allocator", "The heap grows in one direction and the stack in the other; with enough allocations they can meet", "The OS prevents this automatically on all modern systems", "Only the stack can overflow; the heap grows without any limit"], answer: 1 },

    // NULL and error checking
    { q: "What does NULL represent in C memory terms?", options: ["An uninitialized integer with an undefined value", "Memory address 0x0, reserved so it can serve as a sentinel 'nothing here' value", "The null character \\0 that terminates C strings", "A pointer to a free block of memory available for use"], answer: 1 },
    { q: "Why should you check if malloc's return value equals NULL before using it?", options: ["malloc always returns NULL on success to indicate completion", "If the system is out of memory, malloc returns NULL, and dereferencing it would access invalid memory", "NULL means the memory is already initialized and ready to use", "It's only necessary on 32-bit systems that have limited address space"], answer: 1 },
    { q: "What is the difference between the null terminator '\\0' and the pointer value NULL?", options: ["They are identical values used in different contexts", "'\\0' is a single zero byte marking the end of a string; NULL is address 0x0 used as a pointer sentinel", "NULL is the null terminator when used in a pointer context", "'\\0' is only used when writing values in hexadecimal notation"], answer: 1 },

    // The copy bug & passing by value vs. reference
    { q: "If you copy a char* string variable with 't = s', what have you actually copied?", options: ["All the characters of the string into a new memory location", "The address stored in s — both s and t now point to the same characters", "The length of the string as an integer into the variable t", "A null-terminated duplicate of the original string into new memory"], answer: 1 },
    { q: "Why did the buggy swap function (taking int a, int b) fail to swap the caller's variables?", options: ["C doesn't support swapping values between two variables", "C passes arguments by value — a and b are copies, so changes to them don't affect the original variables", "The temp variable wasn't declared large enough to hold an int", "Integers can't be swapped without using pointer dereferencing"], answer: 1 },
    { q: "What change makes swap() correctly swap the caller's values?", options: ["Return both values from the function as a struct", "Change the parameters to int *a, int *b and pass &x, &y at the call site", "Use global variables so both functions share the same data", "Declare both a and b as static inside the function body"], answer: 1 },
    { q: "After fixing swap to use pointers, the line 'int temp = *a' does what?", options: ["Declares a new pointer a that points to temp's address", "Dereferences a to get the value at that address and copies it into temp", "Sets the pointer a equal to the address of the temp variable", "Swaps the values pointed to by a and b in a single operation"], answer: 1 },

    // Valgrind & debugging memory
    { q: "What class of bugs does Valgrind help find that a compiler typically won't catch?", options: ["Syntax errors caused by typos in variable names", "Memory errors: invalid reads/writes, uninitialized values, and memory leaks", "Logic errors in algorithms that produce wrong output", "Missing semicolons and mismatched parentheses in source code"], answer: 1 },
    { q: "Valgrind reports 'definitely lost: 12 bytes in 1 block' — what caused this?", options: ["A variable went out of scope before being used", "malloc was called but free was never called for that allocation", "An array was indexed out of bounds during a loop", "A null pointer was dereferenced during a function call"], answer: 1 },
    { q: "What is a garbage value?", options: ["A value the compiler explicitly initializes to zero", "Whatever bit pattern happens to be in a memory location that you haven't initialized yourself", "A special error value that malloc returns when it fails", "A negative integer used as a sentinel for error conditions"], answer: 1 },

    // File I/O
    { q: "What does fopen() return?", options: ["The number of bytes contained in the file on disk", "A FILE* pointer (address of a structure representing the open file), or NULL on error", "The file's contents as a single C string in memory", "A boolean value indicating whether the open succeeded"], answer: 1 },
    { q: "What is the difference between opening a file with 'w' vs 'a' mode in fopen?", options: ["'w' opens for reading only, while 'a' opens for writing", "'w' overwrites from the beginning; 'a' appends without destroying existing contents", "'a' is faster than 'w' because it skips to the end of the file", "They are interchangeable — both open the file for writing"], answer: 1 },
    { q: "Why does fprintf take a FILE* as its first argument when printf does not?", options: ["fprintf is an older function that uses a different API convention", "fprintf lets you direct output to a specific file; printf always goes to stdout", "fprintf requires a format string as a separate argument; printf doesn't", "There is no functional difference between fprintf and printf"], answer: 1 },
    { q: "In the byte-copy loop using fread/fwrite, why is the loop condition 'while fread(...) != 0' rather than checking a byte count?", options: ["fread always returns 0 regardless of what it reads from the file", "fread returns the number of items successfully read; it returns 0 at end-of-file, signaling the loop to stop", "fwrite returns 0 on success so the condition must check fread instead", "Byte counts are unreliable across different operating system platforms"], answer: 1 },

    // Design & safety
    { q: "Why is using scanf with a char* variable dangerous for reading strings of unknown length?", options: ["scanf doesn't support reading string inputs from the terminal", "You haven't allocated memory for the string, so scanf writes to a garbage address and can corrupt or crash the program", "scanf always truncates input to a single character regardless of buffer size", "char* variables can't hold more than one character at a time in memory"], answer: 1 },
    { q: "What is a buffer overflow?", options: ["Using too much stack memory with deeply nested recursive calls", "Writing more data into a fixed-size chunk of memory than it was allocated to hold", "Attempting to open a file that doesn't exist on the filesystem", "Calling malloc more than once without freeing between calls"], answer: 1 },
  ],
},
// ─── LECTURE 5 ───────────────────────────────────────────────────────
{
  course: "CS50",
  title: "CS50 Week 5: Data Structures",
  questions: [
    // Abstract data types — stacks & queues
    { q: "What does FIFO mean, and which data structure has this property?", options: ["First In, First Out — a stack", "First In, First Out — a queue", "Last In, First Out — a queue", "Last In, First Out — a stack"], answer: 1, explanation: "A queue processes items in the order they arrive (first in, first out), like a line at a store. A stack is the opposite (LIFO) \u2014 like a stack of trays where you take from the top." },
    { q: "What does LIFO mean, and which data structure has this property?", options: ["Last In, First Out — a queue", "First In, First Out — a stack", "Last In, First Out — a stack", "First In, First Out — a queue"], answer: 2 },
    { q: "What are the two fundamental operations of a queue?", options: ["push and pop", "insert and delete", "enqueue and dequeue", "add and remove"], answer: 2 },
    { q: "What are the two fundamental operations of a stack?", options: ["enqueue and dequeue", "push and pop", "insert and traverse", "malloc and free"], answer: 1 },
    { q: "Why are stacks and queues called 'abstract' data types?", options: ["They can only be implemented in abstract languages like Python or Java", "Their useful properties are defined independently of how they are implemented in memory", "They have no concrete code equivalent in any real programming language", "They exist only in theory and can never be coded or compiled"], answer: 1 },
    { q: "What is the core limitation of implementing a queue or stack with a fixed-size array?", options: ["You cannot iterate over the elements stored in it", "You must decide its maximum size at compile time, wasting or running out of memory", "Arrays don't support FIFO ordering of their elements", "The enqueue operation becomes O(n) instead of O(1)"], answer: 1 },

    // Arrays — memory model & resizing
    { q: "Why can't you simply extend an array by writing to the memory slot right after it?", options: ["C doesn't allow pointer arithmetic past an array boundary", "That adjacent memory may already be in use by another variable", "Arrays are read-only in memory after their initial allocation", "The compiler will catch this at build time and throw an error"], answer: 1 },
    { q: "When you resize an array by allocating a new larger chunk, what must you do with the original data?", options: ["The OS copies it automatically to the new location for you", "You must copy the old values into the new chunk manually or via realloc", "The old data is accessible via the original pointer indefinitely", "You free the old block and the values carry over to the new one"], answer: 1 },
    { q: "Why is copying a large array to a new location potentially very slow?", options: ["malloc itself is an O(n) operation that slows down with size", "You must copy every element one by one, so the work scales linearly with size", "The OS must pause other programs while performing the copy", "C doesn't have a memcpy function, so you always need a manual loop"], answer: 1 },
    { q: "What is the danger of doing 'list = malloc(4 * sizeof(int))' when list already points to a previously allocated chunk?", options: ["malloc will fail because that memory region is already allocated", "You lose the pointer to the original chunk, causing a memory leak", "The old memory is automatically freed when the pointer is reassigned", "list will point to both chunks simultaneously after the assignment"], answer: 1 },
    { q: "What does realloc do that a raw malloc + copy loop does not?", options: ["It allocates memory on the stack instead of the heap for speed", "It tries to extend the existing chunk in place, copying only if it must move the data", "It automatically checks for null on every call so you don't have to", "It frees the old memory before allocating the new block of memory"], answer: 1 },
    { q: "Even when using realloc, why must you use a temporary variable to store its return value instead of directly overwriting your pointer?", options: ["realloc may return the same address, causing confusion in the caller", "If realloc fails and returns null, you'd lose the pointer to the original data", "C requires two separate variables when working with any realloc call", "The temporary variable is needed to track the total allocation size"], answer: 1 },
    { q: "After a successful realloc into tmp, and before updating your main pointer, what should you do if realloc fails?", options: ["Call free(tmp) and continue with the original pointer", "Free the original list pointer before returning an error", "Simply return 0 — the OS reclaims all memory when the program ends", "Reassign list to null and let the caller handle the cleanup"], answer: 1 },

    // Linked lists — structure & mental model
    { q: "What two pieces of data does every node in a singly linked list contain?", options: ["A key and a value paired together", "A value and a pointer to the next node", "An index and a value for random access", "A pointer to the previous and next node"], answer: 1 },
    { q: "Why does a linked list not require its nodes to be contiguous in memory?", options: ["Linked lists use virtual memory that handles gaps between nodes automatically", "Each node stores the address of the next node, so the computer can follow pointers to find it anywhere", "The OS packs them together into a contiguous block behind the scenes", "C arrays are actually non-contiguous in memory too, so it's the same"], answer: 1 },
    { q: "What value is stored in the 'next' pointer of the last node in a linked list?", options: ["The address of the first node, creating a cycle", "-1 as a sentinel to mark the end of the list", "NULL", "0xFF as the universal end-of-list marker"], answer: 2 },
    { q: "To keep track of an entire linked list, what single variable do you need?", options: ["A pointer to every node stored in a separate array", "A pointer to the first node", "A pointer to the middle node for efficient search", "An integer storing the list's current total length"], answer: 1 },
    { q: "Why does a struct node need the tag 'struct node' inside its own definition (e.g., 'struct node *next') instead of just 'node *next'?", options: ["It's a style convention with no technical reason behind it", "The compiler reads top-to-bottom, so 'node' isn't defined yet when it sees the pointer field; the struct tag is already known", "You need two different names for the type to avoid naming collisions", "It tells the linker that this is a recursive type for special handling"], answer: 1 },
    { q: "What does the arrow operator '->' do in C?", options: ["Dereferences a pointer and accesses a struct field in one step, equivalent to (*ptr).field", "Returns the address of a specific struct field in memory", "Copies a struct to a new dynamically allocated memory location", "Checks if a pointer is null before attempting to dereference it"], answer: 0 },

    // Linked lists — insertion & pointer manipulation
    { q: "When prepending a new node to a linked list, what is the correct order of pointer updates?", options: ["Set list = newNode, then set newNode->next = list", "Set newNode->next = list, then set list = newNode", "Set newNode->next = null, then set list = newNode", "Set list = newNode — that's the only step needed"], answer: 1 },
    { q: "What goes wrong if you set 'list = newNode' before setting 'newNode->next = list'?", options: ["newNode->next will contain a garbage pointer value", "You overwrite list before saving where it pointed, orphaning all existing nodes", "The program will segfault immediately on the next access", "The old list is freed automatically when the pointer changes"], answer: 1 },
    { q: "Prepending to a linked list runs in O(1). Why?", options: ["You only manipulate a constant number of pointers at the front, regardless of list length", "C optimizes linked list operations at compile time into single instructions", "The list is sorted, so the correct front position is always found immediately", "Malloc runs in O(1) time so the overall operation is also O(1)"], answer: 0 },
    { q: "Why is searching a linked list O(n) even if it is sorted?", options: ["Sorting a linked list takes O(n) time before every search", "You can't do pointer arithmetic to jump to the middle; you must follow pointers from the start", "C doesn't allow comparison operators to be used on struct fields", "The null terminator at the end prevents any kind of early exit"], answer: 1 },
    { q: "When freeing a linked list, why can't you just call free(list) once?", options: ["free() only works on stack memory, not heap allocations", "free() has no knowledge of your data structure — you must free each individually malloc'd node yourself", "Calling free on a struct pointer will only free the first field of the struct", "You need to call free on the nodes in reverse order of allocation"], answer: 1 },
    { q: "When traversing a linked list to free it, why do you need a temporary pointer?", options: ["To satisfy the compiler's strict type system requirements", "Once you free a node, you can no longer read its 'next' field to find the following node", "free() sets the pointer to null automatically, so you'd lose your iteration variable", "C requires two separate pointers for any loop that involves struct types"], answer: 1 },

    // Big-O analysis of data structures
    { q: "What is the big-O time complexity of binary search on a sorted array?", options: ["O(1)", "O(log n)", "O(n)", "O(n log n)"], answer: 1 },
    { q: "What is the main performance advantage arrays have over linked lists?", options: ["Arrays can be resized in O(1) without copying any elements", "Arrays support binary search (O(log n)) because you can jump to any index via arithmetic", "Arrays use less memory per element than linked list nodes do", "Arrays are faster to allocate because malloc isn't needed"], answer: 1 },
    { q: "What is the big-O time for searching a singly linked list, even a sorted one?", options: ["O(1)", "O(log n)", "O(n)", "O(n²)"], answer: 2 },
    { q: "What is the space trade-off of a linked list compared to an array storing the same integers?", options: ["Linked lists use less memory because no contiguous block is required", "Linked lists use more memory because each node stores both data and a pointer", "They use the same amount of memory regardless of the data structure", "Arrays use more memory due to pre-allocated empty slots at the end"], answer: 1 },

    // Binary search trees
    { q: "What is the key ordering property of a binary search tree?", options: ["Every node is greater than all other nodes in the entire tree", "Every node's value is greater than its left child and less than its right child", "The left subtree is always larger in size than the right subtree", "Nodes are stored and retrieved in their original insertion order"], answer: 1 },
    { q: "Why can a binary search tree support O(log n) search?", options: ["Each node stores two values instead of one, doubling the search space", "At each node you can eliminate half the remaining tree based on a comparison, just like binary search on an array", "The tree is sorted left to right and can be read like a linear array", "Recursive calls are automatically memoized by the C compiler"], answer: 1 },
    { q: "What goes wrong if you insert values 1, 2, 3, 4, 5 in order into a naive binary search tree?", options: ["The tree violates the BST property and becomes completely unsearchable", "The tree devolves into a linked list shape, losing the O(log n) property", "Duplicate adjacent values cause a segfault during insertion", "The tree automatically rebalances itself to stay optimal after each insert"], answer: 1 },
    { q: "How does a BST node struct differ from a linked list node struct?", options: ["It stores a string key instead of an integer value", "It has two child pointers (left and right) instead of one (next)", "It includes a parent pointer to enable upward traversal", "It stores the subtree size for O(1) length queries on demand"], answer: 1 },
    { q: "Why is the recursive approach to BST search considered elegant?", options: ["Recursion is always faster than iteration when searching in C", "The BST property is itself recursive — it applies identically to every subtree, matching the recursive code structure", "C optimizes tail recursion so there's no additional stack overhead", "It avoids the need for pointer arithmetic when navigating nodes"], answer: 1 },

    // Hash tables
    { q: "What does a hash function do?", options: ["Encrypts a value so it can be stored securely in memory", "Maps an input from a potentially infinite domain to a value in a finite range (a bucket index)", "Sorts values into alphabetical order for faster sequential access", "Converts a string to an integer so it can be compared numerically"], answer: 1 },
    { q: "What is a 'collision' in a hash table?", options: ["Two pointers pointing to the same node in a linked list", "Two different keys hashing to the same bucket index", "An array index going out of bounds during a hash lookup", "A null pointer dereference inside a struct during insertion"], answer: 1 },
    { q: "How does chaining resolve collisions in a hash table?", options: ["It uses a second hash function to probe for a new empty bucket", "It stores a linked list at each bucket so multiple values can share the same index", "It doubles the array size whenever a collision is detected at any bucket", "It overwrites the older value with the newer one in the same slot"], answer: 1 },
    { q: "A hash table with n elements and k buckets, assuming uniform distribution, has chains of length roughly n/k. What is its big-O search time?", options: ["O(1) because k is a constant that divides n evenly", "O(log n) because of the sorted chains within each bucket", "O(n) because the constant factor k doesn't change asymptotic behavior", "O(k) because you only need to search within one chain"], answer: 2 },
    { q: "What is the fundamental trade-off when designing a hash function with more buckets?", options: ["More buckets means slower insertion due to hashing overhead", "More buckets reduces collisions but wastes more memory on unused slots", "More buckets causes the hash function itself to run in O(n) time", "More buckets forces you to use a linked list instead of a flat array"], answer: 1 },

    // Tries
    { q: "What is a trie's key structural insight that gives it O(1) search time?", options: ["It uses a sorted array at each level for fast binary search lookups", "The path through the trie encodes the key itself, so lookup time depends only on key length, not the number of stored keys", "Each node stores the full key so comparison is a single string operation", "It rebalances itself after every insertion to maintain optimal depth"], answer: 1 },
    { q: "In a trie indexed by English letters, what does each node contain?", options: ["A single character and a pointer to the next node in sequence", "An array of 26 child pointers (one per letter) plus a marker indicating whether a word ends here", "The full string from the root to that point stored as a character array", "A hash of the remaining suffix for O(1) child lookup at each level"], answer: 1 },
    { q: "What is the main cost of using a trie instead of a hash table?", options: ["Tries have O(n) insertion time that scales with the number of stored keys", "Tries use significantly more memory because most of the 26 child pointers in each node are null and unused", "Tries can only store strings and cannot be adapted for arbitrary data types", "Tries don't support deletion of keys once they have been inserted"], answer: 1 },
  ],
},
// ─── LECTURE 3 ───────────────────────────────────────────────────────
{
  course: "CS50",
  title: "CS50 Week 6: Python",
  questions: [
    // Interpreted vs compiled
    { q: "What is the key reason Python programs can run slower than equivalent C programs?", options: ["Python uses more memory by default for every variable", "Python interprets code line by line instead of running pre-compiled machine code", "Python doesn't support pointer arithmetic for low-level optimization", "Python programs are stored as larger files that take longer to load"], answer: 1, explanation: "C compiles to machine code once and runs natively on the CPU. Python is interpreted at runtime \u2014 each line must be translated on the fly, adding overhead. The trade-off is faster development time." },
    { q: "How do you run a Python program called hello.py?", options: ["make hello.py", "clang hello.py", "python hello.py", "./hello.py"], answer: 2 },
    { q: "What does Python's interpreter secretly do to speed up repeated execution of the same file?", options: ["It caches the program's output from the first run", "It compiles your code to bytecode and runs that instead of re-interpreting", "It converts Python to C source code behind the scenes first", "It skips functions whose code hasn't changed since the last run"], answer: 1 },
    { q: "Why don't Python programs need a compilation step before running?", options: ["Python code is already in machine code format on disk", "The Python program itself reads and executes your source file directly", "Python only runs in the browser where no compilation is needed", "Python files are pre-compiled when the interpreter is installed"], answer: 1 },

    // Syntax changes from C
    { q: "Python's print() adds a newline automatically. What named parameter overrides this?", options: ["newline=False", "end=''", "sep=''", "flush=True"], answer: 1 },
    { q: "What does the 'end' parameter in Python's print() default to?", options: ["An empty string", "A space character", "A backslash-n newline", "Nothing — there is no default"], answer: 2 },
    { q: "Why does Python not require you to declare a variable's type (e.g., 'int x')?", options: ["Python has no types and treats all data the same", "The interpreter infers the type from the value assigned", "All Python variables default to the string type initially", "Types are declared separately in a configuration header file"], answer: 1 },
    { q: "What replaces the two-vertical-bar '||' operator in Python conditionals?", options: ["or", "||", "OR", "either"], answer: 0 },
    { q: "In Python, what ends a conditional or loop header instead of an opening curly brace?", options: ["A semicolon", "A colon", "A parenthesis", "Nothing — whitespace alone is enough"], answer: 1 },
    { q: "Python requires indentation. What happens if you skip it?", options: ["The code runs but prints a deprecation warning", "Python infers the indentation from surrounding context", "The interpreter raises a syntax error", "Only the first line of the block needs proper indenting"], answer: 2 },

    // Strings & types
    { q: "What does Python's 'str' type correspond to from the CS50 C library?", options: ["char (a single character value)", "char * (a pointer to a character array)", "string (a CS50 typedef for char *)", "Both B and C — they are equivalent"], answer: 3 },
    { q: "When you call input() in Python, what type does it always return?", options: ["int if the user types a number at the prompt", "float if the user types a decimal at the prompt", "str, regardless of what the user typed", "It depends on the terminal settings being used"], answer: 2 },
    { q: "What happens if you add two variables from input() without converting them — e.g., x + y where x='1' and y='2'?", options: ["You get 3 because Python auto-converts numeric strings", "You get 12 — the strings are concatenated", "Python raises a TypeError about incompatible operands", "You get 1.0 + 2.0 as a float result"], answer: 1 },
    { q: "What is the correct way to convert a string from input() to an integer?", options: ["Cast it with C-style syntax: (int)x", "Call int(x)", "Call the x.to_int() method on the string object", "Assign it with a type declaration: int x = input()"], answer: 1 },
    { q: "Why is converting with int() called 'converting' rather than 'casting' as in C?", options: ["It's just a style preference with no technical difference", "Converting does more work — it parses the string's digits, not just reinterprets the bytes", "Python has no casting mechanism of any kind available", "int() is an object method, not a standalone function"], answer: 1 },
    { q: "Python does not have a 'char' type. How do you represent a single character?", options: ["Use the char() built-in constructor function", "Use a string of length 1", "Use an int with the ASCII value of the character", "Use a byte literal with the b prefix notation"], answer: 1 },

    // Object-oriented & string methods
    { q: "What does it mean that a Python string is an 'object'?", options: ["It is stored on the heap rather than the stack", "It has both data (the characters) and built-in methods like .lower() and .upper()", "It cannot be reassigned after it is first created", "It has a fixed size that is set at creation time"], answer: 1 },
    { q: "What is the difference between calling a function like toupper(c) in C versus s.upper() in Python?", options: ["There is no difference — they do the same thing in the same way", "Python's dot notation calls a method built into the object; C passes the data as an argument to an external function", "Python's version works on full strings while C's toupper only works on single chars", "Both B and C are true"], answer: 3 },
    { q: "You want to check if a user typed 'yes' regardless of capitalization. What is the most Pythonic approach?", options: ["Check s == 'yes' or s == 'YES' or s == 'Yes' explicitly", "Call s.lower() and compare against 'yes'", "Use a loop to check each character for its case individually", "Use a try/except block around the comparison to catch mismatches"], answer: 1 },
    { q: "Why does Python's == operator work correctly for comparing strings, unlike C?", options: ["Python strings are null-terminated just like C strings are", "Python's == compares character by character automatically, not raw memory addresses", "Python interns all strings so their memory addresses are always equal", "Python converts both strings to integers before comparing their values"], answer: 1 },

    // Lists & loops
    { q: "What is the Python equivalent of a C array, and what key advantage does it have?", options: ["A tuple — it is faster than an array for most operations", "A list — it can grow and shrink dynamically; you don't declare its size in advance", "A dict — it uses string keys instead of numeric indices", "A set — it automatically prevents duplicate elements"], answer: 1 },
    { q: "What does 'for i in range(3)' produce for i on each iteration?", options: ["1, 2, 3", "0, 1, 2", "0, 1, 2, 3", "It depends on the starting value"], answer: 1 },
    { q: "Why does Python use 'range()' instead of a traditional C-style for loop with init/condition/increment?", options: ["Python can't track a counter variable across iterations", "range() is more readable and expressive — you say what you want, not how to count it", "range() is faster than a manually managed counter loop", "Python's for loops can only iterate over iterable objects, not raw numbers"], answer: 1 },
    { q: "What does the 'in' keyword do when used with a list — e.g., 'if name in names'?", options: ["It checks that name is a valid variable in the current scope", "It performs a linear search through the list and returns True if found", "It returns the index of the element within the list", "It only works with strings, not other data types in lists"], answer: 1 },
    { q: "Python for loops can have an 'else' clause. When does the else block execute?", options: ["Always, after the loop finishes regardless of how it ended", "Only if the loop ran exactly zero iterations on the iterable", "Only if the loop completed without hitting a 'break'", "Only if the loop variable evaluates to a truthy value"], answer: 2 },
    { q: "If you want to signal that a loop variable is intentional but unused, what Pythonic convention replaces it?", options: ["Use 'none' as the variable name to indicate unused", "Use an underscore '_' as the variable name", "Omit the variable entirely from the for statement", "Use 'unused' as the variable name for clarity"], answer: 1 },
    { q: "Python has no '++' operator. What is the idiomatic replacement for i++ in Python?", options: ["i += 1", "i =+ 1", "increment(i)", "i.increment()"], answer: 0 },
    { q: "Python has no do-while loop. What is the Pythonic substitute when you need to prompt a user at least once?", options: ["Use a for loop with range(1, infinity) and break when done", "Use 'while True:' with a 'break' once valid input is received", "Use a recursive function that calls itself on invalid input", "Use a try/except block that retries automatically on failure"], answer: 1 },

    // Exceptions
    { q: "What problem do Python exceptions solve that return-value error checking in C does not?", options: ["They make code run faster by avoiding conditional checks", "They let functions signal errors out-of-band without stealing a return value or requiring the caller to check it", "They prevent all runtime errors automatically before they happen", "They replace the need for conditional logic in error handling entirely"], answer: 1 },
    { q: "What does a 'ValueError' in Python typically indicate?", options: ["A variable was used before being declared in the current scope", "A function received a value of the right type but an inappropriate value — e.g., int('cat')", "You tried to access memory you don't own, like in a segfault", "A module was not found when Python tried to import it"], answer: 1 },
    { q: "What is the structure of a try/except block in Python?", options: ["try: risky code — catch: handler", "try: risky code — except ExceptionType: handler", "attempt: risky code — handle: ExceptionType", "watch: risky code — except: handler"], answer: 1 },
    { q: "If int('cat') raises a ValueError inside a try block, what happens to the lines after int('cat') in the try block?", options: ["They execute normally as if no error occurred", "They are skipped — Python immediately jumps to the except block", "Python retries the failing line up to 3 times before giving up", "The program crashes regardless of the except block being present"], answer: 1 },

    // Dictionaries & data structures
    { q: "What Python built-in type implements a hash table with key-value pairs?", options: ["list", "set", "dict", "tuple"], answer: 2 },
    { q: "How do you look up a value by key in a Python dictionary — e.g., get David's number from a 'people' dict?", options: ["people.get('David')", "people['David']", "people->David", "people.David"], answer: 1 },
    { q: "What does a 'list of dicts' represent conceptually, as compared to a spreadsheet?", options: ["Each dict is a column; each list element is a single row value", "Each dict is a row (one record); each key-value pair is a column (one field)", "Each list is a sheet; each dict is a single cell value within it", "They are unrelated structures that cannot be meaningfully compared"], answer: 1 },

    // Modules & libraries
    { q: "What is a Python 'module'?", options: ["A compiled binary that Python loads at startup automatically", "A library — a file or package of reusable functions and objects", "A type of class that organizes related methods together", "A command-line tool for running Python scripts on a server"], answer: 1 },
    { q: "What is the difference between 'import sys' and 'from sys import argv'?", options: ["They are identical in behavior and produce the same result", "'import sys' loads the whole library, accessed via sys.argv; 'from sys import argv' brings argv directly into scope", "'from sys import argv' is slower because it loads the full module anyway", "'import sys' only works in Python 3, not in Python 2 or earlier"], answer: 1 },
    { q: "How do you install a third-party Python library that isn't included with the language?", options: ["Download and compile it from source code manually", "Use pip install <library-name>", "Add it to your imports and Python fetches it automatically from the web", "Copy the .py file directly into your project's source directory"], answer: 1 },
    { q: "What error type does Python raise when you try to import a library that isn't installed?", options: ["ImportError", "ValueError", "ModuleNotFoundError", "AttributeError"], answer: 2 },

    // Design & Pythonic patterns
    { q: "Why is it Pythonic to call 'main()' explicitly at the bottom of a Python file rather than relying on the interpreter?", options: ["Python requires it for all programs to run at all", "Python doesn't auto-call main() — it's just a function like any other, so you must call it yourself", "It makes the program execute significantly faster at runtime", "It is required whenever the program uses sys.argv for arguments"], answer: 1 },
    { q: "What is the purpose of the 'if __name__ == \"__main__\":' guard before calling main()?", options: ["It makes main() run faster by skipping initialization", "It prevents main() from running when the file is imported as a module by another file", "It is required syntax — Python won't run any file without it", "It replaces the need for defining a main() function entirely"], answer: 1 },
    { q: "What does the 'with' keyword do when opening a file in Python?", options: ["It opens the file in read-only mode by default", "It opens the file and automatically closes it when the indented block exits, even if an error occurs", "It locks the file so other programs can't access it simultaneously", "It is equivalent to 'import' for loading file-based modules"], answer: 1 },
  ],
},
// ─── LECTURE 7 ───────────────────────────────────────────────────────
{
  course: "CS50",
  title: "CS50 Week 7: SQL",
  questions: [
    // Declarative vs procedural programming
    { q: "What makes SQL a 'declarative' language, unlike Python or C?", options: ["It runs faster than procedural languages in all cases", "You declare what result you want, not the step-by-step procedure to get it", "It only supports read operations and cannot modify data", "It requires no variables or loops anywhere in the entire system"], answer: 1 },
    { q: "When you write 'SELECT language FROM favorites', who is responsible for deciding how to actually loop through the data?", options: ["You, by writing the loop yourself in the query", "The database engine, not you", "The CSV library that handles the underlying file", "Python's for-loop that iterates over the result set"], answer: 1 },

    // CSV & flat files
    { q: "Why does CSV use a delimiter character (comma) instead of fixed-width columns?", options: ["Commas are required by the operating system's file format", "A separator character allows columns of varying width while still parsing correctly", "It compresses the file to a significantly smaller size on disk", "Fixed-width columns are not supported by Python's standard library"], answer: 1 },
    { q: "What is the main fragility of using numeric indices (row[0], row[1]) to access CSV columns?", options: ["Numeric indices are slower than string keys for dictionary lookups", "If someone reorders the columns, your code silently reads the wrong data", "Python's CSV reader doesn't actually support numeric indexing at all", "It only works correctly on files that have exactly 3 columns"], answer: 1 },
    { q: "What advantage does csv.DictReader give you over csv.reader?", options: ["It's faster because it skips parsing the header row entirely", "Each row is a dictionary keyed by column name, so column order doesn't matter", "It automatically converts numbers from their string representation", "It can handle malformed files that have missing or extra commas"], answer: 1 },
    { q: "With csv.DictReader, why don't you need to call next(reader) to skip the header row?", options: ["DictReader ignores the first row of the file by default", "DictReader automatically consumes the header to learn column names", "The header is stored separately in a companion .header file", "You still need to call next() — it's optional but strongly recommended"], answer: 1 },

    // Python counting patterns & dictionaries
    { q: "Why does 'counts[favorite] += 1' raise a KeyError the first time a new key is encountered?", options: ["The += operator doesn't work on dictionary integer values", "The key doesn't exist yet, so there's no value to increment", "Python dictionaries are immutable once they are created", "favorite is not a valid type to use as a dictionary key"], answer: 1 },
    { q: "What is the design problem with using three separate variables (scratch=0, c=0, python=0) to count languages?", options: ["Three variables use too much memory for a simple counting task", "Adding a fourth language requires changing the code structure, not just the data", "Variables can't be incremented with the += compound operator", "You can't print variables and dictionary values in the same output loop"], answer: 1 },
    { q: "What does the Python keyword 'in' test when used like 'if favorite in counts'?", options: ["Whether the value exists somewhere in the dictionary's values", "Whether the key exists in the dictionary", "Whether the string is a substring of another string variable", "Whether the variable is defined in the current execution scope"], answer: 1 },

    // Relational databases & schema
    { q: "What is the core difference between a 'flat file database' (CSV) and a relational database?", options: ["CSVs can only store text data; databases can store binary data too", "Relational databases can express relationships across multiple tables; CSVs are a single table", "Databases are always faster than CSVs regardless of data set size", "CSVs require more disk space than equivalent relational database tables"], answer: 1 },
    { q: "In database terminology, what is a 'schema'?", options: ["A backup copy of the database stored on a separate disk", "The structure of the database — its tables, columns, and types", "The query used to import CSV data into a database table", "A temporary table returned by a SELECT query for the current session"], answer: 1 },
    { q: "What is a primary key?", options: ["The first column defined in any database table", "A column whose values uniquely identify every row in that table", "A column that is constrained to never contain NULL values", "The column specifically designated for joining two tables together"], answer: 1 },
    { q: "What is a foreign key?", options: ["A primary key imported from a different database product", "A column whose values reference the primary key of another table", "Any column that stores integer values in a table", "A key used for encrypting sensitive data in the database"], answer: 1 },
    { q: "Why does the lecture recommend using integer IDs as primary keys rather than strings like names?", options: ["Integers sort alphabetically faster than strings do", "Integers are a fixed, predictable size in memory, enabling efficient storage and search", "String columns can't be used as primary keys in SQLite at all", "Names are never unique enough to reliably identify individual rows"], answer: 1 },

    // CRUD operations
    { q: "In SQL, which command corresponds to 'reading' data?", options: ["GET", "SELECT", "FETCH", "READ"], answer: 1 },
    { q: "What does 'SELECT * FROM favorites' mean?", options: ["Select only the first row from the favorites table", "Select all columns from every row in the favorites table", "Select every table that exists in the entire database", "Count all rows in the favorites table and return the total"], answer: 1 },
    { q: "What does the WHERE clause do in a SELECT statement?", options: ["Sorts the results in ascending or descending order", "Filters rows to only those matching a condition", "Groups rows by a specified column for aggregation", "Limits the number of columns returned in the result"], answer: 1 },
    { q: "Why is 'DELETE FROM favorites' (without a WHERE clause) so dangerous?", options: ["It causes a syntax error that crashes the database engine", "It deletes every row in the table with no undo", "It only deletes the first row, which is misleading behavior", "It requires admin privileges that most users don't have"], answer: 1 },
    { q: "What does NULL represent in SQL, and why is it different from an empty string?", options: ["NULL means zero in numeric context; empty string means missing data", "NULL is the conscious absence of data; an empty string is still a value (just blank)", "They are identical — both represent the concept of no data present", "NULL means the column definition was deleted from the schema"], answer: 1 },
    { q: "What does 'DROP TABLE favorites' do that 'DELETE FROM favorites' does not?", options: ["DROP is faster because it avoids row-level locking", "DROP removes the table structure itself, not just its rows", "DROP requires a WHERE clause to specify which table to remove", "There is no difference — both delete all data from the table"], answer: 1 },

    // SELECT with functions & clauses
    { q: "What does 'SELECT COUNT(*) FROM favorites' return?", options: ["The names of all columns in the favorites table", "A single number: the total number of rows in the table", "The first row of the favorites table as a sample", "The number of distinct values found in each column"], answer: 1 },
    { q: "What does 'SELECT DISTINCT language FROM favorites' return?", options: ["Every language value repeated once for each matching row", "A deduplicated list of languages — each appears only once", "The single most common language in the table", "All languages sorted in alphabetical ascending order"], answer: 1 },
    { q: "What does 'GROUP BY language' do in a SELECT query?", options: ["Sorts the results by language in alphabetical order", "Collapses all rows with the same language into one group so aggregate functions work per group", "Filters out rows where the language column is NULL", "Joins two tables together using the language column as the key"], answer: 1 },
    { q: "What does the LIKE operator with a '%' wildcard do?", options: ["Matches an exact string with no flexibility for variation", "Matches a pattern where '%' represents zero or more characters", "Converts the value to lowercase before performing the comparison", "Matches only rows where the column contains a NULL value"], answer: 1 },
    { q: "Why does the lecture recommend aliasing 'COUNT(*)' as N (using AS)?", options: ["It's required for ORDER BY to work correctly in SQLite", "It gives the column a readable name and lets you reference it elsewhere in the query", "It makes the query execution plan run noticeably faster", "SQLite won't display COUNT(*) results without a named alias"], answer: 2 },

    // JOINs & relationships
    { q: "What type of relationship exists between shows and ratings (one show has exactly one rating)?", options: ["One-to-many", "Many-to-many", "One-to-one", "Zero-to-many"], answer: 2 },
    { q: "What type of relationship exists between shows and genres (one show can belong to multiple genres)?", options: ["One-to-one", "One-to-many", "Many-to-many", "None — genres are stored directly in the shows table"], answer: 1 },
    { q: "What type of relationship exists between shows and people (a person can be in many shows; a show has many people)?", options: ["One-to-one", "One-to-many", "Many-to-many", "Hierarchical"], answer: 2 },
    { q: "What is the purpose of the 'stars' table that links shows and people?", options: ["To store the star ratings that each person gave to a show", "To implement a many-to-many relationship using only two foreign key columns", "To duplicate show data across rows for faster lookup performance", "To store each person's full biography alongside their show credits"], answer: 1 },
    { q: "In a JOIN, why might the result contain duplicate data (e.g., a show's title repeated for each genre)?", options: ["JOIN has a bug that incorrectly copies rows into the output", "To produce a valid grid, the database repeats the fixed side to match every row on the many side", "The database stores duplicates internally to speed up future query lookups", "Duplicates only appear when you forget to include DISTINCT in the query"], answer: 1 },
    { q: "What is the key difference between solving a multi-table query with nested SELECT statements vs. JOIN?", options: ["JOINs are always faster regardless of the query structure", "Nested SELECTs take baby steps — each inner query solves one piece; JOINs combine tables in one breath", "Nested SELECTs are not valid SQL and will cause a syntax error", "JOINs are only valid for one-to-one relationship types between tables"], answer: 1 },

    // Indexes & performance
    { q: "What problem does creating an index solve?", options: ["It prevents duplicate rows from being inserted into the table", "It allows the database to find rows without scanning every row linearly", "It compresses the table to use significantly less disk space overall", "It enforces foreign key constraints between related tables automatically"], answer: 1 },
    { q: "What data structure does SQLite typically use when building an index?", options: ["A hash table", "A linked list", "A B-tree", "A binary heap"], answer: 2 },
    { q: "Why does creating an index take a moment of up-front time but save time on every subsequent query?", options: ["The index compresses the data, which requires significant CPU time", "The database must build the tree structure once from existing data; after that, lookups traverse the tree instead of scanning linearly", "SQLite downloads the pre-built index from a remote optimization server", "The index stores a backup copy of the entire table for redundancy"], answer: 1 },
    { q: "What is the trade-off of building an index on a frequently-updated column?", options: ["The column becomes effectively read-only after indexing", "The index must be updated on every INSERT/UPDATE/DELETE, adding overhead to writes", "The column can no longer store NULL values after being indexed", "Queries that use that column actually become slower with an index"], answer: 1 },

    // SQL + Python integration
    { q: "In the CS50 SQL library, what does db.execute() return?", options: ["A single string with all results concatenated together", "A list of dictionaries, one per row, keyed by column name", "A tuple of tuples with positional column access only", "A CSV-formatted string ready to write directly to a file"], answer: 1 },
    { q: "Why should you use a '?' placeholder in db.execute() instead of an f-string to insert user input?", options: ["f-strings are slower than placeholders in SQL execution contexts", "The library escapes dangerous characters in the placeholder, preventing SQL injection; f-strings do not", "? placeholders support multiple data types while f-strings only handle strings", "SQLite doesn't support f-strings at the database interpreter level"], answer: 1 },

    // SQL injection
    { q: "What is a SQL injection attack?", options: ["A performance attack that floods the database with thousands of SELECT queries", "An attack where malicious input is inserted into a query string, causing the database to execute unintended commands", "A technique to bypass index lookups and force full table scans instead", "An attack that corrupts the .db binary file directly on the filesystem"], answer: 1, explanation: "When user input is concatenated directly into SQL (e.g., via f-strings), an attacker can craft input that closes the original query and appends malicious commands like DROP TABLE or OR 1=1." },
    { q: "How does a single quote in user input become dangerous when using f-string interpolation in a SQL query?", options: ["It causes a Python SyntaxError before the query even runs", "It can prematurely close a string literal in the SQL, letting the attacker append arbitrary SQL commands", "It causes the database to return NULL for every column in the result", "SQLite rejects all queries that contain any single quote characters"], answer: 1 },
    { q: "What does '--' do in SQL, and why is it useful to an attacker?", options: ["It starts a multi-line comment that spans multiple lines of SQL", "It comments out everything to the right, letting an attacker nullify the rest of a query (e.g., the password check)", "It creates a delimiter between two separate SQL statements in a batch", "It's an arithmetic operator for performing subtraction on numeric columns"], answer: 1 },

    // Race conditions & transactions
    { q: "What is a race condition in the context of a database?", options: ["A query that runs slower than expected when the server is under heavy load", "Two operations reading and writing shared data concurrently, producing an incorrect result because neither knows the other is running", "An index that becomes outdated when rows are updated by multiple users simultaneously", "A deadlock caused by circular foreign key references between two related tables"], answer: 1 },
    { q: "In the Instagram likes example, why does the count end up wrong when two servers handle a like at the same time?", options: ["The database rounds floating-point values incorrectly during aggregation", "Both servers read the same old count, both compute count+1, and both write the same new value — losing one increment", "The JOIN across the users and posts tables returns duplicate rows in the count", "SQL UPDATE is not atomic by default in SQLite, so partial writes can occur"], answer: 1 },
    { q: "What does wrapping database operations in BEGIN TRANSACTION / COMMIT protect against?", options: ["SQL injection attacks from malicious user input in queries", "Race conditions — it ensures the enclosed operations run atomically, not interleaved with other operations", "Accidental DROP TABLE commands that would destroy the schema", "Foreign key constraint violations between related database tables"], answer: 1 },
  ],
},
{
  course: "CS50",
  title: "CS50 Week 8: HTML, CSS & JavaScript",
  questions: [
    // Networking fundamentals
    { q: "What is a router's primary job on the internet?", options: ["Store web pages for fast delivery to nearby clients", "Assign IP addresses to devices joining the network", "Forward packets toward their destination", "Translate domain names to their corresponding IP addresses"], answer: 2 },
    { q: "Why can a packet from point A to point B take different paths each time?", options: ["Routers randomly choose paths to balance the network load", "The internet has many redundant connections, so multiple valid routes exist", "DNS servers redirect packets based on current traffic conditions", "TCP chooses the exact path before any packets are sent"], answer: 1 },
    { q: "What problem does IP (Internet Protocol) solve?", options: ["Guarantees that all packets arrive in the correct order", "Gives every device a unique address so data can be routed to it", "Breaks large files into smaller chunks for efficient transmission", "Encrypts data between sender and receiver for security"], answer: 1 },
    { q: "An IPv4 address is 32 bits. What is the practical consequence of this?", options: ["It limits the maximum packet size to 32 bytes per transmission", "Only 32 devices can be connected on a single network segment", "There are roughly 4 billion possible addresses, which is becoming insufficient", "Routing tables on each router can only hold up to 32 entries"], answer: 2 },
    { q: "What does TCP add on top of IP?", options: ["Unique addresses for every device connected to the internet", "Guaranteed delivery and the ability to multiplex multiple services on one device", "Encryption of the packet contents to prevent eavesdropping", "Faster routing through fewer network hops between endpoints"], answer: 1 },
    { q: "Why does a packet include a sequence number?", options: ["So routers know how many hops remain before the packet expires", "So the recipient can reassemble fragments in the correct order and detect missing pieces", "To prevent duplicate packets from being accepted by the receiver", "To specify which DNS server should be used for the next lookup"], answer: 1 },
    { q: "Port numbers solve which specific problem?", options: ["Uniquely identifying devices on the internet globally", "Allowing one device to run multiple services and route incoming data to the right one", "Measuring round-trip latency between a client and a server", "Encoding IP addresses in a shorter and more readable format"], answer: 1 },
    { q: "By convention, HTTPS uses port 443. What would happen if a server used a non-standard port?", options: ["The connection would automatically fail without any error message", "Clients would need to specify the port explicitly in the URL", "The server's SSL certificate would become invalid and untrusted", "DNS resolution would break because the port is encoded in the domain"], answer: 1 },

    // DNS & DHCP
    { q: "What is the core function of a DNS server?", options: ["Assign IP addresses to new devices joining a local network", "Translate domain names like google.com into IP addresses", "Encrypt traffic between client and server using TLS certificates", "Store and serve web pages to browsers that request them"], answer: 1 },
    { q: "Why don't devices come pre-loaded with a complete list of IP addresses for every domain?", options: ["That data is too sensitive to store on consumer devices locally", "IP addresses change frequently and the list would be enormous and instantly stale", "Operating systems aren't permitted to store network lookup data", "DNS lookups are faster than reading from a local file on disk"], answer: 1 },
    { q: "What does DHCP do when you connect a device to a new network?", options: ["Encrypts your connection to the network automatically", "Automatically assigns your device an IP address, DNS server, and router address", "Downloads the latest DNS table to your device for local caching", "Opens port 80 for incoming web traffic on your device"], answer: 1 },
    { q: "DNS resolution is described as 'recursive' or hierarchical. What does that mean in practice?", options: ["Your device asks every DNS server in parallel and takes the fastest reply", "If your local DNS server doesn't know an answer, it escalates to higher-level servers and caches the result", "DNS servers only answer queries from devices within the same local network", "Each domain has exactly one authoritative DNS server that exists worldwide"], answer: 1 },

    // HTTP
    { q: "What does an HTTP status code of 200 mean?", options: ["The request was redirected to a new URL location", "The requested resource was not found on the server", "The request succeeded and the response contains the resource", "The server encountered an internal error while processing"], answer: 2 },
    { q: "When a server returns a 301 status code, what is it telling the browser?", options: ["The request was malformed — try again with the correct syntax", "The resource has permanently moved; go to this new URL instead", "Access denied — you need to authenticate before proceeding", "The server is temporarily unavailable and should be retried later"], answer: 1 },
    { q: "What does a 404 response mean?", options: ["The server is overloaded and cannot respond to the request now", "The client sent an invalid or unsupported HTTP version header", "The requested resource does not exist on that server", "The connection was encrypted incorrectly and must be retried"], answer: 2 },
    { q: "What is the GET verb used for in HTTP?", options: ["Sending form data to a server to create or update a resource", "Requesting a resource from a server", "Deleting a resource stored on the remote server", "Establishing an encrypted tunnel between client and server"], answer: 1 },
    { q: "What is the purpose of the 'Host' header in an HTTP request?", options: ["It specifies the port number the client wants to connect to", "It tells the server which domain the client wants, since one server can host multiple sites", "It authenticates the client's identity to the server", "It defines the preferred language the client wants content returned in"], answer: 1 },
    { q: "What does the Content-Type header in an HTTP response tell the browser?", options: ["How large the response body is measured in bytes", "What format the response body is in, so the browser knows how to render it", "Whether the response body is compressed with gzip encoding", "The version of HTML that the response document is written in"], answer: 1 },
    { q: "In a URL like 'https://www.example.com/folder/file.html?q=cats', what is 'q=cats'?", options: ["A file path fragment that identifies a section of the page", "An HTTP header included in the request automatically", "An HTTP parameter (key-value pair) passed to the server in the URL", "A CSS selector that targets elements with class 'cats'"], answer: 2 },
    { q: "Why is using HTTP (not HTTPS) considered a security risk?", options: ["HTTP uses port 80, which is blocked by most modern firewalls", "Traffic is unencrypted, so anyone between client and server can read it", "HTTP doesn't support persistent connections between requests", "HTTP lacks the ability to send POST requests with form data"], answer: 1 },

    // HTML structure & mental model
    { q: "When a browser reads an HTML file, what data structure does it build in memory?", options: ["A hash table mapping tag names to their content strings", "A linked list of elements in their document source order", "A tree (the DOM) representing the nesting of elements", "A flat array of all the text content found on the page"], answer: 2, explanation: "The DOM (Document Object Model) is a tree where each HTML element is a node. Nesting in HTML becomes parent-child relationships in the tree. JavaScript can then traverse and modify this tree to change what the user sees." },
    { q: "Why does the browser ignore extra whitespace in HTML source code?", options: ["Whitespace characters are stripped before the file is sent over the network", "The HTML spec says only explicit tags control spacing and layout", "The browser compresses the file before parsing it into the DOM tree", "Whitespace is only meaningful inside <pre> tags by long-standing convention"], answer: 1 },
    { q: "What is the difference between an HTML tag and an HTML element?", options: ["Tags are CSS selectors used in stylesheets; elements are JavaScript objects", "A tag is the opening or closing marker; an element includes both the opening tag, content, and closing tag", "Elements can have attributes assigned to them; tags cannot have any attributes", "There is no meaningful difference — the two terms are completely interchangeable"], answer: 1 },
    { q: "Why do some HTML tags (like <img>) not need a closing tag?", options: ["They are only valid when placed inside <head>, not inside <body>", "They are 'empty elements' — it doesn't make semantic sense for them to wrap content", "The browser automatically adds the missing closing tags for all elements", "Self-closing tags are a deprecated legacy syntax from older HTML versions"], answer: 1 },
    { q: "What is the semantic purpose of using <header>, <main>, and <footer> tags instead of just <div> everywhere?", options: ["They apply default CSS styles that <div> elements do not have", "They give search engines and assistive tools meaningful context about the page structure", "They are required by the HTML5 validator to produce valid markup", "They automatically center their contents in the middle of the page"], answer: 1 },

    // CSS
    { q: "What does the 'cascading' in Cascading Style Sheets mean?", options: ["Styles are applied from most specific to least specific, overriding each other", "CSS properties flow down from parent elements to their children", "Stylesheets are loaded one after another from multiple linked files", "CSS animations cascade over time to create sequential visual effects"], answer: 1 },
    { q: "What is the difference between a CSS class selector (.centered) and an ID selector (#harvard)?", options: ["Classes apply to one single element; IDs can be reused across many elements", "IDs apply to one unique element per page; classes can be reused on many elements", "Classes only work on <div> elements; IDs work on any element type", "There is no functional difference — it's just a naming convention preference"], answer: 1 },
    { q: "Why is it better design to put CSS in a separate .css file rather than inline style attributes on every tag?", options: ["Browsers load external CSS files faster than parsing inline styles", "It separates presentation from content, making both easier to read and maintain", "Inline styles are not considered valid HTML5 by the specification", "External files allow the browser to cache styles across multiple pages"], answer: 1 },
    { q: "Why does the CSS property 'background-color' become 'backgroundColor' in JavaScript?", options: ["JavaScript uses camelCase because hyphens are subtraction operators and would be parsed as math", "CSS property names are case-sensitive while JavaScript requires all-lowercase identifiers", "JavaScript only supports single-word property names with no separators allowed", "It's a legacy naming convention from early browser implementations that stuck"], answer: 0 },
    { q: "What does the CSS pseudo-selector 'a:hover' allow you to do?", options: ["Apply styles to all anchor tags permanently on the page", "Apply styles to an anchor only while the user's cursor is over it", "Hide a link until the user actively clicks on it to reveal the content", "Change the href destination attribute when the user hovers over it"], answer: 1 },

    // JavaScript & the DOM
    { q: "What is the key capability JavaScript adds to a webpage that HTML and CSS alone cannot provide?", options: ["Loading content from a remote server over the network", "Applying visual styles and colors to page elements", "Dynamically modifying the DOM (the page's content and structure) in response to user actions", "Defining the semantic structure and meaning of a document"], answer: 2 },
    { q: "What does document.querySelector('#name') return?", options: ["All elements on the page that have the class 'name'", "The element with the unique ID 'name'", "The text content of every element with the name attribute set to 'name'", "A CSS rule object that targets the ID selector 'name'"], answer: 1 },
    { q: "Why is it safer to place a <script> tag at the bottom of <body> rather than inside <head>?", options: ["Scripts placed in <head> are not executed by the browser at all", "Scripts execute when encountered; placing them last ensures the DOM elements they reference already exist", "Browsers load <body> scripts in parallel with other resources, making them faster", "Scripts placed in <head> block CSS stylesheets from loading properly"], answer: 1 },
    { q: "What does addEventListener('keyup', ...) do?", options: ["Fires once when the page loads and a key is already held down by the user", "Registers a callback to run every time a key is released while that element is focused", "Listens for predefined keyboard shortcuts configured in the browser settings", "Prevents the default keyboard behavior on that element from triggering"], answer: 1 },
    { q: "Why is it a security mistake to rely solely on HTML form validation (like the 'pattern' attribute) to protect your server?", options: ["The pattern attribute uses a different regex dialect than most server-side languages", "The user has a local copy of your HTML and can simply delete or modify those attributes", "Pattern validation only works on desktop browsers and is ignored on mobile devices", "Form validation attributes block JavaScript event handlers from running on the page"], answer: 1 },
    { q: "In the autocomplete example, why is inner HTML being set via JavaScript rather than written directly in the HTML file?", options: ["HTML files can't contain list items like <li> elements inside them", "The matching words aren't known until the user types — they must be generated dynamically at runtime", "Static HTML doesn't support <ul> and <li> tags without JavaScript enabled", "Writing HTML directly in the file is slower than generating it with JavaScript"], answer: 1 },
    { q: "What is window.setInterval used for in the blink example?", options: ["Delaying code execution by a fixed number of milliseconds just once", "Repeatedly calling a function at a set interval (e.g., every 500ms) indefinitely", "Measuring how long a given function takes to execute in milliseconds", "Pausing JavaScript execution entirely until a specified condition is met"], answer: 1 },
    { q: "What does navigator.geolocation.getCurrentPosition() do, and why does it take a callback function?", options: ["It immediately returns GPS coordinates from the device's location cache", "It requests the device's location asynchronously — the callback runs later when coordinates are available, since GPS may take time", "It opens a map application in a new browser tab centered at the user's location", "It queries a server-side API and returns the user's coordinates synchronously"], answer: 1 },
  ],
},
{
  course: "CS50",
  title: "CS50 Week 9: Flask & Web Applications",
  questions: [
    // Flask & frameworks
    { q: "What problem does Flask solve that writing your own Python web server would not?", options: ["It provides a built-in database for storing application data", "It handles low-level HTTP parsing so you focus only on your application's logic", "It runs faster than standard Python by using a C backend", "It compiles Python to machine code for better performance"], answer: 1, explanation: "Flask is a web framework that handles HTTP request/response parsing, URL routing, template rendering, and session management so you can focus on your application logic instead of networking code." },
    { q: "What command starts a Flask application?", options: ["python app.py", "http-server", "flask run", "pip start flask"], answer: 2 },
    { q: "By convention, what file must exist in your project directory for Flask to run?", options: ["server.py", "main.py", "app.py", "index.py"], answer: 2 },
    { q: "What is a 'framework' as opposed to a plain library?", options: ["A framework is only usable in Python and not other languages", "A framework is a library plus a set of conventions for how to use it", "A library requires more code to accomplish the same tasks", "There is no difference — both terms mean the exact same thing"], answer: 1 },
    { q: "What does 'from flask import Flask' achieve?", options: ["Installs Flask from the internet via pip automatically", "Gives access to the Flask constructor function from the flask package", "Starts the web server immediately on the default port", "Creates a virtual environment for the project to isolate dependencies"], answer: 1 },
    { q: "What does `app = Flask(__name__)` do?", options: ["Starts listening on port 5000 for incoming HTTP requests", "Turns the current file into a Flask application using its filename", "Imports all Flask routes from every file in the project automatically", "Creates a database connection to a default SQLite file"], answer: 1 },

    // Routes & decorators
    { q: "What is a Python decorator, as used in Flask routes?", options: ["A comment that documents a function's expected behavior", "A function that modifies the behavior of the function immediately below it", "A type of class inheritance specific to web frameworks", "A way to import external libraries into the current file"], answer: 1 },
    { q: "What does `@app.route('/')` tell Flask?", options: ["To run app.py from the project's root folder", "To associate the function directly below it with the slash URL path", "To redirect all incoming traffic to the home page automatically", "To make the function run once on server startup for initialization"], answer: 1 },
    { q: "What HTTP status code means the server had an internal error?", options: ["400", "404", "200", "500"], answer: 3 },
    { q: "What HTTP status code means 'method not allowed'?", options: ["400", "404", "405", "500"], answer: 2 },
    { q: "By default, which HTTP method does Flask assume a route supports?", options: ["POST", "GET", "PUT", "DELETE"], answer: 1 },
    { q: "How do you tell a Flask route to accept POST in addition to GET?", options: ["Add post=True to the route decorator as a parameter", "Pass methods=['GET','POST'] to @app.route", "Use @app.post_route as a separate decorator instead", "Flask supports all HTTP methods by default on every route"], answer: 1 },

    // Templates & Jinja
    { q: "What function renders an HTML template file in Flask?", options: ["flask.render()", "send_file()", "render_template()", "display_html()"], answer: 2 },
    { q: "Where must template HTML files be placed for Flask to find them automatically?", options: ["The root project folder alongside app.py", "A folder named 'views' inside the project", "A folder named 'templates'", "A folder named 'html' inside the project"], answer: 2 },
    { q: "In Jinja, what syntax outputs a variable's value into HTML?", options: ["${variable}", "<% variable %>", "{{ variable }}", "@(variable)"], answer: 2 },
    { q: "What are Jinja's percent-sign tags (e.g. {% if %}) used for, unlike the double-curly-brace tags?", options: ["Outputting variable values into the rendered HTML", "Control logic like conditionals and loops", "Defining CSS classes on HTML elements directly", "Including external CSS files in the template head"], answer: 1 },
    { q: "What is a Jinja 'block' such as `{% block body %}{% endblock %}` used for?", options: ["Blocking certain users from seeing specific page content", "Defining a placeholder region that child templates can fill in", "Adding JavaScript code to the rendered page output", "Creating a reusable CSS class for template elements"], answer: 1 },
    { q: "What does `{% extends 'layout.html' %}` do in a child template?", options: ["Copies layout.html into the current file permanently at build time", "Tells Flask this template should inherit the structure from layout.html", "Imports layout.html as a Python module that can be called from code", "Replaces the contents of layout.html with the current template file"], answer: 1 },
    { q: "What is the main design benefit of using layout.html with template inheritance?", options: ["It makes the site load faster by reducing HTTP requests", "Boilerplate HTML is written once and shared — reducing duplication across pages", "It allows Flask to pre-compile templates into bytecode for speed", "It removes the need for a separate templates folder in the project"], answer: 1 },
    { q: "Why does extra whitespace in Jinja templates not matter for what the browser renders?", options: ["Browsers cache whitespace separately from the page content", "HTML ignores superfluous whitespace and collapses it to a single space", "Flask strips all extra whitespace before sending the response", "Jinja automatically minifies the HTML output for production use"], answer: 1 },

    // GET vs POST & request object
    { q: "When using method='get' on a form, where do submitted values appear?", options: ["In a hidden cookie stored by the browser automatically", "In the request body where only the server can see them", "In the URL after a question mark as key=value pairs", "In the HTTP headers alongside the content type information"], answer: 2 },
    { q: "What is one security reason to prefer POST over GET for sensitive form data?", options: ["POST encrypts the data automatically using HTTPS", "POST prevents values from appearing in the browser's URL bar and history", "POST is faster than GET for large form submissions", "POST data is automatically deleted after one server request"], answer: 1 },
    { q: "In Flask, how do you access a GET parameter named 'name'?", options: ["request.params.get('name')", "request.form.get('name')", "request.args.get('name')", "request.query['name']"], answer: 2 },
    { q: "In Flask, how do you access a POST form field named 'name'?", options: ["request.args.get('name')", "request.form.get('name')", "request.body.get('name')", "request.post['name']"], answer: 1 },
    { q: "What does `request.args.get('name', 'world')` do when no name parameter is present?", options: ["Raises a KeyError exception immediately", "Returns None as the default missing value", "Returns 'world' as the default value", "Returns an empty string as a fallback"], answer: 2 },
    { q: "Why is it bad practice to validate a sport value only in the HTML form's dropdown?", options: ["HTML dropdowns are slow to render in most browsers", "A user can modify the HTML client-side to inject arbitrary values; server-side validation is required", "Dropdowns don't support the POST method for form submission", "Flask ignores all HTML-side validation rules completely"], answer: 1 },

    // Sessions & cookies
    { q: "What is an HTTP cookie in its simplest form?", options: ["A file stored on the server to track user analytics data", "A key-value pair the server plants in the browser to maintain state across requests", "An encrypted token required for all HTTPS connections to work", "A type of SQL record tied to a specific user account in the database"], answer: 1, explanation: "HTTP is stateless \u2014 each request is independent. Cookies solve this by letting the server attach data to the browser (via Set-Cookie header) that the browser sends back on every subsequent request (via Cookie header)." },
    { q: "What HTTP header does the server send to plant a cookie in the browser?", options: ["Cookie:", "Set-Cookie:", "New-Cookie:", "Session-Token:"], answer: 1 },
    { q: "What HTTP header does the browser send to present a previously-received cookie back to the server?", options: ["Set-Cookie:", "Auth-Token:", "Cookie:", "Session:"], answer: 2 },
    { q: "What is a session in web programming?", options: ["A permanent database record created for each registered user", "A persistent dictionary of key-value pairs associated with one user across multiple requests via cookies", "A TCP connection that stays open between all client-server requests", "A JavaScript object shared between all users of the web application"], answer: 1 },
    { q: "Why is storing a user's raw username and password in a cookie considered bad practice?", options: ["Cookies are deleted after one request and wouldn't persist", "The credential would be stored in the browser and visible to anyone with physical access", "Cookies can't store strings, only numeric session identifiers", "Flask forbids storing any type of password data in sessions"], answer: 1 },
    { q: "In Flask, what does `session.clear()` accomplish when a user logs out?", options: ["Deletes the database record associated with that user permanently", "Removes the session data associated with that user, effectively logging them out", "Closes the Flask server and terminates all active connections", "Deletes all cookies from all users currently on the application"], answer: 1 },
    { q: "Why does Incognito mode in a browser provide more privacy regarding tracking cookies?", options: ["It blocks all HTTP requests from being sent to any server", "It deletes session cookies when the window closes, removing the 'hand stamp'", "It encrypts all outgoing requests with an additional layer of security", "It prevents servers from setting any cookies on the browser at all"], answer: 1 },

    // Static files & redirects
    { q: "Where should static assets like images and CSS files be placed in a Flask project?", options: ["In the templates folder alongside HTML files", "In the root project directory next to app.py", "In a folder named 'static'", "Anywhere — Flask searches for them automatically by filename"], answer: 2 },
    { q: "Why would an <img src='cat.jpeg'> fail even if cat.jpeg exists alongside app.py?", options: ["Flask doesn't support serving image files of any type", "Static files must be in the 'static' folder; the browser can't access files outside it via the server", "The src attribute only accepts full URLs, not local filenames", "Images require installing a special Flask extension to be served"], answer: 1 },
    { q: "What does Flask's `redirect()` function do?", options: ["Reloads the current page in the user's browser", "Sends the user's browser to a different URL", "Renders a template file at a new URL path", "Forwards the POST data to a different route handler"], answer: 1 },
    { q: "After a POST form submission, why is redirecting to another route better than rendering a template directly?", options: ["Templates are slower to render than redirect responses", "It prevents the browser from resubmitting the form on page reload", "Redirect is required by the HTTP spec after every POST request", "It reduces server memory usage by not holding template data"], answer: 1 },

    // SQL in Flask & security
    { q: "Why is storing registrant data in a Python dictionary in memory problematic for a real application?", options: ["Dictionaries are slower than lists for lookups by key", "The data is lost whenever the server restarts because RAM is volatile", "Flask can't iterate over dictionaries in Jinja templates", "Dictionaries require significantly more code to update than databases"], answer: 1 },
    { q: "Why should you use SQL placeholders (?) rather than f-strings when inserting user input into a query?", options: ["F-strings don't work with most SQL library execute methods", "Placeholders prevent SQL injection by sanitizing user input before it reaches the database", "Placeholders are faster to execute than string interpolation approaches", "F-strings are deprecated in Python 3 and will be removed soon"], answer: 1 },
    { q: "Why is it important that a delete operation (like deregistering a user) uses POST rather than GET?", options: ["GET requests are cached by browsers and would fail on repeated attempts", "A GET request can be triggered by simply clicking a link, enabling attackers to cause deletions via phishing URLs", "DELETE operations require POST according to the HTTP specification", "POST is the only HTTP method Flask supports for any database writes"], answer: 1 },

    // MVC paradigm
    { q: "In the MVC paradigm as described in the lecture, what does the 'controller' correspond to in a Flask project?", options: ["The HTML templates stored in the /templates directory", "The SQL database file that persists all application data", "app.py — the Python logic that processes requests and decides what to show", "The requirements.txt file listing project dependencies"], answer: 2 },
    { q: "In MVC, what is the 'model'?", options: ["The HTML templates that the user sees rendered in the browser", "The persistent data layer — e.g., the SQLite database", "The Python functions that handle incoming HTTP route requests", "The CSS stylesheets that control the visual appearance of the page"], answer: 1 },

    // APIs & JSON
    { q: "What is an API in the context of web applications?", options: ["A type of SQL database used for web applications", "A web-based interface you can call to get data, often returning JSON", "A Flask extension for user authentication and authorization", "A JavaScript library for making HTTP requests from the browser"], answer: 1 },
    { q: "What does Flask's `jsonify()` function do?", options: ["Converts an HTML template to JSON format for the client", "Takes a Python list or dictionary and returns it as a JSON HTTP response", "Parses incoming JSON data from the client request body", "Validates JSON data against a predefined schema for correctness"], answer: 1 },
    { q: "What is JSON and why is it useful for APIs?", options: ["A Python-specific data format that is incompatible with other languages", "A text format representing arrays and dicts that any language can consume", "A compiled binary format designed for fast data transfer over networks", "An HTML extension for embedding structured data directly in web pages"], answer: 1 },

    // Checkboxes & multi-value inputs
    { q: "What HTML input type creates mutually exclusive options (only one selectable)?", options: ['type="checkbox"', 'type="radio"', 'type="select"', 'type="exclusive"'], answer: 1 },
    { q: "How does the browser know that multiple radio buttons belong to the same group?", options: ["They must be inside the same <div> element container", "They share the same 'name' attribute", "They share the same 'id' attribute value", "They are wrapped in a <fieldset> element together"], answer: 1 },
    { q: "What Flask method retrieves multiple checked checkbox values submitted under the same name?", options: ["request.form.get()", "request.args.getlist()", "request.form.getlist()", "request.form.all()"], answer: 2 },
  ],
}
];
