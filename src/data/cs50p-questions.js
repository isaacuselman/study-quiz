// ╔══════════════════════════════════════════════════════════════════╗
// ║  CS50P (Introduction to Programming with Python) lectures       ║
// ╚══════════════════════════════════════════════════════════════════╝

export const CS50P_LECTURES = [

// ─── LECTURE 0 ───────────────────────────────────────────────────────
{
  course: "CS50P",
  title: "CS50P Week 0: Functions & Variables",
  questions: [
    // Functions & arguments
    { id: "cs50p-w0-q1", q: "What is the relationship between a function and an argument?", options: ["An argument is a function that calls another function in sequence", "An argument is an input to a function that influences its behavior", "A function is a type of argument passed to other functions", "Arguments and functions are the same thing in Python"], answer: 1, explanation: "A function performs an action, and an argument is the value you pass into it to customize that action. For example, in print('hello'), 'hello' is the argument that tells print() what to display." },
    { id: "cs50p-w0-q2", q: "What is a 'side effect' in programming?", options: ["A bug caused by a function modifying shared state", "Something visible or audible that a function produces, like printing to the screen", "An unused return value that wastes computation time", "A variable that changes unexpectedly due to a scoping error"], answer: 1, explanation: "A side effect is any observable interaction with the outside world, such as displaying text on screen or writing to a file. It's distinct from a return value, which is handed back silently to the calling code." },
    { id: "cs50p-w0-q3", q: "What is the difference between a return value and a side effect?", options: ["They are the same thing expressed in different terminology", "A return value is handed back to the caller; a side effect is something observable like output to the screen", "A side effect is faster to produce than a return value", "Return values only work with strings, not other types"], answer: 1, explanation: "A return value is data passed back to the code that called the function, where it can be stored or used. A side effect is something the user can see or hear, like print() displaying text. They serve different purposes." },
    { id: "cs50p-w0-q4", q: "What is the difference between a 'parameter' and an 'argument'?", options: ["Parameters are in the function definition; arguments are the actual values passed when calling it", "They are completely unrelated concepts in programming", "Arguments are in the definition; parameters are the values passed in", "Parameters are only used for string-type inputs to functions"], answer: 0, explanation: "A parameter is the placeholder name in the function definition (e.g., 'name' in def greet(name)). An argument is the actual value you supply when calling the function (e.g., 'Alice' in greet('Alice'))." },

    // Variables & assignment
    { id: "cs50p-w0-q5", q: "What does the = sign mean in Python?", options: ["It tests whether two values are equal to each other", "It assigns the value on the right to the variable on the left", "It compares the types of two different values", "It creates a constant that cannot be changed later"], answer: 1, explanation: "The single = is the assignment operator. It evaluates the expression on the right side and stores the result in the variable on the left. Equality comparison uses == instead." },
    { id: "cs50p-w0-q6", q: "Why can't you just put a variable name inside quotes in a print statement to print its value?", options: ["Python doesn't allow variable names to appear inside print calls", "Anything inside quotes is treated as literal text, not as a variable reference", "You need to use single quotes instead of double quotes for variables", "It only works with numeric variables, not string variables"], answer: 1, explanation: "Quotes create a string literal, so Python treats the text exactly as written. To include a variable's value, use an f-string like f\"{name}\" or pass the variable as a separate argument outside the quotes." },
    { id: "cs50p-w0-q7", q: "What does 'scope' mean in Python?", options: ["The speed at which a variable is accessed from memory", "The region of code where a variable exists and can be used", "How large a value a variable can hold before overflowing", "Whether a variable is classified as a string or integer type"], answer: 1, explanation: "Scope determines where in your code a variable is visible and accessible. A variable defined inside a function has local scope and cannot be used outside that function." },
    { id: "cs50p-w0-q8", q: "If you define a variable inside function A, can function B access it directly?", options: ["Yes, all variables are automatically global in Python", "No — it only exists within function A's scope", "Only if both functions are defined in the same source file", "Only if the variable holds a string value, not a number"], answer: 1, explanation: "Variables defined inside a function are local to that function. Other functions cannot see or use them. To share data between functions, pass values as arguments or use return values." },

    // Strings & string methods
    { id: "cs50p-w0-q9", q: "What does the + operator do when used between two strings?", options: ["Adds their lengths together as an integer result", "Concatenates (joins) them into one string", "Compares them alphabetically and returns the first one", "Throws an error because + only works with numbers"], answer: 1, explanation: "When + is used between strings, Python concatenates them end-to-end. 'hello' + ' world' produces 'hello world'. The + operator behaves differently depending on the types of its operands." },
    { id: "cs50p-w0-q10", q: "What does an f-string (f\"...\") allow you to do?", options: ["Make strings execute faster at runtime", "Embed variable values directly inside a string using curly braces", "Convert strings to floats for mathematical operations", "Format strings as valid filenames on the operating system"], answer: 1, explanation: "F-strings (formatted string literals) let you embed expressions inside {curly braces} within a string. Python evaluates the expression and inserts the result, making string building cleaner than concatenation." },
    { id: "cs50p-w0-q11", q: "What does the .strip() method do to a string?", options: ["Removes all spaces from throughout the entire string", "Removes whitespace from the beginning and end only", "Converts every character in the string to lowercase", "Splits the string into a list at each whitespace boundary"], answer: 1, explanation: "strip() removes leading and trailing whitespace (spaces, tabs, newlines) but leaves characters in the middle untouched. This is useful for cleaning up user input that may have accidental extra spaces." },
    { id: "cs50p-w0-q12", q: "What is the difference between .capitalize() and .title()?", options: ["They do the same thing to the string's capitalization", ".capitalize() uppercases only the first letter of the string; .title() uppercases the first letter of every word", ".title() only works correctly on proper name strings", ".capitalize() also works on numeric string representations"], answer: 1, explanation: "capitalize() treats the entire string as one unit, uppercasing only the very first character. title() treats each word separately, uppercasing the first letter of every word, which is useful for formatting names." },
    { id: "cs50p-w0-q13", q: "You can chain multiple string methods like name.strip().title(). What happens?", options: ["Only the last method in the chain actually runs", "Each method runs on the result of the previous one, left to right", "It throws a syntax error because chaining is invalid", "They run in a random order determined at runtime"], answer: 1, explanation: "Method chaining works because each string method returns a new string. strip() returns a cleaned string, and then title() is called on that result. This lets you compose multiple transformations concisely." },
    { id: "cs50p-w0-q14", q: "What does the backslash (\\) do inside a string?", options: ["Ends the string at that position immediately", "Acts as an escape character, giving special meaning to the next character", "Divides the string into two separate substrings", "Comments out the rest of the line after it appears"], answer: 1, explanation: "The backslash is an escape character that changes the meaning of the next character. For example, \\n becomes a newline and \\t becomes a tab. To include a literal backslash, you write \\\\." },

    // Input function
    { id: "cs50p-w0-q15", q: "The input() function always returns what type of data?", options: ["An integer if the user types digits", "A float if the user types a decimal number", "A string", "Whatever type the user intended to enter"], answer: 2, explanation: "input() always returns a string (str), regardless of what the user types. Even if they type '42', you get the string '42', not the integer 42. You must explicitly convert with int() or float() if needed." },
    { id: "cs50p-w0-q16", q: "If a user types '2' in response to input(), and you add it to '3' with +, what do you get?", options: ["5, because Python auto-converts numeric strings", "23", "An error because the types are incompatible", "2.3 as a float concatenation result"], answer: 1, explanation: "Since input() returns strings, '2' + '3' performs string concatenation, not addition. The result is '23'. To get 5, you would need to convert both values to integers first with int()." },

    // Type conversion
    { id: "cs50p-w0-q17", q: "Why is int() needed when doing math with user input?", options: ["input() returns strings, and you can't do arithmetic on strings", "int() makes the program execute faster at runtime", "Python requires explicit casting for all arithmetic operations", "It's optional but recommended for code readability purposes"], answer: 0, explanation: "Since input() always returns a string, using + on two input values would concatenate them instead of adding them mathematically. int() converts the string to an integer so arithmetic operations work as expected." },
    { id: "cs50p-w0-q18", q: "What does int('cat') do?", options: ["Returns 0 as a default fallback value", "Returns the length of the string as an integer", "Raises an error because 'cat' can't be converted to an integer", "Returns None to signal the conversion failed"], answer: 2, explanation: "int() can only convert strings that represent valid integers (like '42'). The string 'cat' has no numeric meaning, so Python raises a ValueError. This is why try/except is important when converting user input." },
    { id: "cs50p-w0-q19", q: "What does float() do that int() does not?", options: ["It works with negative numbers as well as positive ones", "It preserves the decimal portion of a number", "It converts strings to numbers for use in expressions", "It rounds the result automatically to the nearest whole number"], answer: 1, explanation: "float() converts a value to a floating-point number, which can represent decimals like 3.14. int() truncates any decimal portion, so float() is necessary when you need to preserve fractional values." },

    // Print function parameters
    { id: "cs50p-w0-q20", q: "What does the 'end' parameter of print() control?", options: ["Which argument in the call prints last in the output", "What character is appended after the printed output (default: newline)", "When the program terminates after printing is done", "The final value returned by the print function call"], answer: 1, explanation: "By default, print() appends a newline character (\\n) after its output, which moves the cursor to the next line. You can change this with end='' to keep printing on the same line, or end=' ' to add a space instead." },
    { id: "cs50p-w0-q21", q: "What does the 'sep' parameter of print() control?", options: ["Whether arguments print on separate lines or together", "What character is placed between multiple arguments (default: space)", "The separator between consecutive function calls in a script", "Nothing — it's deprecated and no longer has any effect"], answer: 1, explanation: "When you pass multiple arguments to print(), Python inserts a separator between each one. The default separator is a space, but you can change it with sep=', ' or sep='-' or any string you want." },
    { id: "cs50p-w0-q22", q: "If you call print('a', 'b', 'c'), what appears by default?", options: ["abc", "a b c", "a, b, c", "'a' 'b' 'c'"], answer: 1, explanation: "print() separates multiple arguments with a space by default (the sep parameter defaults to ' '). So print('a', 'b', 'c') outputs 'a b c' with single spaces between each value." },
    { id: "cs50p-w0-q23", q: "How do you make two consecutive print() calls output on the same line?", options: ["Use print() only once with all values together", "Set end='' on the first print call to suppress the automatic newline", "Use a comma instead of making two separate print calls", "It's not possible to control where print places its output"], answer: 1, explanation: "Since print() adds a newline by default, the second call would start on a new line. Setting end='' on the first print suppresses that newline, so the next print continues on the same line." },

    // Named vs positional parameters
    { id: "cs50p-w0-q24", q: "What is a 'positional' parameter?", options: ["A parameter that has a default value assigned to it", "A parameter whose meaning is determined by its position in the argument list", "A parameter that must always be named explicitly in the call", "A parameter that only accepts integer values as arguments"], answer: 1, explanation: "Positional parameters get their values based on the order in which arguments are passed. The first argument fills the first parameter, the second fills the second, and so on." },
    { id: "cs50p-w0-q25", q: "What is a 'named' (keyword) parameter?", options: ["A parameter you refer to by name when calling the function, like end='\\n'", "A parameter that must always come first in the argument list", "A variable with a long descriptive name used as a parameter", "A parameter that specifies the name of the function being called"], answer: 0, explanation: "Named (keyword) parameters are specified using name=value syntax in the function call. This makes the code more readable and lets you pass arguments in any order, like print('hi', end='')." },

    // Defining your own functions
    { id: "cs50p-w0-q26", q: "What does the 'def' keyword do?", options: ["Deletes a function from the current namespace", "Defines (creates) a new function", "Defers execution of the next line to a later time", "Declares a new variable in the current scope"], answer: 1, explanation: "The def keyword defines a new function. It creates the function and gives it a name, but does not execute the function body. The body only runs when the function is called later." },
    { id: "cs50p-w0-q27", q: "Why must you call main() at the bottom of your file if you define it at the top?", options: ["Python requires main() to be the last function called in every program", "Defining a function doesn't execute it — you must explicitly call it", "main() only works when placed at the very end of a Python file", "It's just a style convention with no actual functional impact at all"], answer: 1, explanation: "Python's def keyword only creates a function; it doesn't run it. Without an explicit call to main() somewhere in your code, the function's body will never execute, no matter where it's defined." },
    { id: "cs50p-w0-q28", q: "What problem does defining a main() function solve for code organization?", options: ["It makes the code run significantly faster at execution time", "It lets you put your main logic at the top and helper functions below, since nothing runs until main() is called at the end", "It's required by Python syntax for all programs to be valid", "It prevents scope errors between local and global variables"], answer: 1, explanation: "Without main(), Python runs code top-to-bottom, so helper functions must be defined before they're used. With main(), you can put primary logic at the top for readability and define helpers below, since nothing executes until main() is called." },
    { id: "cs50p-w0-q29", q: "What does the 'return' keyword do inside a function?", options: ["Prints a value to the screen for the user to see", "Hands a value back to the caller so it can be used elsewhere", "Ends the entire program and exits to the terminal", "Restarts the function from its first line of code"], answer: 1, explanation: "return sends a value back to wherever the function was called. The caller can store it in a variable or use it in an expression. Unlike print(), return doesn't display anything on screen." },
    { id: "cs50p-w0-q30", q: "What happens if you define a function that uses return but you never store or use the returned value?", options: ["Python throws an error about the unused return value", "The value is computed and then discarded — nothing happens with it", "It prints the returned value automatically to the screen", "The program crashes with an unhandled exception error"], answer: 1, explanation: "Python doesn't require you to use a return value. If you call a function without capturing its result (e.g., just writing square(5) on its own), the returned value is simply discarded." },
    { id: "cs50p-w0-q31", q: "How do you give a parameter a default value?", options: ["def hello(to := 'world')", "def hello(to = 'world')", "def hello(to, default='world')", "You can't — all parameters are always required"], answer: 1, explanation: "You assign a default value in the function definition using = (e.g., def hello(to='world')). If the caller doesn't provide that argument, the default value is used automatically." },

    // Integers & floats
    { id: "cs50p-w0-q32", q: "Why can't a float represent every possible decimal number perfectly?", options: ["Floats are slower than ints and sacrifice precision for speed", "Computers have finite memory, so precision is limited", "Python doesn't fully support float operations natively", "Floats only store up to 2 decimal places of precision"], answer: 1, explanation: "Floating-point numbers use a fixed number of bits to represent values, which means they can only approximate most decimal numbers. This is why 1/3 or 0.1 can have tiny rounding errors in binary representation." },
    { id: "cs50p-w0-q33", q: "What does round(n) do when called with one argument?", options: ["Rounds to exactly 2 decimal places", "Rounds to the nearest integer", "Truncates the decimal portion completely", "Returns the absolute value of the number"], answer: 1, explanation: "With one argument, round() returns the nearest integer. You can optionally pass a second argument to specify decimal places, like round(3.14159, 2) which gives 3.14." },
    { id: "cs50p-w0-q34", q: "What does the ':,' inside an f-string like f\"{z:,}\" do?", options: ["Adds a currency symbol before the number", "Formats the number with commas as thousands separators", "Rounds to the nearest comma-separated value grouping", "Converts the variable z to a string representation"], answer: 1, explanation: "The :, format specifier inside an f-string inserts commas as thousands separators. So f'{1000000:,}' produces '1,000,000', making large numbers much easier to read." },
    { id: "cs50p-w0-q35", q: "What does ** do in Python (e.g., n ** 2)?", options: ["Performs standard multiplication on the two values", "Raises n to the power of 2", "Finds the square root of the left operand", "Repeats the string representation of n twice"], answer: 1, explanation: "The ** operator performs exponentiation. n ** 2 computes n squared, n ** 3 computes n cubed, and so on. It is different from * which performs regular multiplication." },

    // Nesting & readability trade-offs
    { id: "cs50p-w0-q36", q: "What is the risk of nesting too many function calls in a single line like print(int(input('x?')) + int(input('y?')))?", options: ["It won't compile because Python limits nesting depth", "It reduces readability and makes debugging harder", "It's slower than writing the same logic on multiple lines", "There is no risk — shorter code is always objectively better"], answer: 1, explanation: "Deeply nested expressions are hard to read and debug because you can't inspect intermediate values. If something goes wrong, it's difficult to tell which part of the nested expression caused the error." },
    { id: "cs50p-w0-q37", q: "When is it reasonable to combine multiple operations on one line?", options: ["Always — fewer lines of code is always better design", "Never — each operation needs its own separate line for clarity", "When the line stays readable and the logic is straightforward", "Only when using f-strings to format output values"], answer: 2, explanation: "Readability is the key consideration. Simple combinations like int(input('x? ')) are fine because the intent is clear. But if the line becomes hard to parse at a glance, breaking it into multiple lines is better." },
  ]
},

// ─── LECTURE 1 ───────────────────────────────────────────────────────
{
  course: "CS50P",
  title: "CS50P Week 1: Conditionals",
  questions: [
    // Boolean expressions & comparison operators
    { id: "cs50p-w1-q1", q: "Why does Python use == for equality instead of =?", options: ["It's an arbitrary syntax choice with no technical reason", "= is already used for assignment, so == distinguishes comparison from assignment", "== is faster to evaluate than a single equals sign", "= only works with strings so == was added for numbers"], answer: 1, explanation: "Since = assigns a value to a variable, Python needs a different symbol for testing equality. Using == prevents ambiguity: x = 5 stores 5 in x, while x == 5 asks whether x equals 5." },
    { id: "cs50p-w1-q2", q: "What does the != operator represent?", options: ["Assignment negation applied to a variable", "Not equal to", "Logical NOT applied to a single expression", "Bitwise inversion of the operand's value"], answer: 1, explanation: "The != operator checks whether two values are different. It returns True when the values are not equal and False when they are. It is the opposite of the == equality operator." },
    { id: "cs50p-w1-q3", q: "What is a boolean expression?", options: ["Any expression that involves mathematical operations", "A question that evaluates to true or false", "A variable declaration that stores text values", "A function that returns a formatted string"], answer: 1, explanation: "Boolean expressions evaluate to one of two values: True or False. They are the foundation of conditional logic, used in if statements, while loops, and anywhere Python needs to make a yes/no decision." },

    // if / elif / else control flow
    { id: "cs50p-w1-q4", q: "What is wrong with using three independent 'if' statements for mutually exclusive conditions?", options: ["It causes syntax errors in the Python interpreter", "All three questions are evaluated even after finding a true answer — wasteful and potentially buggy", "Python doesn't allow multiple if statements in a row", "It uses significantly more memory than alternatives"], answer: 1, explanation: "Separate if statements are independent checks, so Python evaluates all of them even after one matches. With elif, Python stops checking once it finds a true condition, which is both more efficient and prevents unintended double-matching." },
    { id: "cs50p-w1-q5", q: "What does 'elif' allow you to do that a second 'if' does not?", options: ["Ask more complex questions using nested logic", "Skip evaluation if a previous condition was already true", "Use different comparison operators in the check", "Return values directly from conditional branches"], answer: 1, explanation: "elif is part of the same decision chain as the preceding if. If the if condition was True, all elif and else blocks are skipped entirely. A separate if starts a new independent check." },
    { id: "cs50p-w1-q6", q: "When should you use 'else' instead of a final 'elif'?", options: ["When you want the code to execute faster overall", "When the remaining case is the only logical possibility and doesn't need an explicit check", "When else and elif are interchangeable in that context", "Only when there are exactly two conditions to handle"], answer: 1, explanation: "else is a catch-all that handles everything not covered by the preceding if/elif conditions. Use it when the remaining case is logically guaranteed and doesn't need its own explicit condition to be checked." },
    { id: "cs50p-w1-q7", q: "If you use 'if score >= 90' followed by 'if score >= 80' (not elif), and score is 95, what happens?", options: ["Only the first block runs because 90 is higher", "Both blocks run, because each 'if' is evaluated independently", "The program crashes due to a logic conflict", "Only the second block runs since it comes last"], answer: 1, explanation: "Since 95 >= 90 is True AND 95 >= 80 is also True, both blocks execute. Each if starts an independent check. Using elif instead of the second if would prevent this, because elif is only evaluated when the prior condition was False." },
    { id: "cs50p-w1-q8", q: "What does the colon at the end of an 'if' line signify?", options: ["It's optional decoration for readability purposes", "It marks the beginning of the indented block that belongs to that condition", "It ends the condition and returns the result", "It separates the condition from the variable name"], answer: 1, explanation: "The colon is required Python syntax that signals the start of an indented code block. It's used after if, elif, else, for, while, def, class, and other compound statements." },
    { id: "cs50p-w1-q9", q: "What happens if you don't indent the line after an 'if' statement in Python?", options: ["Python auto-indents it during the compilation step", "The program will not work — Python requires indentation to define blocks", "It runs but silently ignores the condition entirely", "It only matters for readability, not for execution"], answer: 1, explanation: "Python uses indentation (not braces) to define code blocks. This is not optional styling; it is part of the language syntax. An if statement with no indented body is a syntax error." },

    // or / and keywords
    { id: "cs50p-w1-q10", q: "What does the 'or' keyword do between two boolean expressions?", options: ["Runs both associated blocks of code sequentially", "Evaluates to true if at least one of the expressions is true", "Combines two variables into a single new variable", "Checks whether both expressions evaluate to false"], answer: 1, explanation: "The or operator returns True if at least one side is True. It uses short-circuit evaluation: if the left side is True, Python skips evaluating the right side since the overall result is already determined." },
    { id: "cs50p-w1-q11", q: "What does the 'and' keyword do between two boolean expressions?", options: ["Evaluates to true only if both expressions are true", "Evaluates to true if either expression is true", "Assigns both values to a variable simultaneously", "It behaves identically to the 'or' keyword"], answer: 0, explanation: "The and operator requires both sides to be True for the result to be True. If the left side is False, Python short-circuits and skips the right side since the result must be False." },
    { id: "cs50p-w1-q12", q: "In the grade program, why can you simplify 'if score >= 90 and score <= 100' to just 'if score >= 90' when using elif?", options: ["Python ignores the upper bound check automatically in chains", "The elif chain already ruled out higher values, so the upper bound is implicit", "You can't simplify it — both conditions are always required", "Scores above 100 don't exist in Python's integer system"], answer: 1, explanation: "In an if/elif chain, once a condition is True, all remaining branches are skipped. So by the time you reach elif score >= 90, you already know earlier conditions (like score > 100) were False, making the upper bound check redundant." },
    { id: "cs50p-w1-q13", q: "In Python, which expression is equivalent to '90 <= score and score <= 100'?", options: ["90 <= score <= 100", "score between 90 and 100", "score in range(90, 100)", "90 < score > 100"], answer: 0, explanation: "Python uniquely supports chained comparisons, letting you write 90 <= score <= 100 as a natural mathematical expression. It is syntactic sugar for (90 <= score) and (score <= 100)." },

    // Modulo operator
    { id: "cs50p-w1-q14", q: "What does the % (modulo) operator return?", options: ["The quotient of the division operation", "The remainder after dividing one number by another", "A percentage of the given value", "The absolute value of the operand"], answer: 1, explanation: "The modulo operator divides the left number by the right and returns the remainder. For example, 7 % 3 is 1 because 7 divided by 3 is 2 with a remainder of 1." },
    { id: "cs50p-w1-q15", q: "What is the value of 7 % 3?", options: ["2", "3", "1", "0"], answer: 2, explanation: "7 divided by 3 is 2 with a remainder of 1. The modulo operator returns that remainder, so 7 % 3 equals 1." },
    { id: "cs50p-w1-q16", q: "How can you determine if a number is even using the modulo operator?", options: ["n % 2 == 2", "n % 2 == 0", "n % 0 == 2", "n / 2 == 0"], answer: 1, explanation: "An even number is divisible by 2 with no remainder. n % 2 gives the remainder when dividing by 2, so if it equals 0, the number is even. If it equals 1, the number is odd." },

    // Boolean return values & function design
    { id: "cs50p-w1-q17", q: "What type does a function like 'is_even' return?", options: ["An int value such as 0 or 1", "A bool (True or False)", "A string like 'even' or 'odd'", "None by default in all cases"], answer: 1, explanation: "Functions named with 'is_' by convention return a boolean (True or False). This makes them natural to use in conditionals: if is_even(n) reads like plain English." },
    { id: "cs50p-w1-q18", q: "In Python, boolean values True and False must be written how?", options: ["All lowercase: true, false", "All uppercase: TRUE, FALSE", "Capital first letter: True, False", "Any capitalization works fine"], answer: 2, explanation: "Python is case-sensitive and True/False are keywords with specific capitalization. Writing 'true' or 'TRUE' will cause a NameError because Python doesn't recognize them as boolean values." },
    { id: "cs50p-w1-q19", q: "Why can 'return n % 2 == 0' replace an entire if/else block that returns True or False?", options: ["It's a Python shortcut that doesn't work in other contexts", "The expression itself evaluates to True or False, so wrapping it in if/else is redundant", "It returns the remainder instead of a boolean value", "It only works for even/odd checks and nothing else"], answer: 1, explanation: "The comparison n % 2 == 0 already produces True or False. Writing if n % 2 == 0: return True else: return False just wraps a boolean in unnecessary logic. Returning the expression directly is cleaner and equivalent." },
    { id: "cs50p-w1-q20", q: "What is the Pythonic one-liner equivalent of 'if n % 2 == 0: return True else: return False'?", options: ["return True if n % 2 == 0 else False", "return n % 2", "if n % 2 == 0 then True", "return (True, False)[n % 2]"], answer: 0, explanation: "Python's ternary expression (X if condition else Y) lets you express simple conditionals in one line. Even more Pythonic would be return n % 2 == 0, since the comparison already yields a boolean." },
    { id: "cs50p-w1-q21", q: "If a boolean expression already evaluates to True or False, what is the most concise way to return it?", options: ["Wrap it in an if/else block for clarity", "Use a match statement to handle it", "Return the expression directly", "Convert it to an int first then return"], answer: 2, explanation: "Since comparison operators like == and < already produce True or False, you can return the expression directly. Writing return x > 0 is cleaner than if x > 0: return True else: return False." },

    // Flow chart / control flow reasoning
    { id: "cs50p-w1-q22", q: "In a flowchart, what shape represents a decision (boolean question)?", options: ["Rectangle", "Oval", "Diamond", "Circle"], answer: 2, explanation: "In standard flowcharts, diamonds represent decision points where the flow branches based on a yes/no (boolean) question. Rectangles represent actions/processes, and ovals represent start/end points." },
    { id: "cs50p-w1-q23", q: "In a flowchart of an if/elif/else chain, what happens after a true branch executes its action?", options: ["It continues checking all remaining conditions afterward", "It goes directly to stop — remaining conditions are skipped", "It loops back to the first condition to recheck it", "It raises an exception and halts the flowchart"], answer: 1, explanation: "In an if/elif/else chain, once a True branch runs, the program skips all remaining conditions and continues after the entire chain. This is the key efficiency advantage over separate if statements." },
    { id: "cs50p-w1-q24", q: "What is the maximum number of questions evaluated in an if/elif/else chain with 3 conditions?", options: ["Always exactly 3 regardless of input values", "1 (best case) to 2 (worst case, since else needs no question)", "Exactly 2, because else handles the rest automatically", "It depends entirely on the variable type being checked"], answer: 1, explanation: "In an if/elif/else chain, if the first condition is True, only 1 question is evaluated. If neither if nor elif is True, 2 questions are checked and else runs without a question. So the maximum is 2 condition checks." },

    // match/case
    { id: "cs50p-w1-q25", q: "What keyword introduces the catch-all (default) case in a match statement?", options: ["default", "else", "case _:", "otherwise"], answer: 2, explanation: "In Python's match/case (added in 3.10), the underscore _ acts as a wildcard that matches anything. case _: is the catch-all, similar to 'default' in other languages' switch statements." },
    { id: "cs50p-w1-q26", q: "How do you match multiple values in a single case (e.g., Harry or Hermione)?", options: ["case 'Harry', 'Hermione':", "case 'Harry' or 'Hermione':", "case 'Harry' | 'Hermione':", "case ['Harry', 'Hermione']:"], answer: 2, explanation: "The pipe symbol | acts as an OR pattern in match/case statements. case 'Harry' | 'Hermione': matches if the value is either 'Harry' or 'Hermione', running the same block for both." },
    { id: "cs50p-w1-q27", q: "What happens if no case matches and there is no underscore catch-all?", options: ["Python raises an error and halts execution", "Nothing — the match block is silently skipped", "It matches the first case as a default fallback", "The program crashes with an unhandled exception"], answer: 1, explanation: "If no case pattern matches the value and there is no case _: catch-all, Python simply moves past the match block and continues with the next statement. No error is raised." },
    { id: "cs50p-w1-q28", q: "Unlike switch statements in C or Java, Python's match/case does NOT require what keyword?", options: ["case", "default", "break", "return"], answer: 2, explanation: "In C and Java, switch statements 'fall through' to subsequent cases unless you add break. Python's match/case automatically stops after executing the matched case, so break is unnecessary and not used." },

    // Design thinking
    { id: "cs50p-w1-q29", q: "Why is reducing the number of questions a program asks considered better design?", options: ["It makes the code shorter, which is always the priority", "Fewer checks means less chance of bugs, better readability, and (at scale) better performance", "Python charges per condition evaluated in the interpreter", "It only matters for interviews and coding assessments"], answer: 1, explanation: "Every condition is a place where logic can go wrong. Fewer conditions mean fewer opportunities for bugs, simpler code to read and maintain, and marginally better performance since fewer checks are evaluated." },
    { id: "cs50p-w1-q30", q: "What does 'pythonic' mean?", options: ["Code that only runs in the Python interpreter", "Code that follows Python community conventions for clean, idiomatic expression", "Code that uses no external libraries at all", "Code written directly by Python's original creator"], answer: 1, explanation: "Pythonic code leverages Python's unique features and conventions to be clear, concise, and readable. It follows the philosophy outlined in 'The Zen of Python' (try: import this), favoring simplicity and expressiveness." },
  ],
},

// ─── LECTURE 2 ───────────────────────────────────────────────────────
{
  course: "CS50P",
  title: "CS50P Week 2: Loops",
  questions: [
    // While loops & counting
    { id: "cs50p-w2-q1", q: "What is the fundamental risk of a while loop whose condition never becomes false?", options: ["It crashes immediately with a runtime error", "It runs forever (infinite loop)", "It skips the loop body entirely each time", "It throws a syntax error before running"], answer: 1, variants: [
      { q: "If you write a while loop and forget to update the variable in the condition, what is the most likely outcome?", options: ["The program halts with a traceback", "The loop body never runs at all", "The program enters an infinite loop", "Python detects the issue before execution"], answer: 2 },
      { q: "Which behavior describes a while loop whose test expression always evaluates to True?", options: ["It terminates after one pass through the body", "It raises a LogicError at runtime", "It produces a syntax warning in the editor", "It repeats endlessly without stopping"], answer: 3 },
      { q: "A student's while loop keeps running and never finishes. What is the most likely cause?", options: ["The loop condition is never becoming false", "The loop body contains a syntax error", "The variable was declared with the wrong type", "The indentation inside the body is incorrect"], answer: 0 },
      { q: "What happens when the boolean expression controlling a while loop stays true permanently?", options: ["Python automatically stops it after 1000 iterations", "The loop executes its body indefinitely", "The except block catches the resulting error", "The program skips ahead to the next function"], answer: 1 },
    ], explanation: "A while loop checks its condition before each iteration. If nothing inside the loop ever makes that condition False, the loop never stops. Always ensure something in the body moves toward termination." },
    { id: "cs50p-w2-q2", q: "In 'while i != 0', if i starts at 3 and is never modified, what happens?", options: ["The loop runs exactly 3 times then stops", "The loop never executes any iterations", "The loop runs forever", "Python raises an error about the condition"], answer: 2, variants: [
      { q: "You set x = 5 and write 'while x != 0' but never change x inside the loop. What is the result?", options: ["The loop runs five times and exits", "An infinite loop because x stays 5 forever", "The loop is skipped since 5 is not 0", "Python throws a NameError about x"], answer: 1 },
      { q: "Why does 'while count != 10' loop forever if count starts at 7 and the body doesn't update count?", options: ["count is always 7, so it never equals 10", "Python resets count to 0 each iteration", "The != operator doesn't work with integers", "The loop exits after a default timeout period"], answer: 0 },
      { q: "A loop reads 'while n > 0' and n starts at 4 but is never decremented. How many iterations occur?", options: ["Exactly four iterations total", "Zero — the condition is checked but never satisfied", "Infinitely many iterations", "One iteration before Python detects the issue"], answer: 2 },
      { q: "What must happen inside a while loop's body to ensure it eventually terminates?", options: ["The body must contain a print statement", "Something must change so the condition becomes false", "The loop variable must be a global variable", "The body must call a built-in function each pass"], answer: 1 },
    ], explanation: "If the loop variable is never modified inside the body, the condition stays the same forever. The body must change something (typically the loop variable) so the condition eventually becomes False." },
    { id: "cs50p-w2-q3", q: "What does Ctrl+C do when a program is stuck in an infinite loop?", options: ["Copies the output to the clipboard", "Interrupts/kills the running process", "Closes the terminal window entirely", "Restarts the program from the beginning"], answer: 1, variants: [
      { q: "Your Python script is frozen in an endless loop. Which keyboard shortcut stops it?", options: ["Ctrl+Z to undo the last action", "Ctrl+C to send an interrupt signal", "Ctrl+S to save and halt the process", "Ctrl+X to cut the running process"], answer: 1 },
      { q: "What signal does pressing Ctrl+C send to a running program in the terminal?", options: ["A save signal that writes output to disk", "A copy signal that duplicates the text", "An interrupt signal that terminates execution", "A pause signal that freezes the program"], answer: 2 },
      { q: "If you need to forcefully stop a Python program that won't finish on its own, what do you press?", options: ["Ctrl+D to send end-of-file", "Ctrl+Q to quit the terminal app", "Ctrl+C to interrupt the process", "Ctrl+R to restart the interpreter"], answer: 2 },
      { q: "A student accidentally runs an infinite loop. Which action immediately halts the program?", options: ["Closing the code editor window", "Pressing Ctrl+C in the terminal", "Waiting for Python's built-in timeout", "Pressing Enter to skip to the next line"], answer: 1 },
    ], explanation: "Ctrl+C sends a SIGINT (interrupt signal) to the running process, which causes Python to raise a KeyboardInterrupt exception and terminate the program. It is the standard way to stop a runaway process." },
    { id: "cs50p-w2-q4", q: "Why do most programmers start counting at 0 instead of 1?", options: ["It's significantly faster for the CPU to process", "It's a convention with practical benefits in indexing and ranges", "Python requires starting all loops at zero", "It prevents infinite loops from occurring"], answer: 1, variants: [
      { q: "What is the main reason zero-based counting is standard practice in programming?", options: ["It aligns with how indexing and range functions work", "It reduces memory usage in all languages", "Zero is the only valid starting value in Python", "It eliminates off-by-one errors completely"], answer: 0 },
      { q: "A new programmer asks why range(3) starts at 0 rather than 1. What is the best explanation?", options: ["Python chose this arbitrarily with no deeper reason", "Starting at 0 is a widespread convention that matches array indexing", "The CPU processes zero faster than other numbers", "Starting at 1 would cause an error in range()"], answer: 1 },
      { q: "How does zero-based counting relate to list indexing in Python?", options: ["They are unrelated concepts with no connection", "Both follow the convention that the first position is 0", "Zero-based counting only applies to loops, not lists", "List indexing always starts at 1 unlike loop counters"], answer: 1 },
      { q: "Which statement best explains why 'for i in range(3)' gives 0, 1, 2 rather than 1, 2, 3?", options: ["range() has a bug that shifts values down by one", "It follows the zero-based convention used throughout Python", "Starting from 1 would require extra memory allocation", "Python inherited this behavior from a specific hardware design"], answer: 1 },
    ], explanation: "Zero-based counting aligns with how arrays and lists are indexed in most programming languages. Starting at 0 means range(n) and list indices 0 through n-1 work naturally together." },
    { id: "cs50p-w2-q5", q: "What does 'i += 1' do compared to 'i = i + 1'?", options: ["It's faster at runtime due to optimization", "Exactly the same thing — it's just shorter syntax", "It increments the variable by 2 instead of 1", "It only works inside loops, not elsewhere"], answer: 1, variants: [
      { q: "Which statement about the += operator in Python is correct?", options: ["It performs a different operation than explicit addition and assignment", "It is shorthand that does the same thing as writing x = x + value", "It can only be used with integer types in Python", "It modifies the variable in place without reassignment"], answer: 1 },
      { q: "A student replaces 'count = count + 1' with 'count += 1'. How does this affect the program?", options: ["The behavior changes because += works differently", "The program runs identically — it's just more concise", "It causes a syntax error outside of loop bodies", "The variable is now incremented by 2 each time"], answer: 1 },
      { q: "What does the expression 'x += 5' mean in Python?", options: ["Set x to 5, replacing its current value entirely", "Add 5 to x and store the result back in x", "Compare x to 5 and return a boolean result", "Create a new variable named x5 with the sum"], answer: 1 },
      { q: "If score = 10 and you write 'score += 3', what is score afterward?", options: ["10 — the original value is unchanged", "3 — the new value replaces the old one", "30 — it multiplies score by 3", "13 — it adds 3 to the existing value"], answer: 3 },
    ], explanation: "The += operator is shorthand for adding a value to a variable and reassigning the result. x += 1 is identical to x = x + 1. Similar shorthands exist for other operators: -=, *=, /=." },
    { id: "cs50p-w2-q6", q: "If you loop 'while i < 3' starting from i = 0, how many times does the body execute?", options: ["2", "3", "4", "It depends on what's in the body"], answer: 1, variants: [
      { q: "With i starting at 0 and incrementing by 1 each pass, 'while i < 5' runs the body how many times?", options: ["4 times total", "5 times total", "6 times total", "It varies based on the body"], answer: 1 },
      { q: "You write 'i = 0' then 'while i < 3' with 'i += 1' at the end of the body. Which values does i take inside the loop?", options: ["1, 2, 3 across three iterations", "0, 1, 2 across three iterations", "0, 1, 2, 3 across four iterations", "1, 2 across two iterations only"], answer: 1 },
      { q: "Why does 'while i < 3' (starting at i = 0, incrementing by 1) execute exactly 3 times rather than 4?", options: ["When i reaches 3, the condition i < 3 is false so the loop stops", "Python imposes a limit of 3 iterations for safety reasons", "The loop skips the first iteration when i equals 0", "It actually does execute 4 times in this scenario"], answer: 0 },
      { q: "If you change 'while i < 3' to 'while i <= 3' (starting i = 0, incrementing by 1), how does the count change?", options: ["It stays the same at 3 iterations total", "It increases from 3 iterations to 4 iterations", "It decreases from 3 iterations to 2 iterations", "It causes an infinite loop that never terminates"], answer: 1 },
    ], explanation: "With i starting at 0 and < 3, the body runs for i = 0, 1, 2 (3 times). The loop stops when i reaches 3 because the condition i < 3 is False. Careful counting like this avoids off-by-one errors." },

    // For loops & range
    { id: "cs50p-w2-q7", q: "What does 'for i in [0, 1, 2]' do?", options: ["Creates a list and then immediately ignores it", "Assigns i to each value in the list sequentially, executing the body each time", "Runs the body once with i set to the whole list itself", "Checks whether i is already present in the list"], answer: 1, variants: [
      { q: "How does a for loop process the elements of a list like [10, 20, 30]?", options: ["It picks one random element and runs the body once", "It sets the variable to each element in turn, running the body for each", "It sums the elements and passes the total to the body", "It checks if the variable exists in the list already"], answer: 1 },
      { q: "When Python encounters 'for x in [5, 6, 7]', what value does x have during the second iteration?", options: ["5, the first element of the list", "6, the second element of the list", "7, the last element of the list", "2, the index of the second element"], answer: 1 },
      { q: "What is the role of the variable name in 'for item in my_list'?", options: ["It holds the entire list during each iteration", "It receives one element from the list on each pass through the loop", "It stores the current index position as an integer", "It is a label that Python ignores completely"], answer: 1 },
      { q: "Which description best matches what 'for n in [3, 6, 9]' accomplishes?", options: ["It creates three separate variables named n", "It checks whether n divides evenly into 3, 6, and 9", "It iterates the body three times with n set to 3, then 6, then 9", "It assigns n to the list object [3, 6, 9] as a whole"], answer: 2 },
    ], explanation: "A for loop iterates over a sequence, assigning each element to the loop variable one at a time. On each iteration, the variable holds a single element, not the whole list." },
    { id: "cs50p-w2-q8", q: "Why is 'for i in [0, 1, 2, ..., 999999]' a bad way to loop a million times?", options: ["It's too slow for the Python interpreter to handle", "You'd have to manually type out every number", "Python can't handle lists that large in memory", "for loops only work with small lists under 1000"], answer: 1, variants: [
      { q: "What problem does range() solve that a hand-written list of numbers does not?", options: ["range() runs the loop body faster than a list would", "range() generates numbers on demand so you don't type or store them all", "range() allows floating-point values unlike lists", "range() automatically handles infinite sequences safely"], answer: 1 },
      { q: "A student writes [0, 1, 2, 3, ..., 49] to loop 50 times. Why is this approach impractical for large counts?", options: ["Python rejects lists with more than 50 items", "Manually listing every number becomes tedious and error-prone at scale", "Lists of integers consume no memory so it's actually fine", "for loops ignore lists longer than 100 elements"], answer: 1 },
      { q: "What is the practical disadvantage of hard-coding a list like [0, 1, 2, ..., 99] instead of using range(100)?", options: ["The loop runs in reverse order with a list", "You must write out each value by hand, which is impractical for large ranges", "Python interprets the list as strings, not integers", "Hard-coded lists cannot be used with for loops"], answer: 1 },
      { q: "If you wanted to iterate exactly 10,000 times, why would range(10000) be preferred over a literal list?", options: ["range() uses less memory and doesn't require typing every number", "Lists always start from 1, but range starts from 0", "Literal lists can only hold up to 256 elements", "range() skips even numbers to save processing time"], answer: 0 },
    ], explanation: "range() generates numbers on demand without storing them all in memory, and you don't have to type out each number. For large sequences, this saves both developer effort and computer memory." },
    { id: "cs50p-w2-q9", q: "What does range(3) return?", options: ["The list [1, 2, 3]", "A sequence of values 0, 1, 2", "The integer number 3", "The list [0, 1, 2, 3]"], answer: 1, variants: [
      { q: "Which values does 'for i in range(4)' assign to i across all iterations?", options: ["1, 2, 3, 4 across four iterations", "0, 1, 2, 3 across four iterations", "0, 1, 2, 3, 4 across five iterations", "4 repeated across four iterations"], answer: 1 },
      { q: "What is the last value produced by range(5)?", options: ["5, since it counts up to and including the argument", "4, since range stops before the argument value", "1, since range counts backwards from the argument", "0, since range only produces a single starting value"], answer: 1 },
      { q: "If you convert range(3) to a list with list(range(3)), what do you get?", options: ["[1, 2, 3]", "[0, 1, 2, 3]", "[3]", "[0, 1, 2]"], answer: 3 },
      { q: "How many values does range(6) produce, and what is the first one?", options: ["5 values, starting at 1", "6 values, starting at 0", "7 values, starting at 0", "6 values, starting at 1"], answer: 1 },
    ], explanation: "range(n) produces n values starting from 0 up to n-1. So range(3) gives 0, 1, 2. The argument specifies how many values to produce, not the last value." },
    { id: "cs50p-w2-q10", q: "What is the pythonic convention when a loop variable is never used in the body?", options: ["Name it 'unused' to signal intent", "Name it '_' (underscore)", "Delete it after the loop completes", "Leave it unnamed in the definition"], answer: 1, variants: [
      { q: "You want to repeat an action 5 times but don't need the counter value. What variable name signals this intent in Python?", options: ["temp as a placeholder name", "_ (a single underscore)", "i as the standard counter name", "null to indicate no value needed"], answer: 1 },
      { q: "What does 'for _ in range(3)' communicate to someone reading the code?", options: ["The loop variable is private to the module", "The loop body doesn't use the iteration variable", "The loop runs in debug mode without output", "The underscore catches errors from range()"], answer: 1 },
      { q: "Which naming choice follows Python convention for a throwaway loop variable?", options: ["Naming it 'x' as a generic placeholder", "Naming it 'throwaway' to be descriptive", "Using a single underscore character: _", "Omitting the variable name entirely"], answer: 2 },
      { q: "Why would a programmer write 'for _ in range(10)' instead of 'for i in range(10)'?", options: ["The underscore makes the loop run faster", "It signals that the iteration value is intentionally unused", "Python requires _ for ranges above 5", "It prevents the variable from using memory"], answer: 1 },
    ], explanation: "The underscore _ is a Python convention for throwaway variables. Using it tells readers that the loop just needs to repeat a certain number of times and the actual counter value is irrelevant to the logic." },
    { id: "cs50p-w2-q11", q: "What does 'print(\"meow\" * 3)' output?", options: ["meow meow meow (with spaces between)", "meowmeowmeow (concatenated)", "An error — you can't multiply strings by integers", "meow\\nmeow\\nmeow (on separate lines)"], answer: 1, variants: [
      { q: "What does the * operator do when applied between a string and an integer in Python?", options: ["It raises a TypeError since types don't match", "It repeats the string that many times, concatenated together", "It splits the string into that many equal parts", "It converts the string to a number and multiplies"], answer: 1 },
      { q: "If you write 'ha' * 4, what string is produced?", options: ["ha ha ha ha with spaces between each", "hahahahahaha — 'ha' repeated four times in a row", "An error because strings can't use arithmetic operators", "h4a4 with digits inserted between characters"], answer: 1 },
      { q: "A student wants to print 'abc' five times on one line with no separators. Which expression achieves this?", options: ["print('abc', 5) with a count argument", "print('abc' + 5) using addition", "print('abc' * 5) using string repetition", "print(5 + 'abc') reversing the operands"], answer: 2 },
      { q: "What is the result of the expression '=-' * 3 in Python?", options: ["An error due to the special characters involved", "The string '=-' printed three times on separate lines", "The string '=-=-=-' with no spaces or line breaks", "The number -3 after mathematical evaluation"], answer: 2 },
    ], explanation: "The * operator with a string and an integer repeats the string that many times, concatenating the copies. 'meow' * 3 produces 'meowmeowmeow' with no separators between repetitions." },

    // Break, continue, and input validation
    { id: "cs50p-w2-q12", q: "What does 'break' do inside a loop?", options: ["Pauses the loop temporarily until resumed", "Exits the innermost enclosing loop immediately", "Skips to the next iteration of the loop", "Ends the entire program immediately"], answer: 1, variants: [
      { q: "When Python encounters a 'break' statement inside a for loop, what happens next?", options: ["The current iteration's remaining code runs, then the loop ends", "Execution jumps to the code after the loop immediately", "The loop restarts from its first iteration again", "Python raises a BreakError that must be caught"], answer: 1 },
      { q: "If you have a nested loop (loop inside a loop), which loop does 'break' terminate?", options: ["Both the inner and outer loop at the same time", "Only the innermost loop that contains the break", "Only the outermost loop enclosing the statement", "Whichever loop was declared first in the source code"], answer: 1 },
      { q: "How does 'break' differ from simply letting a loop's condition become false?", options: ["break exits the loop immediately mid-iteration without rechecking the condition", "break and a false condition produce identical behavior", "break requires the condition to be True first", "break only works in while loops, not for loops"], answer: 0 },
      { q: "You want a loop to stop as soon as it finds the number 42 in a list. Which keyword achieves this?", options: ["return to send 42 back to the caller", "continue to skip past the 42 value", "break to exit the loop at that point", "pass to ignore the 42 and move on"], answer: 2 },
    ], explanation: "break immediately exits the innermost loop, jumping to the first line of code after the loop. It is useful when you've found what you need and don't want to process remaining elements." },
    { id: "cs50p-w2-q13", q: "What does 'continue' do inside a loop?", options: ["Exits the loop entirely and moves on", "Goes back to the top of the loop, skipping the rest of the current iteration", "Continues execution to the next function call", "Resumes from a previously set breakpoint"], answer: 1, variants: [
      { q: "In a for loop, if a 'continue' statement executes, what happens to the remaining code in that iteration?", options: ["It runs normally after a brief pause", "It is skipped, and the loop advances to the next iteration", "It runs but its output is suppressed", "It triggers an error for unreachable code"], answer: 1 },
      { q: "How does 'continue' differ from 'break' inside a loop?", options: ["continue ends the loop; break skips one iteration", "continue skips to the next iteration; break exits the loop entirely", "They are synonyms that do the same thing", "continue works in for loops; break works in while loops"], answer: 1 },
      { q: "You want to skip processing negative numbers in a list but keep looping through the rest. Which keyword helps?", options: ["break to halt when a negative is found", "pass to mark the negative as handled", "continue to skip the negative and proceed to the next element", "return to exit and come back later for negatives"], answer: 2 },
      { q: "What happens to the loop counter after 'continue' executes inside a for loop?", options: ["It resets to zero for the next iteration", "It advances to the next value as if the iteration finished normally", "It stays at the same value and retries the iteration", "It jumps to the final value in the sequence"], answer: 1 },
    ], explanation: "continue skips the rest of the current iteration's body and jumps back to the top of the loop for the next iteration. Unlike break, the loop itself keeps running." },
    { id: "cs50p-w2-q14", q: "Why is 'while True' used as a pattern for input validation?", options: ["It's the only way to get input from the user", "It deliberately creates a loop that repeats until you explicitly break out when input is valid", "It runs faster than a conditional while loop would", "Python requires it for the input() function to work"], answer: 1, variants: [
      { q: "What makes 'while True' with 'break' a common pattern for repeatedly asking for valid user input?", options: ["It guarantees the input function works correctly", "It keeps prompting until the code decides the input is acceptable and breaks out", "It automatically validates the input type for you", "It limits the user to exactly three input attempts"], answer: 1 },
      { q: "A function uses 'while True' to collect a positive integer from the user. When does the loop end?", options: ["After a fixed number of attempts determined by Python", "When the code reaches a break statement after confirming the input is valid", "When the user presses a special key combination to exit", "When Python's garbage collector reclaims the loop variable"], answer: 1 },
      { q: "Why might a programmer choose 'while True' over 'while not valid' for input validation?", options: ["while True is the only loop syntax Python supports", "It lets you attempt the input first, then decide whether to break or retry", "It prevents exceptions from occurring during input", "It is required when using the int() conversion function"], answer: 1 },
      { q: "In the pattern 'while True: try: ... break / except: ...', what role does 'while True' play?", options: ["It ensures the try block runs at maximum speed", "It creates the repetition that re-prompts after invalid input", "It catches exceptions that the except block misses", "It converts the input to the correct type automatically"], answer: 1 },
    ], explanation: "while True creates an intentional infinite loop that only ends via break. This is ideal for input validation because you keep asking until the user provides valid input, then break out." },
    { id: "cs50p-w2-q15", q: "What is the difference between using 'break' and 'return' inside a loop within a function?", options: ["They're identical in behavior and effect", "break exits the loop but stays in the function; return exits the function entirely and can hand back a value", "return only works inside the main() function", "break also returns a value to the caller"], answer: 1, variants: [
      { q: "Inside a loop within a function, what does 'return value' do that 'break' does not?", options: ["It stops the loop — both do the same thing otherwise", "It exits the entire function and sends a value back to the caller", "It saves the value to a global variable automatically", "It prints the value to the console before stopping"], answer: 1 },
      { q: "After a 'break' statement executes inside a loop in a function, where does execution continue?", options: ["At the first line of the function body again", "At the code immediately following the loop, still inside the function", "Back at the point where the function was called", "At the beginning of the next function definition"], answer: 1 },
      { q: "A function has a while loop. If you replace 'break' with 'return x', what changes about program flow?", options: ["Nothing — they behave identically in all contexts", "The function ends immediately and gives x to the caller, skipping any code after the loop", "The loop restarts from the beginning with value x", "The variable x is deleted from memory after the call"], answer: 1 },
      { q: "Why might you use 'return' instead of 'break' when you've found the answer inside a loop in a function?", options: ["return is faster than break in all situations", "return lets you exit the function and pass back the result in one step", "break is deprecated and should never be used anymore", "return works in for loops while break only works in while loops"], answer: 1 },
    ], explanation: "break only exits the loop, so the function continues running after the loop. return exits the entire function and can pass a value back to the caller, combining two actions in one step." },

    // Lists
    { id: "cs50p-w2-q16", q: "What do square brackets signify in Python?", options: ["A dictionary data structure", "A tuple data structure", "A list", "A function call expression"], answer: 2, variants: [
      { q: "Which Python data structure is created using the syntax ['a', 'b', 'c']?", options: ["A tuple of three strings", "A set of three strings", "A dictionary with three entries", "A list of three strings"], answer: 3 },
      { q: "If you see [1, 2, 3] in Python code, what type of object does this expression create?", options: ["A list containing three integers", "A tuple containing three integers", "A range of three integer values", "A set containing three integers"], answer: 0 },
      { q: "Which bracket style is associated with Python lists?", options: ["Parentheses: (1, 2, 3)", "Curly braces: {1, 2, 3}", "Square brackets: [1, 2, 3]", "Angle brackets: <1, 2, 3>"], answer: 2 },
      { q: "A student wants to store an ordered collection of items. Which syntax creates the correct data structure?", options: ["items = {1, 2, 3} using curly braces", "items = (1, 2, 3) using parentheses", "items = <1, 2, 3> using angle brackets", "items = [1, 2, 3] using square brackets"], answer: 3 },
    ], explanation: "Square brackets [] create a list in Python. Lists are ordered, mutable collections that can hold any type of data. Curly braces {} create dictionaries or sets, and parentheses () create tuples." },
    { id: "cs50p-w2-q17", q: "If students = ['Hermione', 'Harry', 'Ron'], what is students[0]?", options: ["Harry", "Hermione", "Ron", "An error — lists start at 1"], answer: 1, variants: [
      { q: "Given colors = ['red', 'blue', 'green'], what does colors[0] evaluate to?", options: ["'blue', the second element", "'red', the first element", "'green', the last element", "0, the index value itself"], answer: 1 },
      { q: "In a list fruits = ['apple', 'banana', 'cherry'], which index accesses the first element?", options: ["fruits[1] since counting starts at 1", "fruits[0] since Python uses zero-based indexing", "fruits[-1] which always gives the first element", "fruits['first'] using the position name"], answer: 1 },
      { q: "Why does names[0] give the first name in a list, not names[1]?", options: ["It's a Python bug that was never fixed", "Python uses zero-based indexing where the first element is at position 0", "names[1] actually does give the first name", "The 0 index is reserved for the list's length value"], answer: 1 },
      { q: "Given pets = ['cat', 'dog', 'fish'], what do pets[0] and pets[2] return?", options: ["'dog' and 'fish' respectively", "'cat' and 'fish' respectively", "'cat' and an IndexError respectively", "'dog' and 'cat' respectively"], answer: 1 },
    ], explanation: "Python lists use zero-based indexing, so the first element is at index 0, the second at index 1, and so on. This means the last element of a 3-item list is at index 2, not 3." },
    { id: "cs50p-w2-q18", q: "What does 'for student in students' do when students is a list?", options: ["Sets student to the index of each element in order", "Sets student to each element in the list sequentially", "Creates a copy of the list in a new variable", "Sorts the list alphabetically then iterates over it"], answer: 1, variants: [
      { q: "When you write 'for color in colors' where colors is ['red', 'blue', 'green'], what values does color take?", options: ["0, 1, 2 as numeric indices of the elements", "'red', 'blue', 'green' — the actual elements one by one", "The entire list ['red', 'blue', 'green'] on each pass", "Only the first element 'red' on every iteration"], answer: 1 },
      { q: "How does Python's for loop handle iteration over a list without using range()?", options: ["It assigns each element of the list to the variable in turn", "It assigns the index of each element to the variable", "It converts the list to a range object first, then iterates", "It randomly selects elements from the list each pass"], answer: 0 },
      { q: "What is the advantage of 'for item in my_list' over manually indexing with my_list[0], my_list[1], etc.?", options: ["It processes elements in reverse order automatically", "It handles any list length without hard-coding index values", "It runs the loop body twice for each element", "It only works for lists shorter than 10 elements"], answer: 1 },
      { q: "In 'for fruit in basket', if basket = ['apple', 'pear'], how many times does the loop body run?", options: ["Once, with fruit set to the entire list", "Twice, with fruit set to 'apple' then 'pear'", "Zero times, since basket has fewer than 3 items", "Three times, including one for the list itself"], answer: 1 },
    ], explanation: "A for loop iterates once per element in the sequence. Each iteration assigns the next element (not the whole list) to the loop variable. The loop body runs len(list) times total." },
    { id: "cs50p-w2-q19", q: "What does len() return when given a list?", options: ["The memory size of the list in bytes", "The number of elements in the list", "The index of the last element in the list", "The largest value stored in the list"], answer: 1, variants: [
      { q: "If scores = [90, 85, 78, 92], what does len(scores) return?", options: ["92, the highest value in the list", "3, the index of the last element", "345, the sum of all values together", "4, the count of elements in the list"], answer: 3 },
      { q: "What built-in function tells you how many items a list contains?", options: ["size() returns the item count", "count() returns the item count", "len() returns the item count", "max() returns the item count"], answer: 2 },
      { q: "For a list with 5 elements, how does len() relate to the last valid index?", options: ["len() returns 5, but the last index is 4", "len() returns 4, matching the last index exactly", "len() returns 6, one more than the element count", "len() returns 5, which is also the last valid index"], answer: 0 },
      { q: "What would len([]) return for an empty list?", options: ["None, since the list has no contents", "An error, since you can't measure an empty list", "0, since there are no elements in the list", "-1, to indicate the list is empty"], answer: 2 },
    ], explanation: "len() returns the number of items in a collection. It works on lists, strings, dictionaries, and more. For a list with 5 elements, len() returns 5, and the last valid index is 4." },
    { id: "cs50p-w2-q20", q: "If students has 3 elements, what does range(len(students)) produce?", options: ["0, 1, 2, 3", "1, 2, 3", "0, 1, 2", "The students themselves"], answer: 2, variants: [
      { q: "Given a list of 5 items, what sequence does range(len(my_list)) generate?", options: ["1, 2, 3, 4, 5 starting from one", "0, 1, 2, 3, 4 starting from zero", "0, 1, 2, 3, 4, 5 including five", "The five items from the list itself"], answer: 1 },
      { q: "What does combining range() with len() accomplish when applied to a list?", options: ["It sorts the list by element length", "It produces index values from 0 up to one less than the list's length", "It doubles the length of the list contents", "It returns the list elements in reverse order"], answer: 1 },
      { q: "For colors = ['red', 'blue'], what does range(len(colors)) produce?", options: ["'red', 'blue' — the elements themselves", "0, 1 — valid indices for the list", "1, 2 — counting from one to the length", "0, 1, 2 — zero through the length value"], answer: 1 },
      { q: "Why does range(len(items)) give the correct set of index positions for a list?", options: ["range starts at 0 and len gives the count, so it covers indices 0 through count-1", "range starts at 1 and len subtracts 1 to compensate", "len returns the last index, and range includes that value", "range and len both add 1 to account for zero indexing"], answer: 0 },
    ], explanation: "range(len(items)) generates indices 0 through len-1, which exactly matches the valid indices of a list. This is because both range() and list indexing are zero-based." },
    { id: "cs50p-w2-q21", q: "Why would you use 'for i in range(len(students))' instead of 'for student in students'?", options: ["It's always considered better practice overall", "When you need access to the numeric index (e.g., for numbering or positional logic)", "It runs faster due to compiler optimizations", "The simpler form doesn't work with string elements"], answer: 1, variants: [
      { q: "When is iterating by index with range(len(...)) more useful than iterating directly over elements?", options: ["It's always more useful regardless of the situation", "When you need the position number, such as for labeling items 1, 2, 3", "When the list contains strings instead of integers", "When the list has more than 100 elements in it"], answer: 1 },
      { q: "A program needs to print each student with a number: '1: Alice', '2: Bob'. Why does iterating by index help here?", options: ["Direct iteration hides the position, but index-based iteration provides it", "Direct iteration can't access string elements in a list", "Index-based iteration is always the preferred approach in Python", "Direct iteration prints elements in random order each time"], answer: 0 },
      { q: "What does the index variable i give you in 'for i in range(len(items))' that 'for item in items' does not?", options: ["Access to the element's actual value from the list", "The numerical position of the current element", "A faster iteration speed through the list", "Protection against IndexError exceptions in the body"], answer: 1 },
      { q: "If you don't need to know an element's position in the list, which loop style is preferred in Python?", options: ["for i in range(len(items)) with explicit indexing", "while i < len(items) with manual incrementing", "for item in items — iterating directly over elements", "for i, item in items using automatic unpacking"], answer: 2 },
    ], explanation: "When you need the index (for numbering or positional logic), use range(len(list)). When you only need the values themselves, iterating directly with 'for item in list' is simpler and more Pythonic." },

    // Dictionaries
    { id: "cs50p-w2-q22", q: "What data structure associates keys with values in Python?", options: ["A list", "A tuple", "A dictionary", "A set"], answer: 2, variants: [
      { q: "If you need to map each student's name to their grade, which Python data structure is most appropriate?", options: ["A list of grade values", "A set of student names", "A tuple of name-grade pairs", "A dictionary mapping names to grades"], answer: 3 },
      { q: "Which data structure lets you look up a value by providing a unique key?", options: ["A list, using positional lookup", "A dictionary, using key-based lookup", "A tuple, using named element access", "A set, using membership-based lookup"], answer: 1 },
      { q: "What Python structure stores data as key-value pairs?", options: ["A set stores key-value pairs", "A list stores key-value pairs", "A dictionary stores key-value pairs", "A range stores key-value pairs"], answer: 2 },
      { q: "You want to associate 'alice' with 95 and 'bob' with 87. Which structure directly supports this?", options: ["A list like [95, 87] with implied names", "A tuple like ('alice', 95, 'bob', 87)", "A set like {'alice', 95, 'bob', 87}", "A dict like {'alice': 95, 'bob': 87}"], answer: 3 },
    ], explanation: "A dictionary stores key-value pairs, making the association between data explicit. You look up a value by providing its key, which is more meaningful and reliable than matching indices across separate lists." },
    { id: "cs50p-w2-q23", q: "What do curly braces signify when defining a data structure in Python (not an f-string)?", options: ["A list of elements", "A dictionary (or set)", "A tuple of values", "A function body block"], answer: 1, variants: [
      { q: "Which bracket type is used to define a dictionary literal in Python?", options: ["Square brackets like [key: value]", "Parentheses like (key: value)", "Curly braces like {key: value}", "Angle brackets like <key: value>"], answer: 2 },
      { q: "If you see {'a': 1, 'b': 2} in Python code, what type of object is being created?", options: ["A list with labeled elements", "A dictionary with two key-value pairs", "A tuple with two named entries", "A function with two default parameters"], answer: 1 },
      { q: "What distinguishes {} with key:value pairs from {} with plain values in Python?", options: ["Key:value pairs create a dict; plain values create a set", "Both create the same type of data structure", "Key:value pairs create a set; plain values create a dict", "Neither is valid — curly braces only create empty dicts"], answer: 0 },
      { q: "A student writes data = {'name': 'Alice', 'age': 20}. What type is data?", options: ["A list with two string elements in it", "A tuple with two labeled fields in it", "A set with two members stored in it", "A dictionary with two key-value entries"], answer: 3 },
    ], explanation: "Curly braces {} with key:value pairs create a dictionary. Curly braces with plain values (no colons) create a set. The colon syntax is what distinguishes a dict literal from a set literal." },
    { id: "cs50p-w2-q24", q: "Given students = {'Hermione': 'Gryffindor', 'Draco': 'Slytherin'}, what does students['Hermione'] return?", options: ["'Hermione'", "'Gryffindor'", "0", "True"], answer: 1, variants: [
      { q: "Given prices = {'apple': 1.50, 'banana': 0.75}, what does prices['banana'] evaluate to?", options: ["'banana'", "1.50", "0.75", "1"], answer: 2 },
      { q: "When you index a dictionary with a key like my_dict['x'], what do you get back?", options: ["The key 'x' is returned unchanged", "The value associated with the key 'x'", "The index position of 'x' in the dictionary", "A boolean indicating if 'x' exists in the dict"], answer: 1 },
      { q: "Given capitals = {'France': 'Paris', 'Japan': 'Tokyo'}, what is capitals['France']?", options: ["'France', the key you looked up", "'Tokyo', the most recent value added", "'Paris', the value mapped to that key", "None, since France is not a valid key"], answer: 2 },
      { q: "How do you retrieve the value for a specific key in a Python dictionary?", options: ["Call dict.get_value() with the key as a string argument", "Use dot notation like dict.key to access the mapped value", "Use square bracket notation like dict[key] with the key inside", "Use parentheses like dict(key) to call the key as a function"], answer: 2 },
    ], explanation: "You access dictionary values using bracket notation with the key: my_dict['key']. The key acts like a label that maps directly to its associated value, similar to how a word maps to its definition." },
    { id: "cs50p-w2-q25", q: "When you iterate over a dictionary with 'for student in students', what does the variable receive?", options: ["The values associated with each key", "The keys", "Tuples of (key, value) pairs each iteration", "The indices 0, 1, 2... for each entry"], answer: 1, variants: [
      { q: "If you write 'for k in my_dict' where my_dict = {'a': 1, 'b': 2}, what values does k take on?", options: ["1 then 2 — the dictionary's values", "'a' then 'b' — the dictionary's keys", "('a', 1) then ('b', 2) — key-value tuples", "0 then 1 — numeric index positions"], answer: 1 },
      { q: "By default, what does a for loop iterate over when given a dictionary?", options: ["The values stored in the dictionary", "The keys of the dictionary", "Both keys and values as paired tuples", "The number of entries as a count"], answer: 1 },
      { q: "To access both the key and value while looping over a dictionary, what must you do differently from 'for x in dict'?", options: ["Nothing — the default loop gives both already", "Use dict.items() to get key-value pairs, since the default gives only keys", "Use dict.values() to include the keys alongside values", "Use dict.keys() which returns both keys and values together"], answer: 1 },
      { q: "A student expects 'for x in scores' to give values like 95, 88, but instead gets 'Alice', 'Bob'. Why?", options: ["There is a bug in the Python dictionary implementation", "Iterating over a dict yields its keys by default, not its values", "The dictionary was defined with the keys and values swapped", "for loops always return data in alphabetical sorted order"], answer: 1 },
    ], explanation: "When you iterate over a dictionary with a for loop, the variable receives the keys, not the values. To get values, use dict.values(). To get both, use dict.items() which yields (key, value) tuples." },
    { id: "cs50p-w2-q26", q: "Why is a dictionary preferable to two parallel lists for associating students with houses?", options: ["Dictionaries use significantly less memory than two lists", "The association is explicit in one structure instead of relying on matching indices across separate lists", "Dictionaries are faster to iterate through than lists are", "You can't store strings in lists so dicts are required"], answer: 1, variants: [
      { q: "What problem arises from using two separate lists like names = [...] and grades = [...] to track student grades?", options: ["Python doesn't allow two lists to exist at the same time", "The connection between a name and its grade depends on fragile index matching", "Two lists always consume more memory than a single list", "It's actually the recommended approach in all situations"], answer: 1 },
      { q: "How does a dictionary improve upon using parallel arrays to connect related data?", options: ["It stores data in sorted order automatically", "It bundles each key with its value directly, making the relationship explicit", "It prevents duplicate entries from being added", "It limits storage to only string-type values"], answer: 1 },
      { q: "If you accidentally insert an item into one parallel list but forget the other, what goes wrong?", options: ["Python automatically pads the shorter list with None values", "The indices fall out of sync, pairing the wrong elements together", "Both lists are automatically truncated to the shorter length", "Nothing — parallel lists handle mismatches gracefully"], answer: 1 },
      { q: "A student stores cities in one list and populations in another at matching indices. What is the main risk?", options: ["Lists can't hold both strings and integers at once", "Any change to one list's order can silently break the pairing", "Python raises an error if two lists have the same length", "The data is impossible to access without a dictionary"], answer: 1 },
    ], explanation: "A dictionary bundles each key with its value directly, making the relationship explicit and robust. Parallel lists rely on fragile index matching that can silently break if one list is modified without the other." },
    { id: "cs50p-w2-q27", q: "What does the Python keyword 'None' represent?", options: ["The numeric value zero", "An empty string with no characters", "The boolean value False", "The deliberate absence of a value"], answer: 3, variants: [
      { q: "If a dictionary value is set to None, what does that communicate?", options: ["The key has an empty string as its value", "The key maps to zero as a numeric placeholder", "The key exists but intentionally has no meaningful value", "The key has been deleted from the dictionary"], answer: 2 },
      { q: "How is None different from 0, '', and False in Python?", options: ["They are all equivalent and interchangeable in practice", "None specifically means no value at all, while the others are actual values of their respective types", "None is just another way to write False in boolean context", "None can only be used inside dictionary data structures"], answer: 1 },
      { q: "What type does None have in Python?", options: ["int — it's treated as the number zero", "str — it's treated as an empty string value", "bool — it's treated as a false boolean", "NoneType — it's its own distinct type entirely"], answer: 3 },
      { q: "A function doesn't include a return statement. What value does it implicitly return?", options: ["Zero, as the default numeric return value", "An empty string as the default text value", "False, as the default boolean return value", "None, indicating no value was returned"], answer: 3 },
    ], explanation: "None is Python's way of representing the intentional absence of a value. It is its own type (NoneType) and is distinct from 0, empty string, or False, each of which are actual values of their respective types." },

    // Lists of dictionaries
    { id: "cs50p-w2-q28", q: "In a list of dictionaries like [{'name': 'Harry', 'house': 'Gryffindor'}, ...], how do you access Harry's house while iterating?", options: ["student.house", "student['house']", "students['house']", "student[1]"], answer: 1, variants: [
      { q: "Given records = [{'city': 'Boston', 'state': 'MA'}, ...], how do you access the city while iterating with 'for r in records'?", options: ["r.city using dot notation on the dictionary", "records['city'] using the list name directly", "r['city'] using bracket notation on the current dict", "r[0] using numeric index on the dictionary"], answer: 2 },
      { q: "When looping over a list of dictionaries, each iteration variable is a dictionary. How do you get a specific field from it?", options: ["Use dot notation like item.field on the variable", "Use bracket notation like item['field'] on the variable", "Use the list name like items['field'] with the key", "Use numeric indexing like item[0] for the first field"], answer: 1 },
      { q: "Why does student[1] NOT work to access the 'house' field when student is a dictionary?", options: ["Dictionaries are indexed by keys, not by numeric positions", "The number 1 is reserved for the first key only in dicts", "student[1] returns the second key, not the second value", "It does work — numeric indexing is valid for dictionaries"], answer: 0 },
      { q: "In 'for person in people' where people is a list of dicts with 'name' and 'age' keys, how do you print the age?", options: ["print(people['age']) using the list variable", "print(person.age) using attribute access syntax", "print(person[1]) using the age's position number", "print(person['age']) using the key on the current dict"], answer: 3 },
    ], explanation: "When iterating over a list of dictionaries, each loop variable is a dictionary. You access its fields using bracket notation with the key name (e.g., person['age']), not numeric indices or dot notation." },
    { id: "cs50p-w2-q29", q: "Why would you use a list of dictionaries instead of a single dictionary mapping names to houses?", options: ["It's always the better approach regardless of context", "When each item has multiple attributes (name, house, patronus) rather than just one value per key", "Lists are faster than dictionaries for lookups and iteration", "Single dictionaries can't store string values as entries"], answer: 1, variants: [
      { q: "When does a list of dictionaries become more useful than a flat dictionary mapping keys to single values?", options: ["When you only need to store one piece of data per entry", "When each entry needs to hold several related fields together", "When the list has fewer than 10 items total in it", "When you want faster lookup speed for single values"], answer: 1 },
      { q: "A student stores {'Harry': 'Gryffindor'} but now needs to add a patronus for each student too. What structure works better?", options: ["Add a second dictionary mapping names to patronuses separately", "Use a list of dicts where each dict has name, house, and patronus keys", "Store the patronus in the same key as the house with a comma separator", "Use a set to hold all three pieces of information together"], answer: 1 },
      { q: "What limitation of {'Alice': 90} makes a list of dicts better when you also need to store each student's grade letter?", options: ["A simple dict maps each key to only one value, not multiple attributes", "Simple dictionaries cannot use strings as key values at all", "A simple dict cannot have more than 26 entries in total", "There is no limitation — simple dicts handle this well already"], answer: 0 },
      { q: "Why might a database-like structure use [{'id': 1, 'name': 'Jo', 'score': 95}, ...] instead of {1: 'Jo', ...}?", options: ["Lists of dicts are always smaller in memory usage", "Each dict can hold a full record with multiple fields per entry", "This format is required by Python's database libraries", "Flat dictionaries can't use integers as key values"], answer: 1 },
    ], explanation: "A list of dictionaries lets each entry have multiple named fields (like a database row). A flat dict only maps one key to one value, which is limiting when each item has several attributes." },

    // Nested loops & 2D output
    { id: "cs50p-w2-q30", q: "When printing a 3×3 grid, why do you need a loop inside a loop?", options: ["Python requires nested loops for all print() calls", "One loop handles rows (vertical), the inner loop handles columns (horizontal) within each row", "Nested loops are faster than a single loop overall", "You don't — one loop is always sufficient for grids"], answer: 1, variants: [
      { q: "What is the role of each loop in a nested loop that prints a rectangular grid of characters?", options: ["Both loops handle columns working together in parallel", "The outer loop advances through rows; the inner loop prints each column within a row", "The outer loop prints characters; the inner loop moves to the next line", "Both loops handle rows, each one printing alternate lines"], answer: 1 },
      { q: "To produce a 4-row, 5-column block of '#' symbols, how many loops do you need and why?", options: ["One loop that prints 20 characters total at once", "Two loops: one for the 4 rows and one for the 5 columns in each row", "Three loops: one for rows, columns, and line breaks separately", "No loops — string multiplication handles everything on its own"], answer: 1 },
      { q: "Why can't a single loop produce a proper 2D grid without additional logic?", options: ["A single loop iterates in one dimension — you need a second loop for the other dimension", "Single loops can only print numbers, not characters or symbols", "Python limits single loops to 10 iterations maximum", "A single loop actually can produce any grid without nested loops"], answer: 0 },
      { q: "In a grid-printing program, what happens during each iteration of the outer loop?", options: ["A single character is printed to the current position", "An entire row is constructed by the inner loop running to completion", "The column counter is incremented by one position", "The grid dimensions are recalculated for the next pass"], answer: 1 },
    ], explanation: "Nested loops map naturally to 2D structures. The outer loop handles one dimension (rows) and the inner loop handles the other (columns within each row). Each complete inner loop produces one row." },
    { id: "cs50p-w2-q31", q: "Inside a nested loop printing a grid, why pass end='' to print() for each brick?", options: ["To prevent errors from occurring during output", "To keep the cursor on the same line so bricks appear side by side in a row", "To add extra spacing between each character printed", "It's required syntax whenever you use nested loops"], answer: 1, variants: [
      { q: "What would happen if you used print('#') without end='' inside the inner loop of a grid printer?", options: ["Each '#' would appear on its own line instead of side by side", "The '#' characters would print with extra spaces between them", "Nothing different — print() keeps output on one line by default", "The inner loop would raise an error and stop execution"], answer: 0 },
      { q: "What does end='' do when passed to print() in the context of building a row character by character?", options: ["It terminates the string at that exact position in output", "It suppresses the default newline so the next print continues on the same line", "It adds an empty line after the printed character", "It signals that this is the last character in the row"], answer: 1 },
      { q: "By default, print() adds what character after its output, and how does end='' change that?", options: ["It adds a space by default; end='' removes the space", "It adds a newline by default; end='' suppresses the newline", "It adds a tab by default; end='' replaces the tab", "It adds nothing by default; end='' is therefore redundant"], answer: 1 },
      { q: "A student's grid prints each '#' on a separate line instead of forming rows. Which fix solves this?", options: ["Use sep='' between print arguments instead", "Add end='' to the print call inside the inner loop", "Remove the inner loop and use only the outer loop", "Change the print call to use sys.stdout.write() instead"], answer: 1 },
    ], explanation: "print() adds a newline after each call by default. Using end='' suppresses that newline so successive prints appear on the same line, allowing you to build a row character by character." },
    { id: "cs50p-w2-q32", q: "After the inner loop finishes one row of bricks, why call print() with no arguments?", options: ["To flush the output buffer to the terminal", "To move the cursor to the next line before starting the next row", "To reset the loop counter back to zero for reuse", "It's optional — just a style choice with no real effect"], answer: 1, variants: [
      { q: "What does a bare print() call (with no arguments) output?", options: ["An empty space character on the current line", "Nothing at all — it's a no-op that does nothing", "A newline character, moving the cursor down one line", "An error because print requires at least one argument"], answer: 2 },
      { q: "In a grid-printing program, what would happen if you removed the print() call between rows?", options: ["The rows would appear spaced further apart than normal", "All bricks would print on a single continuous line with no row separation", "The program would crash with a formatting error", "Each row would print twice instead of once per iteration"], answer: 1 },
      { q: "After using end='' in the inner loop, how do you signal that a row is complete and the next should start below?", options: ["Add a '\\n' to the last character printed in the row", "Call print() with no arguments to output just a newline", "Set end='\\n' on only the last print call of the row", "The outer loop handles line breaks automatically for you"], answer: 1 },
      { q: "Why is print() placed inside the outer loop but outside the inner loop in a grid printer?", options: ["It must run once per row to insert a line break after each row finishes", "It must run once per character to separate columns properly", "It resets the inner loop variable to zero for the next row", "Its placement doesn't matter — it works the same anywhere"], answer: 0 },
    ], explanation: "A bare print() outputs just a newline character. Placed after the inner loop (but inside the outer loop), it moves the cursor to the next line after each row is complete, separating rows visually." },
    { id: "cs50p-w2-q33", q: "How can 'print(\"#\" * size)' replace an inner loop when printing a row of bricks?", options: ["It can't — loops are always needed for printing rows", "String multiplication repeats the character, producing the same output as looping and printing one at a time", "It only works for single characters, not longer strings", "It's faster but produces slightly different formatted output"], answer: 1, variants: [
      { q: "Instead of using an inner loop to print 5 '#' characters, what single expression produces the same row?", options: ["print('#', 5) passing the count as a second argument", "print('#' + 5) adding the count to the character", "print('#' * 5) using string repetition to build the row", "print(5, '#') reversing the order of arguments"], answer: 2 },
      { q: "What advantage does string multiplication have over an inner loop for printing a row of identical characters?", options: ["It runs on a different CPU core for better performance", "It achieves the same result in a single concise expression", "It allows mixing different characters in the same row", "It automatically adds spaces between each character printed"], answer: 1 },
      { q: "A student replaces a 3-line inner loop with 'print('#' * width)'. Why does this produce identical output?", options: ["The inner loop was using string multiplication internally anyway", "Both approaches produce a string of '#' repeated width times then print it", "It only looks identical but the internal representation differs", "Python optimizes the inner loop into multiplication automatically"], answer: 1 },
      { q: "When is string multiplication NOT a suitable replacement for an inner loop?", options: ["When each position in the row needs a different character or logic", "When the row has more than 10 characters total to print", "When the outer loop runs more than 5 iterations overall", "String multiplication can always replace inner loops for rows"], answer: 0 },
    ], explanation: "String multiplication like '#' * 5 produces a repeated string in one expression, replacing a loop that prints one character at a time. It is more concise and achieves the same output for uniform rows." },

    // Abstraction & function design
    { id: "cs50p-w2-q34", q: "What is the main benefit of defining a print_row() function even if it's only one line?", options: ["It runs faster than inline code would", "It creates a named abstraction — the caller understands *what* happens without knowing *how*", "Python requires functions for all printing operations", "It saves memory by reducing variable allocations"], answer: 1, variants: [
      { q: "Why would a programmer wrap a single line of code in a function with a descriptive name?", options: ["To make the code execute significantly faster overall", "To give the operation a meaningful name that clarifies intent", "To prevent other parts of the code from modifying it", "Single-line functions are required by Python's style guide"], answer: 1 },
      { q: "How does creating a print_row(width) function improve the code that calls it?", options: ["It makes the program use less total memory at runtime", "The caller sees a clear action name rather than implementation details", "It restricts who can call the print statement directly", "It ensures print is only called from within functions"], answer: 1 },
      { q: "What principle of software design does wrapping logic in a well-named function demonstrate?", options: ["Optimization — reducing the number of operations performed", "Abstraction — hiding how something works behind a clear interface", "Inheritance — letting functions share code with each other", "Compilation — converting the code to run faster in machine code"], answer: 1 },
      { q: "A student argues that a one-line function is pointless. What is the counterargument?", options: ["One-line functions compile to faster machine code", "The function name documents the purpose, making the calling code self-explanatory", "Python gives one-line functions special performance treatment", "One-line functions use less memory than inline code does"], answer: 1 },
    ], explanation: "Abstraction is about giving operations meaningful names, not reducing line count. A well-named function makes the calling code read like a description of what it does, hiding implementation details behind a clear interface." },
    { id: "cs50p-w2-q35", q: "If you change print_column's implementation from a loop to string multiplication, what must stay the same for callers?", options: ["The internal variable names used in the function", "The function name, parameters, and behavior — the interface", "The number of lines of code inside the function body", "Nothing — callers always need updating after changes"], answer: 1, variants: [
      { q: "What does it mean for a function to maintain a stable 'interface' even when its internals change?", options: ["The function's source code must never be modified at all", "The function name, parameters, and observable behavior stay the same for callers", "The function must always use the same algorithm internally", "The function must be defined in the same file as its callers"], answer: 1 },
      { q: "You refactor a function's body from 10 lines to 2 lines. What must remain unchanged so callers aren't affected?", options: ["The exact number of lines in the function body", "The variable names used internally within the function", "The function's name, what it accepts, and what it produces", "The comments explaining how the function works inside"], answer: 2 },
      { q: "Why can a function's implementation be swapped without breaking the rest of the program?", options: ["Python automatically updates all callers when a function changes", "Callers depend on the function's interface, not its internal workings", "Changing a function's body is not actually possible in Python", "All functions are compiled once and cached permanently at startup"], answer: 1 },
      { q: "A teammate rewrites your function using a different algorithm but keeps the same name and parameters. What happens to code that calls it?", options: ["It keeps working as before since the interface is unchanged", "It breaks because the internal logic is now different", "It needs to be recompiled before it can use the new version", "It runs but produces different results every time now"], answer: 0 },
    ], explanation: "Callers depend on a function's interface (name, parameters, behavior), not its internal implementation. You can refactor the body freely as long as the function still accepts the same inputs and produces the same outputs." },
  ],
},

// ─── LECTURE 3 ───────────────────────────────────────────────────────
{
  course: "CS50P",
  title: "CS50P Week 3: Exceptions",
  questions: [
    // Syntax errors vs runtime errors
    { id: "cs50p-w3-q1", q: "What is the key difference between a syntax error and a runtime error?", options: ["Syntax errors are generally harder to fix than runtime errors", "Syntax errors prevent the program from running at all; runtime errors occur while the program is executing", "Runtime errors are caused by the compiler during code analysis", "There is no meaningful difference between the two error types"], answer: 1, explanation: "Syntax errors mean the code violates Python's grammar rules and cannot be parsed. Runtime errors happen during execution when an operation fails, like dividing by zero or converting invalid input." },
    { id: "cs50p-w3-q2", q: "Can you use try/except to catch a syntax error?", options: ["Yes, just like any other type of error", "Only if you use a special interpreter flag", "No — syntax errors must be fixed in the code before the program can run", "Yes, but only in Python version 3.10 and above"], answer: 2, explanation: "Syntax errors are caught before the program starts running, during the parsing stage. Since try/except only works at runtime, it cannot catch syntax errors. You must fix the code itself." },

    // Value errors & the int() function
    { id: "cs50p-w3-q3", q: "What happens when you pass the string 'cat' to Python's int() function?", options: ["It returns 0 as a default fallback value", "It returns None to indicate a failed conversion", "It raises a ValueError", "It silently ignores the input and continues"], answer: 2, explanation: "int() raises a ValueError when given a string that doesn't represent a valid integer. This is a runtime error that will crash the program unless caught with try/except." },
    { id: "cs50p-w3-q4", q: "In 'x = int(input(...))', if int() raises a ValueError, does x get assigned a value?", options: ["Yes, x becomes None as a default placeholder", "Yes, x keeps its previous value from before the line", "No — the error interrupts the assignment before the left side executes", "No — x is set to 0 as a safe default by Python"], answer: 2, explanation: "Python evaluates the right side of = before assigning. If int() raises an error, the assignment never completes, so x is never defined (or retains no new value). This can lead to a NameError if x is used later." },
    { id: "cs50p-w3-q5", q: "Why is adding a print statement like 'please type a number' an insufficient solution to bad user input?", options: ["It causes a syntax error when the input is wrong", "Users might not read or follow instructions, so the program can still crash", "Print statements slow down the program noticeably", "It actually is the best solution for handling input"], answer: 1, explanation: "Users are unpredictable and may still enter invalid input despite instructions. Robust code must handle bad input programmatically with try/except rather than relying on users to follow directions." },

    // try / except basics
    { id: "cs50p-w3-q6", q: "What does the 'try' keyword tell Python to do?", options: ["Run the indented code and, if an exception occurs, jump to the except block instead of crashing", "Repeat the code until it works without errors", "Test the code without actually executing any of it", "Compile the code before running it for safety"], answer: 0, explanation: "try/except is Python's error-handling mechanism. Code in the try block runs normally, but if an exception occurs, Python jumps to the except block instead of crashing, letting you handle the error gracefully." },
    { id: "cs50p-w3-q7", q: "Why is it bad practice to write a bare 'except' that catches all exceptions without specifying a type?", options: ["It's not valid Python syntax and will cause errors", "It can hide other bugs because you don't know what went wrong", "It makes the program significantly slower at runtime", "It only catches ValueErrors anyway despite the broad syntax"], answer: 1, explanation: "A bare except catches every exception type, including unexpected ones like KeyboardInterrupt or NameError. This can mask real bugs by silently swallowing errors you didn't anticipate." },
    { id: "cs50p-w3-q8", q: "Why should you minimize the number of lines inside a try block?", options: ["Python limits try blocks to 3 lines of code maximum", "You should only try the lines that might actually raise the exception you're catching", "More lines make the program run slower during execution", "It's purely a style preference with no practical impact at all"], answer: 1, explanation: "Extra lines in a try block might raise unexpected exceptions of the same type, which your except would catch and mishandle. Keep try blocks focused on the specific operation that might fail." },

    // The else clause in try/except
    { id: "cs50p-w3-q9", q: "When does the else block execute in a try/except/else structure?", options: ["Always, after both try and except run", "Only when an exception is raised during try", "Only when the try block succeeds without raising an exception", "Only on the second attempt through the block"], answer: 2, explanation: "The else clause runs only when the try block completes without raising any exception. This lets you separate the risky operation (in try) from the code that should only run on success (in else)." },
    { id: "cs50p-w3-q10", q: "You move 'print(x)' outside and below a try/except block (unindented). The user types 'cat'. What happens?", options: ["It prints 'cat' as a string value", "It prints nothing and moves to the next line", "A NameError occurs because x was never successfully assigned", "A ValueError occurs again at the print statement"], answer: 2, explanation: "When int('cat') raises a ValueError, the assignment x = int(...) never completes, so x is never defined. Code after the try/except then fails with a NameError when trying to use x." },
    { id: "cs50p-w3-q11", q: "Why does the NameError happen in that scenario?", options: ["Python deletes variables after a try block ends", "The ValueError interrupted the assignment, so x was never defined", "print() doesn't work outside of try blocks in Python", "x went out of scope when the try block finished"], answer: 1, explanation: "In x = int(input(...)), the right side is evaluated first. If it raises an exception, execution jumps to except before the assignment completes, so x never gets a value." },

    // Loops with try/except
    { id: "cs50p-w3-q12", q: "What is the purpose of wrapping try/except inside a 'while True' loop?", options: ["It makes the code run faster during execution", "It re-prompts the user until they provide valid input instead of quitting on the first error", "It's required syntax for try/except to work correctly", "It catches more types of errors than try alone"], answer: 1, explanation: "while True creates a loop that keeps asking for input. If the input is invalid, except handles the error and the loop repeats. When input is valid, break exits the loop. This ensures the program gets good data." },
    { id: "cs50p-w3-q13", q: "In a while True loop with try/except, what does 'break' do when placed after a successful conversion?", options: ["Exits the entire program and returns to terminal", "Exits the loop so the program can continue", "Restarts the loop from the beginning of the block", "Raises an exception to signal completion of work"], answer: 1, explanation: "Once the try block succeeds (no exception), break exits the while True loop, allowing the program to continue with the valid input. Without break, the loop would keep prompting forever." },
    { id: "cs50p-w3-q14", q: "If you put the print statement inside the loop but never use break, what happens?", options: ["The program crashes with an unhandled error", "It prints the result once and then exits cleanly", "It loops forever, printing and re-prompting on every iteration", "It prints nothing and waits for more user input"], answer: 2, explanation: "Without break, while True never stops. The program will print the result, then loop back and ask for input again, printing and re-prompting indefinitely." },

    // return vs break
    { id: "cs50p-w3-q15", q: "If you're inside a loop within a function, what does 'return x' do compared to 'break'?", options: ["They're identical in behavior and outcome", "return exits the function and gives back a value; break only exits the loop", "break exits the function entirely; return only exits the loop", "return is only valid inside the main() function"], answer: 1, explanation: "return exits the entire function immediately, returning a value to the caller. break only exits the loop, and the function continues with whatever code comes after the loop." },
    { id: "cs50p-w3-q16", q: "Why can 'return' replace 'break' followed by 'return' when they're adjacent?", options: ["It's a Python compiler optimization that merges them", "return already breaks out of the loop AND returns the value — doing both at once", "They actually can't be combined into a single statement", "break is deprecated in modern versions of Python"], answer: 1, explanation: "When return is executed inside a loop, it exits both the loop and the function simultaneously. Writing break followed by return outside the loop accomplishes the same thing but with an extra step." },

    // pass keyword
    { id: "cs50p-w3-q17", q: "What does the 'pass' keyword do inside an except block?", options: ["It re-raises the exception to the calling code", "It does nothing — the exception is caught but silently ignored", "It prints a default error message to the console", "It skips the current iteration of the enclosing loop"], answer: 1, explanation: "pass is a no-op placeholder. In an except block, it catches the exception and does nothing with it, effectively silencing the error. The program continues as if nothing happened." },
    { id: "cs50p-w3-q18", q: "When might you use 'pass' in an except block instead of printing an error message?", options: ["When you want the program to crash gracefully with a traceback", "When you want to silently retry by staying in the loop without telling the user what went wrong", "When the error is a syntax error that can be recovered from", "You should never use pass because it hides all exceptions"], answer: 1, explanation: "In a while True input validation loop, pass in the except block silently ignores the error and lets the loop repeat, re-prompting the user without displaying an error message." },

    // Function design & abstraction
    { id: "cs50p-w3-q19", q: "Why is it good practice to extract the input-validation loop into a separate function like get_int()?", options: ["It makes the code run faster overall in execution", "It abstracts away the retry logic so main() stays clean and the function is reusable", "Python requires all input calls to be inside a function", "It prevents all possible errors from ever occurring"], answer: 1, explanation: "Extracting get_int() keeps the messy loop/try/except logic in one place while main() reads cleanly. The function can then be reused anywhere you need a validated integer from the user." },
    { id: "cs50p-w3-q20", q: "Why should get_int() accept a 'prompt' parameter instead of hard-coding 'What's x?'?", options: ["Hard-coded strings cause syntax errors in Python", "It makes the function reusable — the caller decides what message to show", "Parameters make functions run faster at execution time", "It's required by Python's official PEP 8 style guide"], answer: 1, explanation: "Hard-coding the prompt makes the function only useful for one specific question. Accepting a prompt parameter lets callers pass any message, making get_int() a general-purpose utility." },
    { id: "cs50p-w3-q21", q: "In get_int(), why is it unnecessary to define a variable x if you immediately return the converted value?", options: ["Variables aren't allowed inside helper functions", "You can return int(input(...)) directly — the intermediate variable adds a line without adding clarity", "It would cause a NameError when accessed later", "Python automatically optimizes the variable away at runtime"], answer: 1, explanation: "If the only use of a variable is to immediately return it, you can skip the variable and return the expression directly. return int(input(prompt)) is cleaner than x = int(input(prompt)) followed by return x." },

    // Pythonic philosophy
    { id: "cs50p-w3-q22", q: "What is the 'Pythonic' approach to handling potentially invalid input?", options: ["Check every possible condition with if/elif/else first", "Try the operation and handle the exception if it fails", "Always convert input to strings first before processing", "Require the user to specify the data type in advance"], answer: 1, explanation: "Python follows the 'EAFP' philosophy: Easier to Ask Forgiveness than Permission. Rather than checking if input is valid first, try the operation and catch exceptions if it fails." },
    { id: "cs50p-w3-q23", q: "A caller uses get_int() which handles ValueError internally with 'pass'. Does the caller know an error occurred?", options: ["Yes, through a system variable that tracks exceptions", "Yes, get_int() returns None on error to signal the issue", "No — the error is fully handled inside get_int() and hidden from the caller", "Yes, Python logs it automatically to a system error stream"], answer: 2, explanation: "When get_int() uses pass in its except block, it silently retries until it gets valid input. The caller only sees the final valid integer and never knows how many attempts it took." },

    // Order of operations & assignment
    { id: "cs50p-w3-q24", q: "In 'x = int(input(...))', which function executes first?", options: ["int() processes the conversion first", "The assignment (=) runs before the functions", "input()", "They all execute simultaneously in parallel"], answer: 2, explanation: "Python evaluates expressions from the inside out. input() runs first to get the user's string, then int() converts it, and only then does = assign the result to x." },
    { id: "cs50p-w3-q25", q: "In that same line, if int() raises an error, what happens to the assignment to x?", options: ["x gets assigned None as a placeholder value", "x gets assigned 0 as a safe default integer", "The assignment never happens — the error occurs before the value reaches the left side of =", "x keeps whatever value it had before the line ran"], answer: 2, explanation: "Assignment happens last, after all expressions on the right side finish evaluating. If int() raises an exception, execution jumps to except before the = assignment ever runs." },

    // Error messages
    { id: "cs50p-w3-q26", q: "What does 'unterminated string literal' mean?", options: ["A variable name is too long for Python to parse", "You started a string with a quote but never closed it", "The program ran out of memory while processing strings", "A string contains illegal characters that can't be encoded"], answer: 1, explanation: "This syntax error means you opened a string with a quote mark but Python reached the end of the line without finding the matching closing quote. Every opening quote needs a closing one." },
    { id: "cs50p-w3-q27", q: "What does 'invalid literal for int() with base 10' mean?", options: ["The number is too large for Python to store as an int", "You passed a value to int() that can't be converted to a decimal integer", "The integer overflowed its maximum allowed bit width", "Base 10 is not supported and you must use binary"], answer: 1, explanation: "This ValueError message means int() received a string (like 'cat' or '3.5') that doesn't represent a valid whole number in base 10 (decimal). Only strings of digits (optionally with a leading minus) are valid." },
  ],
},

// ─── LECTURE 4 ───────────────────────────────────────────────────────
{
  course: "CS50P",
  title: "CS50P Week 4: Libraries",
  questions: [
    // Modules & imports
    { id: "cs50p-w4-q1", q: "What is a module in Python?", options: ["A type of variable used for storage", "A library with one or more functions or features", "A built-in data type like int or str", "A testing framework for unit tests"], answer: 1, explanation: "A module is a Python file containing functions, classes, or variables that you can import and reuse. Python comes with many built-in modules (like random and sys), and you can also create your own." },
    { id: "cs50p-w4-q2", q: "What problem does factoring code into a library solve?", options: ["It makes code run faster at execution time", "It eliminates copying and pasting the same code across projects", "It reduces memory usage during program execution", "It prevents syntax errors from occurring in code"], answer: 1, explanation: "Libraries let you write code once and reuse it across multiple programs via import. This avoids duplication, reduces bugs, and makes maintenance easier since fixes only need to happen in one place." },
    { id: "cs50p-w4-q3", q: "What is the difference between 'import random' and 'from random import choice'?", options: ["There is no practical difference between them", "The first gives access to all functions via random.X; the second loads choice directly into your namespace", "The second is faster to execute at runtime", "The first only imports one function from the module"], answer: 1, explanation: "import random gives you the whole module, requiring random.choice() syntax. from random import choice pulls choice directly into your namespace, so you call choice() without the prefix." },
    { id: "cs50p-w4-q4", q: "What is the risk of using 'from random import choice' instead of 'import random'?", options: ["It's slower to import and execute than the alternative", "If you define your own variable or function called 'choice', it will collide with the imported one", "It doesn't work in all Python versions or platforms", "It imports the entire module anyway despite the syntax"], answer: 1, explanation: "Importing names directly into your namespace risks name collisions. If you later define a variable called 'choice', it overwrites the imported function. Using import random avoids this since you access it as random.choice." },
    { id: "cs50p-w4-q5", q: "When you write 'import random', where does Python find the random module?", options: ["It downloads it from the internet automatically", "It's a file (random.py) installed alongside the Python interpreter", "It's built into the print function as a sub-feature", "It generates the module dynamically at runtime"], answer: 1, explanation: "Python's standard library modules are .py files installed with Python itself. When you import one, Python searches a predefined set of directories (sys.path) and loads the matching file." },

    // random module
    { id: "cs50p-w4-q6", q: "What does random.choice do when given a list of 3 items?", options: ["Returns all three items in a random order", "Returns one item with equal probability (1/3 each)", "Returns the first item in the list every time", "Returns a random number between 1 and 3 instead"], answer: 1, explanation: "random.choice() picks and returns one random element from a sequence. Each element has an equal probability of being selected, making it useful for random selections from a list." },
    { id: "cs50p-w4-q7", q: "What does random.shuffle do differently from most functions we've seen?", options: ["It returns a new list with the elements reordered", "It modifies the list in place rather than returning a new value", "It only works with numbers, not other data types", "It requires two arguments to specify the range"], answer: 1, explanation: "Most functions return a new value without changing the input. shuffle() is different: it rearranges the original list directly and returns None. This is called modifying 'in place.'" },
    { id: "cs50p-w4-q8", q: "You call random.randint(1, 10). Which values are possible?", options: ["1 through 9 only", "0 through 10 inclusive", "1 through 10 inclusive", "0 through 9 only"], answer: 2, explanation: "Unlike range(), randint() is inclusive on both ends. randint(1, 10) can return any integer from 1 to 10, including both 1 and 10. This is an important distinction to remember." },

    // sys.argv & command line arguments
    { id: "cs50p-w4-q9", q: "What does sys.argv contain?", options: ["The number of lines in your program's source code", "A list of all words typed at the command line, including the filename", "The system's environment variables and their values", "The return value of the main() function call"], answer: 1, explanation: "sys.argv is a list of strings from the command line. The program name is always at index 0, and any additional arguments follow. This lets programs accept input without interactive prompts." },
    { id: "cs50p-w4-q10", q: "If you run 'python name.py David', what is sys.argv[0]?", options: ["python", "David", "name.py", "0"], answer: 2, explanation: "sys.argv[0] is always the name of the script being run. In 'python name.py David', sys.argv is ['name.py', 'David'], so index 0 is 'name.py' and index 1 is 'David'." },
    { id: "cs50p-w4-q11", q: "If you run 'python name.py' with no additional arguments and your code accesses sys.argv[1], what happens?", options: ["It returns None as a default placeholder", "It returns an empty string with no characters", "An IndexError exception is raised", "The program silently exits without any output"], answer: 2, explanation: "With no extra arguments, sys.argv only contains ['name.py']. Accessing index 1 is out of bounds, so Python raises an IndexError. Always check the length or use try/except before accessing arguments." },
    { id: "cs50p-w4-q12", q: "What are two ways to handle missing command line arguments?", options: ["try/except for IndexError, or check len(sys.argv) before accessing", "Use input() as a backup prompt, or crash gracefully with a message", "Call sys.fix() to auto-repair, or restart the program from scratch", "There is only one way: try/except handles all argument issues"], answer: 0, explanation: "You can either catch the IndexError with try/except, or proactively check len(sys.argv) to verify the right number of arguments were provided. Both approaches prevent crashes from missing arguments." },
    { id: "cs50p-w4-q13", q: "Why might you prefer checking len(sys.argv) with if/elif/else over try/except?", options: ["It's faster to execute at runtime overall", "You can give more specific error messages (too few vs. too many)", "try/except doesn't work with sys.argv at all", "There's no reason to prefer one approach over the other"], answer: 1, explanation: "Checking len(sys.argv) lets you distinguish between too few and too many arguments, providing specific error messages for each case. try/except only tells you the access failed, not why." },
    { id: "cs50p-w4-q14", q: "What does sys.exit() do?", options: ["Logs the user out of the computer entirely", "Terminates the program immediately, optionally printing a message", "Exits the current function and returns to the caller", "Closes the terminal window and all running processes"], answer: 1, explanation: "sys.exit() stops the entire program. You can pass a string message that gets printed to the console, or an integer exit code (0 for success, nonzero for error). It raises SystemExit internally." },
    { id: "cs50p-w4-q15", q: "Why can't you use 'break' instead of sys.exit() to stop a program?", options: ["break is deprecated in modern Python versions", "break only exits loops, not the entire program", "break requires a numerical argument to work properly", "You can use them interchangeably in all contexts"], answer: 1, explanation: "break is limited to exiting the innermost loop. To stop the entire program from anywhere (including outside loops), you need sys.exit(), which terminates the whole process." },

    // Slicing
    { id: "cs50p-w4-q16", q: "What does sys.argv[1:] return?", options: ["The first element only from the list", "All elements except the first (the filename)", "The last element only from the list", "A copy of the entire list unchanged"], answer: 1, explanation: "The slice [1:] means 'from index 1 to the end.' Since sys.argv[0] is the script name, [1:] gives you just the user-provided arguments, which is usually what you want." },
    { id: "cs50p-w4-q17", q: "In list slicing, what does a negative index like [-1] mean?", options: ["The first element of the list", "An error that halts execution", "Counting from the end of the list", "Remove the last element from the list"], answer: 2, explanation: "Negative indices count backward from the end: [-1] is the last element, [-2] is second-to-last, and so on. This is convenient when you need the end of a list without knowing its length." },

    // Packages & pip
    { id: "cs50p-w4-q18", q: "What is pip?", options: ["A Python integrated development environment", "A package manager that installs third-party libraries", "A built-in Python function for package handling", "A type of Python file used for configuration"], answer: 1, explanation: "pip is Python's package installer. It downloads and installs third-party libraries from PyPI (Python Package Index), making thousands of community-built tools available with a single command." },
    { id: "cs50p-w4-q19", q: "What is the difference between a module that comes with Python and a package you install with pip?", options: ["There is no difference after installation is complete", "Modules are built-in; packages are third-party and must be installed separately", "Packages are faster to import and execute at runtime", "Modules can't be imported into your own Python files"], answer: 1, explanation: "Standard library modules (like random, sys) ship with Python. Third-party packages (like requests, cowsay) are created by the community and must be installed via pip before you can import them." },
    { id: "cs50p-w4-q20", q: "After running 'pip install cowsay', what can you then do in your code?", options: ["Nothing — pip only downloads the documentation files", "import cowsay and call its functions", "cowsay runs automatically without any import needed", "You must restart the Python interpreter before using it"], answer: 1, explanation: "pip install downloads and sets up the package. After that, you use import in your code just like any other module. The package stays installed until you explicitly remove it with pip uninstall." },

    // APIs & JSON
    { id: "cs50p-w4-q21", q: "What is an API in the context of web services?", options: ["A Python-specific file format for data storage", "A mechanism to access data on someone else's server programmatically", "A type of database that stores web page content", "A graphical user interface for building web apps"], answer: 1, explanation: "An API (Application Programming Interface) lets your code communicate with remote services over the internet. You send a request to a URL and get structured data back, allowing programs to access data from other systems." },
    { id: "cs50p-w4-q22", q: "What does the requests library do?", options: ["Prompts the user for keyboard input", "Makes HTTP requests to URLs, like a browser would", "Manages file permissions on the system", "Sends emails through a Python interface"], answer: 1, explanation: "The requests library sends HTTP requests (GET, POST, etc.) to web servers and returns the response. It is the standard way for Python programs to interact with web APIs and download data." },
    { id: "cs50p-w4-q23", q: "What is JSON?", options: ["A Python-only data format for serialization", "A language-agnostic text format using key-value pairs, curly braces, and square brackets", "A type of database for storing structured records", "A compression algorithm for reducing file sizes"], answer: 1, explanation: "JSON (JavaScript Object Notation) is a lightweight text format for exchanging data between systems. Its syntax of key-value pairs, objects, and arrays maps naturally to Python dictionaries and lists." },
    { id: "cs50p-w4-q24", q: "When you call response.json() in Python, what type of object do you get back?", options: ["A raw string of the JSON text content", "A Python dictionary", "A JSON file saved to disk automatically", "A list of tuples with key-value pairs"], answer: 1, explanation: "response.json() parses the JSON text and converts it into Python data structures. JSON objects become Python dictionaries, JSON arrays become lists, making the data immediately usable in your code." },
    { id: "cs50p-w4-q25", q: "Why did the lecture use json.dumps() with indent=2?", options: ["To compress the data into a smaller representation", "To pretty-print the JSON response in a human-readable format", "To convert it to a CSV file for spreadsheet use", "To encrypt the data before displaying it onscreen"], answer: 1, explanation: "json.dumps() converts a Python object back to a JSON string. The indent=2 parameter adds line breaks and indentation, making nested data structures readable instead of appearing as one long line." },
    { id: "cs50p-w4-q26", q: "In the iTunes API response, the 'results' key contains what type of value?", options: ["A single string with all the song titles combined", "A list of dictionaries, each representing a song", "An integer count of the number of total results", "Another URL pointing to the full data download"], answer: 1, explanation: "API responses often nest data structures. The 'results' key holds a list where each element is a dictionary with keys like 'trackName' and 'artistName', representing one song's metadata." },

    // Creating your own modules
    { id: "cs50p-w4-q27", q: "If you create a file called helpers.py with a function greet(), how do you use it from another file?", options: ["Just call greet() directly without importing", "from helpers import greet", "import greet from helpers", "You can't import your own files in Python"], answer: 1, explanation: "Your own .py files work just like built-in modules. Use from helpers import greet (without the .py extension) to import specific functions, or import helpers to use helpers.greet()." },
    { id: "cs50p-w4-q28", q: "You have sayings.py with main() called at the bottom. When another file does 'from sayings import hello', what happens?", options: ["Only hello is loaded; main is not called at all", "Python reads the whole file top-to-bottom, so main() also runs", "An import error occurs because main blocks the import", "Python skips the main function automatically during import"], answer: 1, explanation: "When Python imports a file, it executes the entire file from top to bottom. If main() is called at the bottom without protection, it runs during import, causing unintended side effects." },
    { id: "cs50p-w4-q29", q: "What does 'if __name__ == \"__main__\"' prevent?", options: ["Syntax errors from occurring in the module code", "The main function from running when the file is imported by another file", "Other files from accessing your defined functions at all", "Variables from leaking into the global scope of other modules"], answer: 1, explanation: "This guard checks whether the file is being run directly or imported. When imported, __name__ is set to the module name (not '__main__'), so the main() call is skipped, preventing unintended execution." },
    { id: "cs50p-w4-q30", q: "When is __name__ set to '__main__'?", options: ["Always, regardless of how the file is used", "Only when the file is run directly from the command line, not when imported", "Only when the file is imported by another module", "Only inside functions that are defined in the module"], answer: 1, explanation: "Python sets __name__ to '__main__' only when the file is the entry point (run directly). When the file is imported, __name__ is set to the module's name instead, enabling the if __name__ guard." },

    // Design reasoning
    { id: "cs50p-w4-q31", q: "Why might you prefer command line arguments over calling input() for every value?", options: ["input() is deprecated in modern Python releases", "Command line arguments are faster for repeated use and enable automation", "input() doesn't work in Python 3 or later versions", "There's no practical difference between the two approaches"], answer: 1, explanation: "Command line arguments let you run programs non-interactively, which is essential for automation and scripting. You can chain commands, use them in scripts, and avoid manual input each time." },
    { id: "cs50p-w4-q32", q: "A JSON response contains nested dictionaries. To get the track name, you write result['trackName']. Where does the key name 'trackName' come from?", options: ["You choose it yourself when writing your code", "Python generates it automatically from the response data", "The API developer defined it — you must match their key names exactly", "It's a Python reserved word for media-related API calls"], answer: 2, explanation: "API key names are defined by whoever built the API. You must read their documentation to learn the exact key names and match them precisely in your code, including capitalization." },
  ],
},

// ─── LECTURE 5 ───────────────────────────────────────────────────────
{
  course: "CS50P",
  title: "CS50P Week 5: Unit Tests",
  questions: [
    // Why test & testing philosophy
    { id: "cs50p-w5-q1", q: "Why is manually testing your program by running it with sample inputs insufficient?", options: ["It's too slow for the computer to process efficiently", "You might forget to test important corner cases and can't easily repeat tests", "Manual testing only works on small programs under 100 lines", "Python doesn't support manual testing of any programs"], answer: 1, explanation: "Manual testing relies on human memory and discipline. Automated tests can be run repeatedly with consistent inputs, ensuring corner cases are checked every time you modify the code." },
    { id: "cs50p-w5-q2", q: "What is a 'unit test'?", options: ["A test of the entire program from start to finish", "A test of an individual function or small piece of code", "A test that only runs once and then is discarded", "A test written by someone other than the code author"], answer: 1, explanation: "Unit tests isolate and verify individual functions by calling them with known inputs and checking the outputs. This makes it easy to pinpoint exactly where a bug is when a test fails." },
    { id: "cs50p-w5-q3", q: "Why is it bad practice to put all your logic in main instead of separate functions?", options: ["main runs slower than other functions in Python", "Separate functions are easier to test because they have defined inputs and outputs", "Python limits the main function to only 50 lines of code", "It causes import errors when other files use the module"], answer: 1, explanation: "Functions with clear inputs (parameters) and outputs (return values) can be tested independently. If all logic lives in main, you can only test the whole program at once, making it hard to isolate bugs." },

    // Test file conventions
    { id: "cs50p-w5-q4", q: "What is the conventional naming pattern for a file that tests calculator.py?", options: ["check_calculator.py", "test_calculator.py", "calculator_test_suite.py", "verify_calculator.py"], answer: 1, explanation: "The test_ prefix is a convention that pytest recognizes automatically. Naming your test file test_calculator.py lets pytest discover and run it without any extra configuration." },
    { id: "cs50p-w5-q5", q: "Why do we add 'if __name__ == \"__main__\"' before calling main in calculator.py?", options: ["It makes the program run faster during testing", "It prevents main from executing when we import functions from the file", "It's required Python syntax for all module files", "It enables debugging mode for the test runner"], answer: 1, explanation: "When the test file imports functions from calculator.py, Python runs the entire file. Without this guard, main() would execute during import, causing unwanted side effects like prompting for input." },
    { id: "cs50p-w5-q6", q: "If you want to test a function called 'square', what is the conventional test function name?", options: ["check_square", "square_verify", "test_square", "validate_square"], answer: 2, explanation: "pytest discovers test functions by looking for names starting with test_. Naming your function test_square follows this convention and ensures pytest finds and runs it automatically." },

    // Assert keyword
    { id: "cs50p-w5-q7", q: "What does the 'assert' keyword do when the expression is True?", options: ["Prints 'passed' to the console output", "Returns True to the calling function", "Nothing — execution continues silently", "Logs the result to a file on disk"], answer: 2, explanation: "assert is a silent sentinel: when the condition is True, nothing happens and the program continues. It only takes action (raising an AssertionError) when the condition is False." },
    { id: "cs50p-w5-q8", q: "What happens when an assert statement's expression evaluates to False?", options: ["The program prints 'False' to the terminal", "An AssertionError is raised", "The line is skipped and execution continues", "Python retries the expression automatically"], answer: 1, explanation: "When assert finds a False expression, it raises an AssertionError, which signals that something unexpected happened. Testing frameworks like pytest catch this error and report it as a test failure." },
    { id: "cs50p-w5-q9", q: "What is the advantage of 'assert square(2) == 4' over an if-statement that checks the same thing?", options: ["Assert is faster at runtime than if-statements", "It's less code — you skip the if/print boilerplate", "Assert catches more types of bugs than if-checks do", "There is no advantage to using assert over if"], answer: 1, explanation: "assert lets you express a test expectation in a single line. Without it, you'd need an if-statement, a print call, and possibly a sys.exit(), making each test check much more verbose." },

    // Corner cases & test design
    { id: "cs50p-w5-q10", q: "Why did testing only square(2) fail to catch the bug where n*n was replaced with n+n?", options: ["2 is too small to test with reliably", "Because 2+2 == 2*2, so the bug is invisible for that input", "Assert doesn't work with the number 2 as input", "The test file had a syntax error that masked the bug"], answer: 1, explanation: "The number 2 is special because 2+2 and 2*2 both equal 4. Testing with other values like 3 (where 3+3=6 but 3*3=9) would reveal the bug. This illustrates why testing multiple inputs matters." },
    { id: "cs50p-w5-q11", q: "When testing a mathematical function like square, which set of inputs best covers corner cases?", options: ["Only positive integers above zero", "Only large numbers in the thousands range", "Positive, negative, and zero", "Random floating-point values each run"], answer: 2, explanation: "Good test coverage includes boundary values and representative cases. Zero, negative numbers, and positive numbers exercise different code paths and reveal bugs that single-category testing misses." },
    { id: "cs50p-w5-q12", q: "Why should you keep individual test functions simple and short?", options: ["Python limits test functions to a maximum of 10 lines", "Complex tests might themselves contain bugs, and you don't want to write tests for your tests", "Simple tests run faster than longer ones in the test runner", "pytest rejects long functions and will not execute them"], answer: 1, explanation: "If a test is complicated, you might introduce bugs into the test itself. Simple tests with straightforward assert statements are easy to verify by inspection and less likely to be wrong." },

    // pytest basics
    { id: "cs50p-w5-q13", q: "What is pytest?", options: ["A built-in Python keyword for testing assertions", "A third-party testing framework that automates running and reporting tests", "A Python IDE designed specifically for test-driven development", "A type checker for Python that validates annotations"], answer: 1, explanation: "pytest is a popular third-party testing framework that discovers test functions automatically, runs them, catches assertion errors, and produces clear reports showing which tests passed or failed." },
    { id: "cs50p-w5-q14", q: "How do you install pytest?", options: ["It comes pre-installed with Python by default", "import pytest at the top of your file to activate it", "pip install pytest", "download it from python.org as a separate installer"], answer: 2, explanation: "pytest is a third-party package, so you install it with pip install pytest. After installation, the pytest command becomes available in your terminal to run test files." },
    { id: "cs50p-w5-q15", q: "How do you run tests with pytest?", options: ["python test_calculator.py", "pytest test_calculator.py", "python -m test test_calculator.py", "run test_calculator.py"], answer: 1, explanation: "You run pytest from the command line followed by the test filename. pytest discovers all functions starting with test_, runs them, and reports the results with clear pass/fail indicators." },
    { id: "cs50p-w5-q16", q: "When using pytest, do you need to write your own main function, try/except blocks, or print statements in the test file?", options: ["Yes, all three are required by the framework", "You need main but not try/except for it to work", "No — pytest handles all of that automatically", "You still need try/except but not a main function"], answer: 2, explanation: "pytest handles test discovery, execution, error catching, and result reporting. You just write functions with assert statements, and pytest takes care of everything else." },

    // pytest output & multiple test functions
    { id: "cs50p-w5-q17", q: "In pytest output, what does a dot (.) represent?", options: ["A skipped test case", "A passed test", "A warning about the test", "An incomplete test that timed out"], answer: 1, explanation: "pytest uses concise symbols: a dot (.) means a test passed, F means it failed, and E means an error occurred. This lets you quickly scan results and spot failures." },
    { id: "cs50p-w5-q18", q: "In pytest output, what does F represent?", options: ["Fixed", "Fatal error", "Failed test", "Finished"], answer: 2, explanation: "F in pytest output means a test function raised an AssertionError, indicating the code produced an unexpected result. pytest then shows details about what value was expected versus what was actually returned." },
    { id: "cs50p-w5-q19", q: "If you put all five assertions in one test function and the second one fails, what happens to assertions 3-5?", options: ["They still run and report their own results", "They are skipped — the function stops at the first failure", "They run but their results are hidden from the output", "They are moved to a separate report for later review"], answer: 1, explanation: "An AssertionError stops the current function immediately. Any assertions after the failure are never reached, so you lose diagnostic information about whether those cases pass or fail." },
    { id: "cs50p-w5-q20", q: "Why is splitting tests into test_positive, test_negative, and test_zero better than one big test_square function?", options: ["It's required by pytest to have separate test functions", "Each function runs independently, so a failure in one doesn't prevent the others from providing clues", "Smaller functions use less memory during test execution", "pytest only supports functions with one assert statement each"], answer: 1, explanation: "Separate test functions run independently. If test_negative fails, test_positive and test_zero still run, giving you more information about what works and what doesn't." },

    // Testing exceptions with pytest.raises
    { id: "cs50p-w5-q21", q: "How do you test that a function raises a specific exception (e.g., TypeError)?", options: ["assert square('cat') == TypeError as the expected result", "Use try/except in your test function to catch the error", "Use 'with pytest.raises(TypeError):' then call the function", "You cannot test for exceptions using any testing framework"], answer: 2, explanation: "pytest.raises() is a context manager that expects the code inside it to raise a specific exception. If the exception is raised, the test passes. If not, the test fails." },
    { id: "cs50p-w5-q22", q: "To use pytest.raises, what must you import?", options: ["from pytest import raises directly into the namespace", "import pytest (then use pytest.raises)", "import assert from the standard library modules", "from exceptions import raises as a utility function"], answer: 1, explanation: "You import the pytest module and then access raises as pytest.raises(). This is the standard way to test that your code properly raises exceptions for invalid inputs." },

    // Return values vs side effects
    { id: "cs50p-w5-q23", q: "Why can't you use 'assert hello('David') == \"Hello, David\"' if hello() uses print instead of return?", options: ["print is not allowed inside functions that are tested", "print produces a side effect but returns None, so the comparison fails", "Strings can't be compared with == in assert statements", "assert doesn't work with functions that handle strings"], answer: 1, explanation: "print() displays text on screen but returns None. So hello('David') evaluates to None, and None == 'Hello, David' is False, causing the assertion to fail even though the output looks correct." },
    { id: "cs50p-w5-q24", q: "What change makes a function that prints its result more testable?", options: ["Add a try/except block around the print statement", "Replace print with return and let the caller handle printing", "Use f-strings instead of string concatenation throughout", "Add type hints to the function signature and parameters"], answer: 1, explanation: "Functions that return values are easy to test because you can compare the return value to the expected result. Functions that print instead produce side effects that are harder to verify programmatically." },
    { id: "cs50p-w5-q25", q: "What is a 'side effect' in the context of functions?", options: ["A bug caused by the function's internal logic", "An observable behavior like printing to screen, rather than returning a value", "A variable that unexpectedly changes type during execution", "An unused import that wastes memory in the program"], answer: 1, explanation: "Side effects are interactions with the outside world: printing, writing files, modifying global variables. They make functions harder to test because you can't simply check a return value." },

    // Organizing tests in folders
    { id: "cs50p-w5-q26", q: "What special file must exist inside a test folder for Python to treat it as a package?", options: ["setup.py", "__init__.py", "config.py", "__main__.py"], answer: 1, explanation: "__init__.py tells Python that the directory is a package, allowing imports to work across the folder structure. Without it, Python won't recognize the folder as containing importable modules." },
    { id: "cs50p-w5-q27", q: "What happens when you run 'pytest test/' where test/ is a folder?", options: ["It runs only the first test file it encounters", "It searches the folder and runs all test files it finds", "It fails because pytest only accepts single file arguments", "It creates new test files automatically in the folder"], answer: 1, explanation: "pytest recursively searches the given directory for files matching the test_ pattern and runs all the test functions it discovers. This makes it easy to organize tests into folders." },
    { id: "cs50p-w5-q28", q: "The __init__.py file inside a test folder can be:", options: ["Only a configuration file with specific required content inside", "Empty — its presence alone signals the folder is a package", "A copy of the module being tested for reference purposes", "A list of all test functions that need to be discovered"], answer: 1, explanation: "An __init__.py file can be completely empty. Its mere existence in a directory is what signals to Python that the directory should be treated as a package, enabling imports." },

    // Testability & design
    { id: "cs50p-w5-q29", q: "What makes a function 'testable' in the unit-testing sense?", options: ["It has no parameters and no dependencies", "It has well-defined inputs (parameters) and outputs (return values)", "It uses only built-in types like int and str", "It is fewer than 10 lines long in total"], answer: 1, explanation: "A testable function takes inputs through parameters and communicates results through return values. This lets tests provide known inputs and verify expected outputs without user interaction." },
    { id: "cs50p-w5-q30", q: "If your main function handles user input and your square function handles math, which should your test file focus on?", options: ["main, because that's where most errors start", "Both equally, since all code should be tested the same way", "square, because it has defined inputs/outputs independent of user interaction", "Neither — test the whole program end-to-end as one unit"], answer: 2, explanation: "Pure functions like square() are ideal for unit testing because they take inputs and return outputs without side effects. Functions that call input() are harder to test automatically." },
  ],
},

// ─── LECTURE 6 ───────────────────────────────────────────────────────
{
  course: "CS50P",
  title: "CS50P Week 6: File I/O",
  questions: [
    // File modes & persistence
    { id: "cs50p-w6-q1", q: "What happens to data stored in variables when a Python program exits?", options: ["It's saved automatically to a temp file", "It's lost — variables exist only in memory", "It's cached for the next run of the program", "It depends on the variable type being stored"], answer: 1, explanation: "Variables live in RAM (memory), which is volatile. When the program ends, all its memory is freed. To persist data between runs, you must write it to a file or database." },
    { id: "cs50p-w6-q2", q: "What does open('file.txt', 'w') do if file.txt already exists?", options: ["Appends to the existing file contents", "Raises a FileExistsError exception", "Overwrites the file with a new empty one", "Opens it in read-only mode instead"], answer: 2, explanation: "The 'w' (write) mode truncates the file to zero length before writing. This means all previous content is erased. If you want to keep existing content, use 'a' (append) mode instead." },
    { id: "cs50p-w6-q3", q: "What is the difference between 'w' and 'a' mode in open()?", options: ["'w' is faster than 'a' for writing data", "'w' recreates the file each time; 'a' adds to the end", "'a' sorts the content alphabetically; 'w' does not", "There is no difference between the two modes"], answer: 1, explanation: "'w' wipes the file clean and starts fresh each time. 'a' preserves existing content and adds new data at the end. Choose based on whether you want to replace or accumulate data." },
    { id: "cs50p-w6-q4", q: "If you open a file in read mode, what happens if you try to write to it?", options: ["It silently fails without any notification", "It raises an error", "It works but doesn't save changes to disk", "It creates a backup copy of the file first"], answer: 1, explanation: "File modes enforce access restrictions. Opening in read mode ('r') and then calling write() raises an UnsupportedOperation error. You must open with 'w' or 'a' to write." },
    { id: "cs50p-w6-q5", q: "What is the default mode if you call open('file.txt') without a second argument?", options: ["Write ('w')", "Append ('a')", "Read ('r')", "Read-write ('r+')"], answer: 2, explanation: "When you omit the mode argument, Python defaults to 'r' (read). This is the safest default since reading can't accidentally destroy data, unlike 'w' which would erase the file." },

    // The 'with' statement
    { id: "cs50p-w6-q6", q: "What problem does the 'with' keyword solve when working with files?", options: ["It makes files read faster during large operations", "It automatically closes the file when the block ends, preventing you from forgetting", "It encrypts the file contents before writing them out", "It locks the file so other programs can't access it simultaneously"], answer: 1, explanation: "with is a context manager that guarantees the file is closed when the block ends, even if an exception occurs. Without it, forgetting to call close() can lead to data loss or resource leaks." },
    { id: "cs50p-w6-q7", q: "What happens to the file when code execution leaves a 'with' block?", options: ["Nothing — you must call close() manually yourself", "The file is automatically closed", "The file is deleted from the filesystem entirely", "The variable becomes None and the data is lost"], answer: 1, explanation: "The with statement automatically calls close() on the file when the block ends, whether it ends normally or due to an exception. This is the recommended way to handle files in Python." },

    // write() vs print()
    { id: "cs50p-w6-q8", q: "How does file.write() differ from print() regarding line endings?", options: ["write() adds a newline automatically; print() does not add one", "print() adds a newline automatically; write() does not", "Both add newlines automatically after each call to them", "Neither adds newlines and you must always add them yourself"], answer: 1, explanation: "print() appends \\n by default (controllable with the end parameter). write() writes exactly what you give it with no extras. You must explicitly include \\n in your strings when using write()." },
    { id: "cs50p-w6-q9", q: "You write three names to a file using write() without adding '\\n'. What does the file look like?", options: ["Three names on separate lines as expected", "All three names concatenated on one line", "Only the last name appears, overwriting the others", "An error is raised because newlines are required"], answer: 1, explanation: "Since write() doesn't add newlines automatically, calling it three times without \\n produces one continuous line like 'AliceBobCharlie'. You must add \\n yourself to separate lines." },

    // Reading files
    { id: "cs50p-w6-q10", q: "What does the readlines() method return?", options: ["A single string of all the file's content combined", "A list of lines, each still containing its newline character", "A dictionary mapping line numbers to their text content", "An integer count of the total number of lines"], answer: 1, explanation: "readlines() reads the entire file and returns a list where each element is one line, including the trailing \\n character. You typically need to strip each line to remove the newline." },
    { id: "cs50p-w6-q11", q: "What does 'for line in file' do in Python?", options: ["Reads the entire file into memory at once as a block", "Iterates over each line in the file one at a time", "Searches for a specific line matching a pattern", "Counts the number of lines in the file object"], answer: 1, explanation: "Python file objects are iterable. 'for line in file' reads one line per iteration, which is memory-efficient for large files since it doesn't load everything at once like readlines() does." },
    { id: "cs50p-w6-q12", q: "After reading a file, you see extra blank lines in your output. What is the most likely cause?", options: ["The file is corrupted and contains extra null bytes", "Both the file's newlines and print()'s automatic newline are producing double spacing", "Python adds blank lines by default when printing to the console", "The terminal is misconfigured and inserting extra line breaks"], answer: 1, explanation: "Each line from the file already ends with \\n, and print() adds another \\n. The result is two newlines (a blank line) between each line. Use rstrip() or print(line, end='') to fix this." },
    { id: "cs50p-w6-q13", q: "What does rstrip() do when called on a line read from a file?", options: ["Removes leading whitespace from the left side", "Removes trailing whitespace including the newline character", "Reverses the string from end to beginning", "Splits the string on whitespace into a list of words"], answer: 1, explanation: "rstrip() removes whitespace from the right end of a string, including \\n. This is commonly used when reading files to clean up the trailing newline that each line carries." },

    // Sorting file data
    { id: "cs50p-w6-q14", q: "Why can't you sort and print lines in a single pass through a file?", options: ["Python doesn't support sorting strings from files", "You need all the data in memory before you can determine the correct order", "Files are already sorted by the operating system automatically", "Sorting requires a database connection to process the data"], answer: 1, explanation: "Sorting requires comparing all items to determine their order. You must read the entire file into a list first, then sort the list, then print. You can't determine the final order while still reading." },
    { id: "cs50p-w6-q15", q: "What does sorted() return when given a list?", options: ["None — it sorts the list in place and returns nothing", "A new sorted list, leaving the original unchanged", "A dictionary of sorted values with index keys for each", "A tuple containing the sorted elements from the list"], answer: 1, explanation: "sorted() creates and returns a new sorted list without modifying the original. This contrasts with list.sort(), which sorts in place and returns None." },
    { id: "cs50p-w6-q16", q: "How do you reverse the sort order using sorted()?", options: ["sorted(list, order='desc')", "sorted(list, reverse=True)", "list.sort().reverse()", "sorted(list, direction=-1)"], answer: 1, explanation: "The reverse=True parameter tells sorted() to return elements in descending order instead of the default ascending order. This works for both numbers and strings (reverse alphabetical)." },

    // CSV fundamentals
    { id: "cs50p-w6-q17", q: "What does CSV stand for?", options: ["Coded String Values", "Comma Separated Values", "Column Sorted Variables", "Character Sequenced Vectors"], answer: 1, explanation: "CSV (Comma Separated Values) is a simple text format where each line is a row and commas separate the fields within each row. It is widely used for exchanging tabular data between programs." },
    { id: "cs50p-w6-q18", q: "You split a CSV line on commas, but one field contains 'number four, privet drive'. What goes wrong?", options: ["Nothing — split handles quoted fields correctly on its own", "split() breaks the address into two separate values, corrupting your data", "The comma is automatically escaped by Python before splitting", "Python raises a ValueError because the field count is wrong"], answer: 1, explanation: "str.split(',') doesn't understand CSV quoting conventions. A comma inside a quoted field gets treated as a delimiter, splitting one field into two. The csv module handles this correctly." },
    { id: "cs50p-w6-q19", q: "Why should you use Python's csv module instead of manually splitting on commas?", options: ["It's faster at reading files from the disk into memory", "It handles edge cases like commas inside quoted fields", "It's required by Python syntax for processing CSV data files", "It compresses the file automatically to save disk space"], answer: 1, explanation: "The csv module properly handles quoted fields, escaped characters, and other CSV edge cases that simple string splitting misses. It follows the full CSV specification so your parsing is robust." },

    // csv.reader vs csv.DictReader
    { id: "cs50p-w6-q20", q: "What data type does csv.reader return for each row?", options: ["A dictionary with column headers as keys", "A list of strings", "A tuple of the field values", "A single concatenated string of all fields"], answer: 1, explanation: "csv.reader parses each row into a list of strings. You access fields by numeric index (row[0], row[1]), which is simple but breaks if columns are reordered." },
    { id: "cs50p-w6-q21", q: "What does csv.DictReader use as dictionary keys for each row?", options: ["The row number as an integer index value", "The values from the first row (header) of the CSV", "Integer indices starting at 0 for each column", "You must specify keys manually in the constructor"], answer: 1, explanation: "DictReader reads the first row as column headers and uses them as dictionary keys for each subsequent row. This lets you access fields by name like row['name'] instead of row[0]." },
    { id: "cs50p-w6-q22", q: "If someone reorders the columns in your CSV file, which reader approach keeps working without code changes?", options: ["csv.reader with index-based access to each field", "csv.DictReader with named column access", "Both break equally when columns are rearranged", "Neither — you must rewrite the file to match"], answer: 1, explanation: "DictReader uses column names, not positions. If columns are reordered, row['name'] still works because it looks up by header name. csv.reader would break because row[0] now refers to a different column." },
    { id: "cs50p-w6-q23", q: "What must a CSV file have in its first row for DictReader to work automatically?", options: ["A row count indicating the number of data rows", "A header row with column names", "A version number for the CSV format being used", "Nothing special — it always works regardless of content"], answer: 1, explanation: "DictReader expects the first row to contain column names that become dictionary keys. Without a header row, you must pass fieldnames explicitly to the DictReader constructor." },

    // csv.writer and csv.DictWriter
    { id: "cs50p-w6-q24", q: "When using csv.writer, how do you specify the values for one row?", options: ["As a dictionary with column name keys", "As a list passed to writerow()", "As keyword arguments to the write function", "As a formatted string with commas between values"], answer: 1, explanation: "csv.writer.writerow() takes a list (or tuple) of values and writes them as one CSV row, automatically adding commas between fields and handling any necessary quoting." },
    { id: "cs50p-w6-q25", q: "What does csv.writer automatically do if a value contains a comma?", options: ["Escapes it with a backslash character", "Wraps the value in quotes", "Removes the comma from the value", "Raises an error about invalid field data"], answer: 1, explanation: "csv.writer handles the quoting for you. If a field contains a comma, it wraps the entire field in double quotes so the comma isn't mistaken for a delimiter when the file is read later." },
    { id: "cs50p-w6-q26", q: "What extra argument does csv.DictWriter require that csv.writer does not?", options: ["A file encoding for the output stream", "fieldnames — a list of column names", "A row count for the expected data entries", "A delimiter character to separate the fields"], answer: 1, explanation: "DictWriter needs to know the column names upfront (via fieldnames) so it can write values in the correct order from each dictionary and optionally write a header row." },

    // Sorting lists of dictionaries
    { id: "cs50p-w6-q27", q: "Why can't sorted() directly sort a list of dictionaries without additional information?", options: ["Dictionaries can't be compared — sorted doesn't know which key to use for ordering", "Dictionaries are immutable and cannot be rearranged in a list", "sorted() only works on strings and numbers, not complex types", "It can sort them — no extra information is needed at all"], answer: 0, explanation: "Dictionaries have no natural ordering. sorted() needs to know which field to compare (name? age? score?), so you must provide a key function that extracts the comparison value from each dict." },
    { id: "cs50p-w6-q28", q: "What does the 'key' parameter in sorted() expect?", options: ["A string naming the sort column to use for ordering", "A function that extracts the comparison value from each element", "A boolean for choosing ascending or descending order", "An integer index pointing to the sort position in each item"], answer: 1, explanation: "The key parameter takes a function that sorted() calls on each element to get the value used for comparison. For example, key=len sorts strings by their length instead of alphabetically." },
    { id: "cs50p-w6-q29", q: "What is a lambda function?", options: ["A function that runs in parallel with other code", "An anonymous function defined inline without a name", "A function that only works with lists and tuples", "A function imported from the lambda standard module"], answer: 1, explanation: "Lambda functions are small, unnamed functions defined with the lambda keyword. They are useful for short operations you only need once, like key functions for sorted()." },
    { id: "cs50p-w6-q30", q: "Given students as a list of dicts, what does sorted(students, key=lambda s: s['name']) do?", options: ["Filters students by name to remove duplicates", "Returns a new list of students sorted alphabetically by their 'name' value", "Modifies each student's name to be uppercase", "Groups students by name into sublists of matches"], answer: 1, explanation: "The lambda extracts s['name'] from each dictionary, and sorted() uses those name values to determine the order. The result is a new list of the same dictionaries, arranged alphabetically by name." },
    { id: "cs50p-w6-q31", q: "Why do you pass 'get_name' to sorted() without parentheses (key=get_name, not key=get_name())?", options: ["It's a syntax error to include parentheses in that context", "You're passing the function itself so sorted can call it repeatedly, not calling it once yourself", "Parentheses would reverse the sort order unexpectedly", "There's no difference between the two forms at all"], answer: 1, explanation: "Without parentheses, you pass the function object itself. sorted() then calls it on each element. With parentheses, you'd call the function immediately and pass its return value, which isn't what sorted() needs." },

    // Unpacking
    { id: "cs50p-w6-q32", q: "What does 'name, home = line.split(\",\")' do?", options: ["Creates a list with two elements named name and home", "Unpacks the two values returned by split into separate variables simultaneously", "Joins name and home into one combined string variable", "Raises an error if there aren't exactly two comma-separated values"], answer: 1, explanation: "This is tuple unpacking. split() returns a list of two items, and Python assigns the first to name and the second to home. The number of variables on the left must match the number of items." },

    // Binary files & Pillow
    { id: "cs50p-w6-q33", q: "What is the fundamental difference between a text file and a binary file?", options: ["Text files are smaller in size on disk than binary files", "Text files store human-readable characters; binary files store raw bytes for any data type", "Binary files can't be opened or processed in Python directly", "There is no real difference between the two file formats"], answer: 1, explanation: "Text files contain characters encoded in formats like UTF-8. Binary files store raw bytes representing anything: images, audio, executables. You open binary files with 'rb' or 'wb' mode in Python." },
    { id: "cs50p-w6-q34", q: "In sys.argv, what is always stored at index 0?", options: ["The first command-line argument provided by the user", "The name of the program itself", "The Python version currently being used to run the program", "The current working directory as a string path"], answer: 1, explanation: "sys.argv[0] always contains the script name. User-provided arguments start at index 1. This convention means sys.argv always has at least one element, even with no extra arguments." },
    { id: "cs50p-w6-q35", q: "What does sys.argv[1:] give you?", options: ["The first argument only from the command line", "All command-line arguments except the program name", "The last argument only from the list of arguments", "An error if there are no additional arguments provided"], answer: 1, explanation: "The slice [1:] skips index 0 (the script name) and returns everything else. If there are no extra arguments, it returns an empty list rather than raising an error." },
  ],
},

// ─── LECTURE 7 ───────────────────────────────────────────────────────
{
  course: "CS50P",
  title: "CS50P Week 7: Regular Expressions",
  questions: [
    // Core concept: what regular expressions are
    { id: "cs50p-w7-q1", q: "What is a regular expression (regex)?", options: ["A Python data type for storing text patterns", "A pattern used to match, validate, or extract data from strings", "A function that formats strings for output display", "A type of loop for iterating over individual characters"], answer: 1, explanation: "Regular expressions are a powerful pattern-matching language for strings. They let you define patterns like 'an email address' or 'a phone number' and search for, validate, or extract matching text." },

    // re.search basics
    { id: "cs50p-w7-q2", q: "What does re.search() return if the pattern is NOT found in the string?", options: ["An empty string with no content", "False", "None", "It raises an exception to signal the failure"], answer: 2, explanation: "re.search() returns None when no match is found. Since None is falsy, you can use 'if re.search(...)' to check whether a pattern matched, making the code clean and readable." },
    { id: "cs50p-w7-q3", q: "What two required arguments does re.search() take?", options: ["A string and a filename to search in", "A pattern and a string to search", "Two strings to compare against each other", "A pattern and a replacement string for substitution"], answer: 1, explanation: "re.search(pattern, string) takes the regex pattern as the first argument and the string to search through as the second. It returns a match object if found, or None if not." },

    // . * + ?
    { id: "cs50p-w7-q4", q: "In a regex, what does a single dot (.) represent?", options: ["A literal period character in the text", "Any single character except a newline", "The end of the pattern being matched", "A whitespace character like a space or tab"], answer: 1, explanation: "The dot is a wildcard that matches any single character (except newline by default). To match a literal period, you must escape it as \\. since the dot has special meaning in regex." },
    { id: "cs50p-w7-q5", q: "What is the difference between * and + in a regex?", options: ["* means one or more; + means zero or more repetitions", "* means zero or more; + means one or more", "They are identical in behavior and meaning", "* is greedy matching; + is lazy matching by default"], answer: 1, explanation: "* matches zero or more repetitions (the element is optional and can repeat). + requires at least one match. For example, .* matches an empty string but .+ requires at least one character." },
    { id: "cs50p-w7-q6", q: "Why does the pattern '.*@.*' match a string with ONLY an @ sign and nothing else?", options: ["The @ counts as a wildcard that matches anything", "* means zero or more, so no characters on either side is acceptable", "The dot matches the @ itself as any character would", "It's a bug in Python's re library that should be fixed"], answer: 1, explanation: "Since * means zero or more, .* can match zero characters. So the pattern accepts an @ with nothing before or after it. Use .+ (one or more) if you require at least one character on each side." },
    { id: "cs50p-w7-q7", q: "What does ? mean after a character or group in a regex?", options: ["Match any character in that position of the string", "The preceding element is optional (zero or one)", "Repeat the preceding element forever until end of string", "Negate the preceding element so it matches the opposite"], answer: 1, explanation: "The ? quantifier means the preceding element can appear zero or one time. For example, colou?r matches both 'color' and 'colour' because the 'u' is optional." },

    // Escaping with backslash
    { id: "cs50p-w7-q8", q: "How do you match a literal period in a regex, rather than 'any character'?", options: ["Use two periods (..) to indicate a literal", "Use \\. (backslash before the dot)", "Surround it in square brackets like [.]", "Both B and C work"], answer: 3, explanation: "Both \\. and [.] match a literal period. The backslash escapes the dot's special meaning, and inside square brackets, the dot loses its wildcard behavior and becomes literal." },
    { id: "cs50p-w7-q9", q: "Why should you prefix a regex string with r (e.g., r\"pattern\") in Python?", options: ["It makes the regex run faster during matching operations", "It tells Python to treat backslashes literally, avoiding misinterpretation as escape sequences", "It's required syntax — regex won't work at all without it", "It enables case-insensitive matching for the entire pattern"], answer: 1, explanation: "Raw strings (r'...') prevent Python from interpreting backslashes as escape sequences. Without r, \\d would be interpreted by Python before reaching the regex engine. With r, it passes through correctly." },

    // Anchors: ^ and $
    { id: "cs50p-w7-q10", q: "What does the ^ symbol mean at the START of a regex pattern?", options: ["Match any character at the current position", "Match the start of the string", "Negate the entire pattern that follows it", "Match a literal caret character in the text"], answer: 1, explanation: "^ anchors the pattern to the beginning of the string. Without it, re.search can find a match anywhere. With ^, the match must start at position 0." },
    { id: "cs50p-w7-q11", q: "What does $ at the END of a regex pattern mean?", options: ["Match a currency value in the text being searched", "Match the end of the string", "The pattern is optional and may not match at all", "Escape the last character in the pattern expression"], answer: 1, explanation: "$ anchors the pattern to the end of the string. Together with ^, they ensure the entire string matches the pattern, which is essential for validation (e.g., checking that a whole input is a valid email)." },
    { id: "cs50p-w7-q12", q: "Without ^ and $, what happens if the user types 'my email is malan@harvard.edu.' into a validator using re.search?", options: ["It fails because there are extra words around the email", "It matches, because re.search finds the pattern anywhere in the string", "It throws an error due to the period at the end of input", "It only matches the email portion and ignores the surrounding text"], answer: 1, explanation: "re.search looks for the pattern anywhere in the string, not just at the boundaries. Without ^ and $, the email pattern matches within the longer string, defeating the purpose of validation." },

    // Character sets and complements
    { id: "cs50p-w7-q13", q: "What does [abc] mean in a regex?", options: ["Match the literal string 'abc' in sequence", "Match any one character that is a, b, or c", "Match anything except the characters a, b, or c", "Match a followed by b followed by c in order"], answer: 1, explanation: "Square brackets define a character set. [abc] matches exactly one character that is either a, b, or c. It does not match the sequence 'abc' — that would just be the pattern abc without brackets." },
    { id: "cs50p-w7-q14", q: "What does [^@] mean inside a regex?", options: ["Match @ at the start of a string only", "Match any single character EXCEPT @", "Match a literal ^ followed by the @ character", "Match the @ sign optionally, zero or one time"], answer: 1, explanation: "Inside square brackets, ^ at the start negates the set. [^@] means 'match any character that is NOT @.' This is useful for matching everything up to a certain delimiter." },
    { id: "cs50p-w7-q15", q: "How do you specify a range of lowercase letters in a character set?", options: ["[a, b, c, ... z]", "[a-z]", "[lowercase]", "\\a-z"], answer: 1, explanation: "Inside square brackets, a hyphen between two characters creates a range. [a-z] matches any lowercase letter, [A-Z] matches uppercase, and [0-9] matches any digit." },

    // Shorthand character classes
    { id: "cs50p-w7-q16", q: "What does \\w match in a regex?", options: ["Whitespace characters only such as spaces and tabs", "Any word character: letters, digits, and underscore", "Only lowercase letters from a through z", "Any character including special symbols and punctuation"], answer: 1, explanation: "\\w is shorthand for [a-zA-Z0-9_], matching any 'word' character. It's commonly used to match identifiers, usernames, and other text that consists of letters, digits, and underscores." },
    { id: "cs50p-w7-q17", q: "What is the relationship between \\w and [a-zA-Z0-9_]?", options: ["They are unrelated and match different characters", "They match the same set of characters", "\\w matches more characters than the bracket form", "\\w excludes underscores unlike the bracket version"], answer: 1, explanation: "\\w is a convenient shorthand that matches exactly the same characters as [a-zA-Z0-9_]. Using \\w makes patterns shorter and easier to read." },
    { id: "cs50p-w7-q18", q: "What does \\d match?", options: ["Any digit 0-9", "Any non-digit character", "The letter d literally", "A decimal point character"], answer: 0, explanation: "\\d is shorthand for [0-9], matching any single digit. It's useful for patterns that expect numeric input, like phone numbers or zip codes." },
    { id: "cs50p-w7-q19", q: "What does a CAPITAL letter shorthand like \\D or \\W represent?", options: ["The same thing but with case-insensitive matching enabled", "The complement — everything the lowercase version does NOT match", "An error — capitals aren't valid in regex shorthands", "A literal backslash followed by the uppercase letter"], answer: 1, explanation: "Uppercase versions negate their lowercase counterparts. \\D matches any non-digit, \\W matches any non-word character, and \\S matches any non-whitespace character." },

    // Flags
    { id: "cs50p-w7-q20", q: "What does re.IGNORECASE do when passed as a flag to re.search?", options: ["Ignores whitespace characters in the pattern entirely", "Treats uppercase and lowercase letters as equivalent when matching", "Makes the search return only lowercase versions of results", "Disables special character interpretation in the regex pattern"], answer: 1, explanation: "re.IGNORECASE (or re.I) makes the pattern match regardless of letter case. 'Hello' would match 'hello', 'HELLO', or any capitalization variant." },
    { id: "cs50p-w7-q21", q: "Why might you use re.IGNORECASE instead of calling .lower() on the input?", options: ["It's faster to execute at runtime than .lower() is", "It validates without modifying the original string", "lower() doesn't work on email addresses or URLs", "There is no practical difference between the two approaches"], answer: 1, explanation: "re.IGNORECASE performs case-insensitive matching without changing the original string. Using .lower() modifies the data, which could be a problem if you need to preserve the original casing for display or storage." },

    // Groups and capturing
    { id: "cs50p-w7-q22", q: "What do parentheses () do in a regex when used with re.search?", options: ["They are literal characters to match in the string", "They group and CAPTURE the matched text, accessible via .group()", "They make the entire enclosed pattern optional to match", "They negate the enclosed pattern so it matches the opposite"], answer: 1, explanation: "Parentheses create capturing groups that remember the text they match. After a successful search, you can retrieve captured text with .group(1), .group(2), etc." },
    { id: "cs50p-w7-q23", q: "If a regex has two sets of capturing parentheses, what does matches.group(1) return?", options: ["The entire matched string including all groups", "The text matched by the first set of parentheses", "The text matched by the second set of parentheses", "Both groups combined into a single concatenated string"], answer: 1, explanation: "Groups are numbered left to right by their opening parenthesis. group(1) returns what the first set of parentheses matched, group(2) returns the second, and so on." },
    { id: "cs50p-w7-q24", q: "Why does group numbering start at 1 instead of 0 for captured groups?", options: ["It's a Python convention for all sequence types", "Group 0 is reserved for the entire matched string", "It's arbitrary with no technical reason behind it", "Group 0 represents the pattern itself as a raw string"], answer: 1, explanation: "group(0) returns the entire matched string (everything the whole pattern matched). Captured groups start at 1 so you can distinguish between the full match and individual captures." },
    { id: "cs50p-w7-q25", q: "What does (?:...) do compared to (...)?", options: ["It captures more aggressively than regular parentheses", "It groups without capturing — the match isn't stored as a group", "It makes the group optional so zero matches are acceptable", "It's invalid syntax that will raise a regex compilation error"], answer: 1, explanation: "(?:...) creates a non-capturing group. It groups the pattern for applying quantifiers or alternation, but doesn't create a numbered capture. This keeps group numbers tidy when you don't need the matched text." },
    { id: "cs50p-w7-q26", q: "You have the regex r'^(\\w+), (\\w+)$' and input 'Malan, David'. What is group(2)?", options: ["Malan", "David", "Malan, David", "None"], answer: 1, explanation: "The first (\\w+) captures 'Malan' as group(1). The second (\\w+) captures 'David' as group(2). The comma and space between them are part of the pattern but not captured." },

    // re.sub
    { id: "cs50p-w7-q27", q: "What does re.sub() do?", options: ["Searches for a pattern and returns the match object", "Substitutes (replaces) all occurrences of a pattern in a string with a replacement", "Splits a string by a pattern into a list of parts", "Submits the regex for compilation and optimization"], answer: 1, explanation: "re.sub(pattern, replacement, string) finds all matches of the pattern in the string and replaces them with the replacement text. It returns the modified string." },
    { id: "cs50p-w7-q28", q: "re.sub(r'https?://twitter\\.com/', '', url) — what does this produce if url is 'https://twitter.com/davidjmalan'?", options: ["https://twitter.com/", "davidjmalan", "twitter.com/davidjmalan", "An error"], answer: 1, explanation: "re.sub replaces the matched portion (the URL prefix) with an empty string, effectively removing it. What remains is just the username 'davidjmalan'." },

    // The walrus operator
    { id: "cs50p-w7-q29", q: "What does the := (walrus) operator allow you to do?", options: ["Compare two values for deep equality checking", "Assign a value and test it in the same expression (e.g., in an if statement)", "Define a constant that cannot be reassigned later", "Create a regular expression from a string pattern"], answer: 1, explanation: "The walrus operator := assigns a value to a variable and returns it in one expression. This is handy for 'if matches := re.search(...)' where you both check for a match and save it for later use." },

    // Finite state machines (conceptual)
    { id: "cs50p-w7-q30", q: "When re.search processes a regex, what computational model does it use internally?", options: ["A recursive descent parser for breaking down expressions", "A finite state machine (non-deterministic finite automaton)", "A binary search tree for efficient character lookups", "A hash table lookup that maps patterns to matches"], answer: 1, explanation: "Regular expressions are mathematically equivalent to finite automata. The regex engine builds a state machine that processes the input string character by character, transitioning between states." },
    { id: "cs50p-w7-q31", q: "In a finite state machine diagram for a regex, what does a 'double circle' state represent?", options: ["An error state where matching has failed permanently", "The start state where the machine begins processing input", "An accept state — the input is valid if the machine ends here", "A state that loops forever until the input is exhausted"], answer: 2, explanation: "In automata theory, a double circle marks an accept (final) state. If the machine reaches this state after processing the entire input, the string is considered a valid match for the pattern." },

    // Practical design wisdom
    { id: "cs50p-w7-q32", q: "Why is it a bad idea to write a long, complex regex all at once?", options: ["Python limits regex length to 256 characters maximum", "Mistakes are extremely hard to find — incremental building and testing is more effective", "Long regexes are slower to execute than short ones always", "It's fine — there's no disadvantage to writing them all at once"], answer: 1, explanation: "Complex regex patterns are notoriously hard to debug. Building and testing incrementally — starting simple and adding constraints one at a time — helps catch errors as they're introduced." },
    { id: "cs50p-w7-q33", q: "Instead of writing your own regex to validate email addresses, what does the lecture recommend?", options: ["Use a simpler check like just looking for an @ sign", "Copy the official RFC regex specification exactly into your code", "Use a well-tested third-party library", "Don't validate email addresses at all in your application"], answer: 2, explanation: "Email validation is surprisingly complex (the full RFC regex is thousands of characters). Well-tested libraries handle all the edge cases, so you don't have to reinvent the wheel or risk missing cases." },
    { id: "cs50p-w7-q34", q: "What is the practical difference between re.search, re.match, and re.fullmatch?", options: ["They are identical in behavior and result output", "search checks anywhere in the string; match anchors at the start; fullmatch anchors at both start and end", "match is faster than search due to internal optimizations", "fullmatch only works with \\w word character patterns"], answer: 1, explanation: "search finds a match anywhere in the string. match only matches at the beginning (like having an implicit ^). fullmatch requires the entire string to match (like having both ^ and $)." },

    // | (or) operator
    { id: "cs50p-w7-q35", q: "What does the vertical bar | mean in a regex?", options: ["Logical AND — both sides must match simultaneously", "Logical OR — match the pattern on the left or the right", "Pipe the output to another regex for chaining operations", "Escape the next character in the regex pattern string"], answer: 1, explanation: "The | operator means OR in regex. cat|dog matches either 'cat' or 'dog'. When used inside parentheses like (cat|dog), it limits the alternation to that group." },
    { id: "cs50p-w7-q36", q: "To match '.com' OR '.edu' at the end of a domain, which regex fragment works?", options: ["\\.(com)(edu)", "\\.(com|edu)", "\\.com.edu", "\\.[com,edu]"], answer: 1, explanation: "\\.(com|edu) matches a literal dot followed by either 'com' or 'edu'. The parentheses group the alternatives and the pipe | separates them. The backslash-dot ensures a literal period." },

    // Common pitfalls
    { id: "cs50p-w7-q37", q: "You use the pattern r'.+@.+\\.edu$' but forget the ^ anchor. What's the consequence?", options: ["It won't compile and will raise a regex syntax error", "A string like 'my email is malan@harvard.edu' will pass validation", "It will only match .edu domains and nothing else at all", "Nothing — the $ anchor alone is sufficient for validation"], answer: 1, explanation: "Without ^, re.search can find the pattern anywhere in the string. The email embedded in a longer sentence still matches the pattern, so the validation is too permissive." },
    { id: "cs50p-w7-q38", q: "You write r'https://twitter\\.com/' but the user pastes an http:// URL. Why does it fail?", options: ["http is not a valid protocol for web addresses", "The regex requires the 's' literally — you need to make it optional with s?", "re.search automatically handles both http and https protocols", "The backslash breaks the URL matching in the regex engine"], answer: 1, explanation: "The pattern literally requires 'https' with the 's'. To accept both http and https, use https? where the ? makes the 's' optional, matching either 'http' or 'https'." },
  ],
},

// ─── LECTURE 8 ───────────────────────────────────────────────────────
{
  course: "CS50P",
  title: "CS50P Week 8: Object-Oriented Programming",
  questions: [
    // Classes & objects fundamentals
    { id: "cs50p-w8-q1", q: "What is a class in Python?", options: ["A function that returns multiple values at once", "A blueprint for creating your own data type", "A built-in module for organizing code into sections", "A type of dictionary with methods attached to it"], answer: 1, explanation: "A class defines the structure and behavior for a custom data type. It specifies what data (attributes) and actions (methods) objects of that type will have, like a template." },
    { id: "cs50p-w8-q2", q: "What is the relationship between a class and an object?", options: ["They are the same thing in Python", "A class is a specific instance of an object", "An object is a specific instance created from a class", "Objects contain classes inside of them"], answer: 2, explanation: "A class is a blueprint; an object is a specific thing built from that blueprint. You can create many objects (instances) from a single class, each with its own data but sharing the same methods." },
    { id: "cs50p-w8-q3", q: "What does 'instantiation' mean?", options: ["Defining a class with its methods and attributes", "Importing a module into the current namespace", "Creating an object from a class", "Calling a method on an existing object instance"], answer: 2, explanation: "Instantiation is the process of creating a specific object (instance) from a class. When you write Student('Harry', 'Gryffindor'), you instantiate a new Student object with those specific values." },

    // Tuples, lists, dicts as return strategies
    { id: "cs50p-w8-q4", q: "What makes a tuple different from a list?", options: ["Tuples can only hold strings as their elements", "Tuples are immutable — you cannot change their values after creation", "Tuples are faster but otherwise identical to lists in every way", "Tuples must contain exactly two elements and no more"], answer: 1, explanation: "Tuples are immutable sequences: once created, their elements cannot be added, removed, or changed. This makes them useful for data that should stay fixed, like returning multiple values from a function." },
    { id: "cs50p-w8-q5", q: "If you try to assign a new value to an index in a tuple, what happens?", options: ["It silently fails without any notification", "A TypeError is raised", "It works but prints a warning to the console", "The tuple converts to a list automatically"], answer: 1, explanation: "Because tuples are immutable, any attempt to modify an element (like my_tuple[0] = 'new') raises a TypeError. This immutability is by design and helps prevent accidental changes to data." },
    { id: "cs50p-w8-q6", q: "What is the main advantage of a dictionary over a tuple for storing structured data?", options: ["Dictionaries are immutable and prevent accidental changes", "You access values by meaningful keys instead of numeric indices", "Dictionaries use less memory than tuples for the same data", "Dictionaries are faster to iterate through than tuples are"], answer: 1, explanation: "With a tuple, you access elements by position (index 0, 1, 2...), which gives no hint about what each value means. A dictionary uses descriptive keys like 'name' and 'house', making the code self-documenting." },
    { id: "cs50p-w8-q7", q: "When you return name, house with a comma in Python, what are you actually returning?", options: ["Two separate values sent back independently", "A single tuple containing both values", "A list with the two values as elements", "A dictionary with name and house as keys"], answer: 1, explanation: "Comma-separated values in Python automatically form a tuple. So 'return name, house' is equivalent to 'return (name, house)'. The caller can then unpack it with name, house = get_student()." },

    // __init__ method
    { id: "cs50p-w8-q8", q: "What is the purpose of the __init__ method?", options: ["To print the object to the console for debugging", "To delete the object from memory when it is no longer needed", "To initialize an object's attributes when it is first created", "To define the class name and its parent classes"], answer: 2, explanation: "__init__ is the constructor method that runs automatically when you create a new object. It sets up the object's initial state by assigning values to instance variables like self.name and self.house." },
    { id: "cs50p-w8-q9", q: "Why does __init__ take 'self' as its first parameter?", options: ["It's optional but conventional for readability purposes", "So you can access and store data inside the current object being created", "It refers to the class itself, not the individual object", "It's required for Python to parse the function definition correctly"], answer: 1, explanation: "self refers to the specific object being created or acted upon. Without it, __init__ would have no way to store attributes on the individual instance. Python passes self automatically when you call the class." },
    { id: "cs50p-w8-q10", q: "When you write Student('Harry', 'Gryffindor'), who calls __init__?", options: ["You call it explicitly with the self argument included", "Python calls it automatically when the object is constructed", "The print function calls it when displaying the new object", "It's never called — it runs at import time when the module loads"], answer: 1, explanation: "When you call a class like a function, Python internally creates a new object, then automatically calls __init__ on it with the arguments you provided. You never call __init__ directly." },
    { id: "cs50p-w8-q11", q: "If __init__ takes self, name, and house, how many arguments does the programmer pass when creating the object?", options: ["Three, including self explicitly", "Two — Python passes self automatically", "One, since the others have defaults", "It depends on the class hierarchy"], answer: 1, explanation: "Python automatically passes the new object as self, so you only provide the remaining arguments. Student('Harry', 'Gryffindor') passes two arguments, and Python fills in self behind the scenes." },

    // Instance variables & dot notation
    { id: "cs50p-w8-q12", q: "What does self.name = name do inside __init__?", options: ["Creates a local variable called name inside the method only", "Stores the value of name inside the current object as an instance variable", "Creates a class variable shared by all objects of this class", "Assigns name to the class itself rather than the instance"], answer: 1, explanation: "self.name creates an attribute on the specific object being initialized. The parameter 'name' is just a local variable, but self.name persists as part of the object and can be accessed later with object.name." },
    { id: "cs50p-w8-q13", q: "How do you access an instance variable called 'house' on an object called 'student'?", options: ["student['house']", "student.house", "student->house", "house(student)"], answer: 1, explanation: "Python uses dot notation to access attributes on objects. student.house reads the 'house' instance variable. Bracket notation is for dictionaries, -> is from C/C++, and function syntax would call house as a function." },

    // __str__ method
    { id: "cs50p-w8-q14", q: "If you print an object that has no __str__ method defined, what do you see?", options: ["An empty string with no visible output at all", "The object's class name and memory address", "A TypeError because it can't be printed without __str__", "The object's __init__ arguments listed in order"], answer: 1, explanation: "Without __str__, Python falls back to the default representation, which shows the class name and memory address (like <__main__.Student object at 0x...>). This is unhelpful, which is why defining __str__ is important." },
    { id: "cs50p-w8-q15", q: "What is the purpose of __str__?", options: ["To initialize the object's attributes and state", "To define how the object is represented as a string for display", "To compare two objects for equality checking", "To convert the object to an integer value"], answer: 1, explanation: "__str__ returns a human-readable string representation of the object. It determines what you see when you print the object or convert it to a string with str(). It should return something meaningful about the object's state." },
    { id: "cs50p-w8-q16", q: "The __str__ method is called automatically when you pass an object to which function?", options: ["len()", "type()", "print()", "input()"], answer: 2, explanation: "print() internally calls str() on each argument, which in turn calls the object's __str__ method. This is why defining __str__ controls what you see when you print an object." },

    // raise and error checking in classes
    { id: "cs50p-w8-q17", q: "Why is sys.exit() a poor choice for handling invalid input inside __init__?", options: ["It doesn't actually stop the program from continuing", "It's too extreme — it kills the entire program instead of signaling a catchable error", "It only works on Linux and macOS operating systems", "It requires importing the os module as a dependency"], answer: 1, explanation: "sys.exit() terminates the entire program, giving the caller no chance to handle the problem. Using raise instead signals an error that can be caught with try/except, allowing graceful recovery." },
    { id: "cs50p-w8-q18", q: "What does the 'raise' keyword do?", options: ["Catches an exception and handles it in place", "Creates and signals an exception that can be caught elsewhere with try/except", "Prints an error message to the console output", "Exits the program and returns to the terminal"], answer: 1, explanation: "raise creates an exception object and immediately interrupts normal execution flow. The exception propagates up the call stack until it's caught by a matching try/except block, or the program crashes if uncaught." },
    { id: "cs50p-w8-q19", q: "Why is validating data inside __init__ better than validating it in a separate function?", options: ["It runs faster than calling a separate function would", "It keeps all validation logic encapsulated with the class it protects", "Python requires all validation to be inside __init__ methods", "It uses less memory than external validation functions do"], answer: 1, explanation: "Encapsulation means keeping related data and logic together. If validation lives inside the class, every object is guaranteed to be valid from the moment it's created, regardless of how or where it's constructed." },

    // Properties, getters, setters
    { id: "cs50p-w8-q20", q: "What problem do properties (getters/setters) solve that __init__ validation alone does not?", options: ["They make the code shorter and easier to read overall", "They prevent someone from changing an attribute to an invalid value after the object is created", "They allow the object to be printed using the print function", "They make the object completely immutable after construction"], answer: 1, explanation: "__init__ validation only checks values at creation time. A property setter runs validation every time the attribute is changed, so student.house = '' after creation would also be caught and rejected." },
    { id: "cs50p-w8-q21", q: "What does the @property decorator do when placed above a method?", options: ["Makes the method private and inaccessible from outside", "Defines it as a getter — code that runs when the attribute is accessed", "Prevents the method from being called by external code", "Converts the method to a class method on the class itself"], answer: 1, explanation: "@property turns a method into a getter, so accessing student.house calls the method behind the scenes instead of directly reading an attribute. This lets you add logic (like returning _house) transparently." },
    { id: "cs50p-w8-q22", q: "Why must the underlying instance variable use a different name (e.g., _house) than the property (house)?", options: ["Python style guidelines require a leading underscore on all variables", "To avoid infinite recursion — the property and the variable would collide otherwise", "Underscores make variables faster to access during execution", "It's optional but looks cleaner and more professional in code"], answer: 1, explanation: "If the property and variable share the same name, accessing self.house inside the getter calls the getter again, which calls itself infinitely. Using _house for storage and house for the property avoids this loop." },
    { id: "cs50p-w8-q23", q: "In __init__, writing self.house = house (without underscore) triggers what?", options: ["Direct assignment to the instance variable in memory", "The setter method, which includes validation", "A NameError because house is not yet defined at that point", "Nothing — the assignment is ignored by the interpreter"], answer: 1, explanation: "When a property setter is defined for 'house', any assignment to self.house (even inside __init__) goes through the setter. This means validation runs automatically during construction, not just for later changes." },
    { id: "cs50p-w8-q24", q: "In Python, can a determined programmer still bypass a property and set _house directly?", options: ["No — properties make attributes truly private and protected", "Yes — Python relies on convention, not enforcement, for access control", "Only if they import a special module to unlock private access", "Only in debug mode when running with the -d flag enabled"], answer: 1, explanation: "Python has no true private attributes. The leading underscore in _house is a convention meaning 'don't touch this directly,' but nothing prevents someone from writing student._house = 'invalid'. Python trusts programmers." },

    // Class methods & class variables
    { id: "cs50p-w8-q25", q: "What distinguishes a class method from an instance method?", options: ["Class methods are faster to execute at runtime overall", "Class methods belong to the class itself and don't require an object to be created first", "Class methods can't access any variables or other methods", "There is no difference between class methods and instance methods"], answer: 1, explanation: "An instance method operates on a specific object (via self). A class method, decorated with @classmethod, operates on the class itself (via cls) and can be called without creating an instance first, like Student.get()." },
    { id: "cs50p-w8-q26", q: "What is the conventional name for the first parameter of a class method?", options: ["self", "this", "cls", "class"], answer: 2, explanation: "By convention, the first parameter of a class method is named cls (short for class). Just as self refers to the instance in instance methods, cls refers to the class itself. Python passes it automatically." },
    { id: "cs50p-w8-q27", q: "Why can't the first parameter of a class method be called 'class'?", options: ["It would be too long for Python to process", "'class' is a reserved keyword in Python", "It would conflict with self in the same scope", "Convention forbids it but it would technically work"], answer: 1, explanation: "'class' is a reserved keyword used to define classes (class Student:). Using it as a parameter name would confuse the parser, so the convention is cls instead." },
    { id: "cs50p-w8-q28", q: "A variable defined directly inside a class body (not inside any method) is called what?", options: ["A local variable scoped to the class definition", "An instance variable belonging to each individual object", "A class variable — shared across all instances", "A global variable accessible from anywhere in the program"], answer: 2, explanation: "A class variable is shared by all instances of the class. Unlike instance variables (set on self), a class variable exists once on the class itself. Changing it affects every instance that hasn't overridden it." },
    { id: "cs50p-w8-q29", q: "Why would you use a class method like Student.get() instead of a standalone function get_student()?", options: ["It runs faster than a standalone function would", "It keeps student-related functionality encapsulated inside the Student class", "Python requires all factory functions to be class methods", "It uses less memory than a standalone function at runtime"], answer: 1, explanation: "Encapsulation groups related code together. A class method like Student.get() makes it clear the function is about creating Students and keeps it organized within the class, rather than floating as a separate function." },

    // Inheritance
    { id: "cs50p-w8-q30", q: "What does it mean for class Student to inherit from class Wizard?", options: ["Student replaces Wizard and Wizard is deleted", "Student gets a copy of Wizard's source code pasted in", "Student automatically gains all of Wizard's methods and attributes", "Wizard becomes an instance of Student automatically"], answer: 2, explanation: "Inheritance lets a child class (Student) automatically receive all methods and attributes from a parent class (Wizard). Student can then add its own unique features or override inherited ones." },
    { id: "cs50p-w8-q31", q: "What does super().__init__(name) do inside a subclass's __init__?", options: ["Creates a new parent object in addition to the child object", "Calls the parent class's __init__ to handle the shared initialization logic", "Deletes the parent class from the class hierarchy entirely", "Overrides the parent class entirely with the subclass version"], answer: 1, explanation: "super() gives access to the parent class. Calling super().__init__(name) runs the parent's initialization code, so shared setup (like storing name) doesn't need to be duplicated in the child class." },
    { id: "cs50p-w8-q32", q: "What problem does inheritance solve?", options: ["It makes code run faster by reusing compiled methods", "It eliminates duplicated code when multiple classes share common attributes or behavior", "It prevents bugs from occurring in class hierarchies", "It is required to use classes at all in Python programs"], answer: 1, explanation: "When multiple classes share common attributes or methods, inheritance lets you define that shared code once in a parent class. Each child class inherits it automatically, following the DRY principle (Don't Repeat Yourself)." },

    // Operator overloading
    { id: "cs50p-w8-q33", q: "What is operator overloading?", options: ["Using too many operators in one expression at once", "Defining custom behavior for operators like + on your own classes", "A syntax error caused by conflicting operator definitions", "Renaming built-in operators to use different symbols"], answer: 1, explanation: "Operator overloading lets you define what operators like +, -, and == do when used with your custom objects. You implement special methods like __add__ so your class works naturally with Python's operators." },
    { id: "cs50p-w8-q34", q: "Which special method lets you define what + does for your class?", options: ["__str__", "__init__", "__add__", "__plus__"], answer: 2, explanation: "__add__ is the special (dunder) method Python calls when it encounters the + operator. When you write a + b, Python translates that to a.__add__(b). There is no __plus__ method in Python." },
    { id: "cs50p-w8-q35", q: "In __add__(self, other), what do self and other represent?", options: ["The class and the method being called on it", "The left operand and the right operand of the + sign", "Two copies of the same object being merged together", "The old value and the new value being assigned to it"], answer: 1, explanation: "In potter + weasley, self is the left operand (potter) and other is the right operand (weasley). The method defines how these two objects should be combined using the + operator." },
    { id: "cs50p-w8-q36", q: "When you write potter + weasley and both are Vault objects, what must __add__ return?", options: ["Nothing — it modifies potter in place directly", "A new Vault object containing the combined values", "A dictionary with the merged key-value pairs", "An integer representing the total combined value"], answer: 1, explanation: "__add__ should return a new object rather than modifying either operand. This follows Python's convention where a + b creates a new value, leaving the originals unchanged (just like 1 + 2 creates 3 without changing 1 or 2)." },

    // Built-in types are classes
    { id: "cs50p-w8-q37", q: "What does type(50) reveal?", options: ["'number'", "'integer'", "class 'int' — showing that int is a class", "'50'"], answer: 2, explanation: "In Python, everything is an object, including simple values like integers. type(50) returns <class 'int'>, proving that int is a class and 50 is an instance of it. This is why you can call methods on any value." },
    { id: "cs50p-w8-q38", q: "When you call 'hello'.lower(), what are you doing in OOP terms?", options: ["Calling a standalone function on a string argument", "Calling an instance method on a str object", "Calling a class method on the str class itself", "Using operator overloading on the string value"], answer: 1, explanation: "'hello' is an instance of the str class, and .lower() is a method defined on that class. So 'hello'.lower() is calling an instance method on a str object, just like student.get_name() on a custom class." },
    { id: "cs50p-w8-q39", q: "Why did Python's authors make int, str, list, and dict lowercase even though they're classes?", options: ["They aren't actually classes, they are primitive types", "Convention for built-in types — user-defined classes should use CapitalCase", "Lowercase classes are faster for the interpreter to process", "It was a mistake they can't fix due to backward compatibility"], answer: 1, explanation: "Built-in types like int, str, list, and dict are lowercase as a special convention for types that feel fundamental to the language. User-defined classes follow PEP 8's CapitalCase convention (like Student, Account)." },
  ],
},

// ─── LECTURE 9 ───────────────────────────────────────────────────────
{
  course: "CS50P",
  title: "CS50P Week 9: Et Cetera",
  questions: [
    // Sets
    { id: "cs50p-w9-q1", q: "What property distinguishes a set from a list in Python?", options: ["Sets are ordered by insertion time like lists are", "Sets automatically eliminate duplicates", "Sets are faster to iterate through than lists overall", "Sets can only hold strings as their elements"], answer: 1, explanation: "A set is an unordered collection that cannot contain duplicate values. If you add a value that already exists, the set simply ignores it. This makes sets ideal for tracking unique items." },
    { id: "cs50p-w9-q2", q: "To add an element to a set, which method do you use?", options: ["append()", "insert()", "add()", "push()"], answer: 2, explanation: "Sets use .add() instead of .append() because sets have no concept of order or position. append() and insert() are for lists (which are ordered). push() is not a Python method." },
    { id: "cs50p-w9-q3", q: "If you add a string to a set that differs only in capitalization from an existing entry, what happens?", options: ["The set ignores it as a duplicate because case doesn't matter", "Both versions are stored as distinct elements", "The set raises a ValueError due to the near-duplicate entry", "The newer version replaces the older one in the set"], answer: 1, explanation: "Sets compare elements exactly, and Python strings are case-sensitive. 'Harry' and 'harry' are different strings, so both will be stored. To avoid this, normalize case (e.g., with .lower()) before adding." },

    // Global variables & scope
    { id: "cs50p-w9-q4", q: "What happens if you try to modify a global variable inside a function without the global keyword?", options: ["It works fine and modifies the global variable directly", "Python creates a local variable instead, causing an UnboundLocalError if read before assignment", "Python silently ignores the change and keeps the original value", "It raises a SyntaxError before the program can even start running"], answer: 1, explanation: "When Python sees an assignment inside a function, it treats that variable as local. If you try to read it before the assignment, you get an UnboundLocalError. The global keyword tells Python to use the outer variable instead." },
    { id: "cs50p-w9-q5", q: "What does the 'global' keyword do inside a function?", options: ["Creates a new global variable that didn't exist before", "Tells Python the variable refers to one defined outside all functions, allowing modification", "Makes the variable immutable so it cannot be changed anywhere", "Exports the variable to other modules that import this file"], answer: 1, explanation: "The global keyword tells Python that a variable name inside a function refers to the module-level variable, not a new local one. Without it, assignment creates a local variable that shadows the global." },
    { id: "cs50p-w9-q6", q: "Why is using global variables generally discouraged?", options: ["They use more memory than local variables do in Python", "They make it unclear where state is stored and modified, especially as programs grow", "Python runs them slower than local variables during execution", "They can't be used with classes or object-oriented programming"], answer: 1, explanation: "Global variables can be read or modified from anywhere in the program, making it hard to track what changed them and when. This creates confusing bugs as programs grow. Passing data through parameters is clearer." },
    { id: "cs50p-w9-q7", q: "If you define a variable both globally and locally with the same name, what happens inside the function?", options: ["Python throws a naming conflict error and stops execution", "The local variable shadows the global — changes affect only the local copy", "The global variable is overwritten with the local value automatically", "Both variables are accessible simultaneously using different syntax"], answer: 1, explanation: "Local variables take priority over global ones with the same name. This is called shadowing. Inside the function, the name refers to the local copy, and the global variable remains untouched." },
    { id: "cs50p-w9-q8", q: "Why doesn't passing a global variable as a function argument let the function modify the original?", options: ["Arguments are read-only and cannot be changed inside functions", "The function receives its own local copy of the value, not a reference to the global", "Python prohibits this pattern and raises a TypeError if attempted", "It does work — this is the recommended approach for modifying globals"], answer: 1, explanation: "For immutable types like int and str, the function gets a copy of the value. Reassigning the parameter inside the function only changes the local name, not the original variable. Use return values to communicate changes back." },

    // OOP as alternative to globals
    { id: "cs50p-w9-q9", q: "How does using a class with instance variables solve the global variable problem?", options: ["Classes automatically declare all variables as global internally", "Instance variables are accessible to all methods via self, avoiding the need for global state", "Classes run in a separate memory space from the rest of the program", "They don't solve it — it's purely a style preference with no real benefit"], answer: 1, explanation: "Instance variables live on the object and are accessed through self in any method. This bundles related state together without polluting the global scope, and makes it clear which data belongs to which object." },
    { id: "cs50p-w9-q10", q: "What does the @property decorator allow you to do that a plain instance variable does not?", options: ["Store larger values than regular variables can hold", "Control read/write access — e.g., allow reading but prevent direct assignment", "Make the variable global across all instances of the class", "Automatically type-check assignments at runtime with enforcement"], answer: 1, explanation: "@property lets you define getter and setter methods that look like simple attribute access. You can allow reading (getter only) while blocking or validating writes (setter), giving fine-grained access control." },

    // Constants
    { id: "cs50p-w9-q11", q: "How does Python enforce that a variable declared in ALL_CAPS is constant?", options: ["It raises an error if you try to change its value", "It marks the variable as read-only in memory automatically", "It doesn't — ALL_CAPS is a convention only, not enforced by the language", "The compiler optimizes it away into an immutable constant"], answer: 2, explanation: "Python has no built-in constant mechanism. ALL_CAPS naming (like MAX_RETRIES = 3) is purely a convention that signals 'do not modify this.' The language trusts programmers to respect the convention." },
    { id: "cs50p-w9-q12", q: "What is the purpose of defining a class variable in ALL_CAPS inside a class?", options: ["It makes the variable accessible outside the class by other code", "It signals to other programmers that this value should not be changed", "It improves performance by enabling compiler optimizations on it", "It prevents subclasses from overriding it in their own definitions"], answer: 1, explanation: "ALL_CAPS class variables like HOUSES = ['Gryffindor', 'Slytherin', ...] communicate that this is a fixed value shared by all instances. Other programmers know not to modify it, even though Python won't stop them." },

    // Type hints
    { id: "cs50p-w9-q13", q: "What effect do type hints have when you run a Python program?", options: ["Python enforces them and throws errors on type mismatches", "None — Python ignores type hints at runtime", "They make the program run faster by optimizing types", "They automatically convert types to match the annotations"], answer: 1, explanation: "Type hints are metadata for developers and tools like mypy. Python itself completely ignores them at runtime. You can annotate n: int and pass a string without Python raising an error." },
    { id: "cs50p-w9-q14", q: "What does 'n: int' mean in a function signature like 'def meow(n: int)'?", options: ["n is converted to int automatically before the function runs", "It's a hint that n should be an int, but Python won't enforce it", "n can only be assigned once inside the function body", "It creates a new int object and assigns it to the parameter"], answer: 1, explanation: "The annotation n: int documents that the parameter is expected to be an integer. It helps other developers and tools understand the code, but Python will happily accept any type at runtime." },
    { id: "cs50p-w9-q15", q: "What does the arrow notation '-> str' mean in 'def meow(n: int) -> str'?", options: ["The function takes a string argument as its first parameter", "The function returns a value of type str", "The function prints a string to the console as output", "The function converts its input to str before processing"], answer: 1, explanation: "The -> str annotation indicates the function's return type. It tells developers and type checkers that meow() is expected to return a string, making the function's contract clearer." },
    { id: "cs50p-w9-q16", q: "What is the purpose of a tool like mypy?", options: ["It runs Python programs faster through type-based optimization", "It analyzes your code before runtime to check that type hints are consistent with actual usage", "It converts Python to a statically typed language permanently", "It auto-generates type hints for you based on runtime behavior"], answer: 1, explanation: "mypy is a static type checker that reads your code and type hints without running the program. It catches type mismatches (like passing a str where int is expected) before you even run the code, similar to compiled languages." },
    { id: "cs50p-w9-q17", q: "If a function does not explicitly return a value, what does Python implicitly return?", options: ["0", "An empty string", "None", "False"], answer: 2, explanation: "When a function ends without a return statement (or has a bare return with no value), Python automatically returns None. This is important for type hints: such a function's return type is -> None." },
    { id: "cs50p-w9-q18", q: "Why would mypy flag 'number: int = input(\"What's n? \")'?", options: ["input() is deprecated in newer Python versions", "input() returns a str, but the variable is annotated as int — a type mismatch", "You can't annotate local variables with type hints", "mypy doesn't understand the input function at all"], answer: 1, explanation: "input() always returns a string, but the annotation says number should be an int. mypy detects this mismatch. The fix is either to annotate as str, or wrap with int(): number: int = int(input(...))." },

    // argparse
    { id: "cs50p-w9-q19", q: "What problem does argparse solve compared to manually parsing sys.argv?", options: ["It makes programs run faster by caching parsed arguments", "It handles argument ordering, type conversion, defaults, and help text automatically", "It encrypts command-line arguments for security purposes", "It limits the number of arguments a user can pass to the program"], answer: 1, explanation: "Manually parsing sys.argv requires you to handle ordering, type conversion, error messages, and help text yourself. argparse provides all of this out of the box, including automatic -h/--help support." },
    { id: "cs50p-w9-q20", q: "What does running a program with -h or --help typically show when using argparse?", options: ["The program's source code in the terminal window", "Auto-generated usage information describing available arguments", "A list of all installed Python packages on the system", "Debug output showing internal variable states and values"], answer: 1, explanation: "argparse automatically generates and displays a help message listing all available arguments, their types, defaults, and descriptions. You get this for free just by defining your arguments with add_argument()." },
    { id: "cs50p-w9-q21", q: "If you add type=int to an argparse argument, what happens when the user passes a non-integer?", options: ["Python silently ignores the argument and uses a default", "argparse automatically displays an error message and exits", "The value is stored as a string instead of being converted", "The program crashes with a traceback and full stack trace"], answer: 1, explanation: "argparse validates the type when parsing. If the user passes 'cat' for a type=int argument, argparse prints a clear error message (e.g., 'invalid int value: cat') and exits cleanly without a traceback." },
    { id: "cs50p-w9-q22", q: "What is the convention for single-letter vs. full-word command-line flags?", options: ["Single letters use --, full words use a single dash -", "Single letters use one dash (-n), full words use two dashes (--number)", "There is no convention — any format is acceptable for either", "Single letters are for required args, full words for optional ones"], answer: 1, explanation: "By convention, short flags use a single dash (-n) and long flags use double dashes (--number). Short flags are quick to type; long flags are self-documenting. argparse lets you define both for the same argument." },

    // Unpacking
    { id: "cs50p-w9-q23", q: "What does the * operator do when placed before a list being passed to a function?", options: ["Multiplies all elements together into a single product", "Unpacks the list so each element is passed as a separate positional argument", "Creates a deep copy of the list in a new variable", "Reverses the list before passing it to the function"], answer: 1, explanation: "The * operator unpacks an iterable into separate arguments. f(*[1, 2, 3]) is equivalent to f(1, 2, 3). This is useful when you have data in a list but the function expects individual arguments." },
    { id: "cs50p-w9-q24", q: "What does ** do when placed before a dictionary being passed to a function?", options: ["Squares all values in the dictionary entries", "Unpacks the dictionary so each key-value pair becomes a named argument", "Merges it with another dictionary passed as an argument", "Converts it to a list of tuples with key-value pairs"], answer: 1, explanation: "** unpacks a dictionary into keyword arguments. f(**{'name': 'Harry', 'house': 'Gryffindor'}) is equivalent to f(name='Harry', house='Gryffindor'). The dictionary keys must match the function's parameter names." },
    { id: "cs50p-w9-q25", q: "If a function expects 3 arguments and you unpack a list of 4 elements with *, what happens?", options: ["The fourth element is silently ignored by the function", "A TypeError — too many arguments were given", "The last argument receives a list of the extra elements", "Python assigns the extra to a variable called _ automatically"], answer: 1, explanation: "Unpacking with * expands the list into individual positional arguments. If the function expects 3 but receives 4, Python raises a TypeError for too many arguments, just as if you had called f(1, 2, 3, 4) directly." },

    // *args and **kwargs
    { id: "cs50p-w9-q26", q: "What does *args in a function definition allow?", options: ["Exactly one argument of any type to be passed in", "A variable number of positional arguments, collected into a tuple", "A variable number of named keyword arguments as input", "Arguments that must be integers or numeric types only"], answer: 1, explanation: "*args collects any extra positional arguments into a tuple. def f(*args) lets you call f(1), f(1, 2), or f(1, 2, 3). Inside the function, args is a tuple containing whatever was passed." },
    { id: "cs50p-w9-q27", q: "What does **kwargs in a function definition allow?", options: ["A variable number of positional arguments collected in a tuple", "A variable number of keyword arguments, collected into a dictionary", "Two required arguments that must both be provided", "Arguments passed by reference instead of by value"], answer: 1, explanation: "**kwargs collects extra keyword arguments into a dictionary. def f(**kwargs) lets you call f(name='Harry', house='Gryffindor'). Inside the function, kwargs is {'name': 'Harry', 'house': 'Gryffindor'}." },
    { id: "cs50p-w9-q28", q: "In print(*objects, sep=' ', end='\\n'), what does the * before objects indicate?", options: ["objects is a pointer to a memory address in Python", "print accepts a variable number of positional arguments", "objects must be a list for the function to work properly", "The output will be formatted with special characters added"], answer: 1, explanation: "The * in print's signature means it accepts any number of positional arguments. This is why you can write print('a', 'b', 'c') with as many values as you want. They are collected into the objects tuple." },

    // map, filter, list/dict comprehensions
    { id: "cs50p-w9-q29", q: "What does the map() function do?", options: ["Creates a dictionary from two lists by pairing elements", "Applies a given function to every element of a sequence and returns the results", "Filters elements based on a condition and removes non-matches", "Sorts a list in place without returning a new one"], answer: 1, explanation: "map(function, iterable) applies the function to each element and returns an iterator of results. For example, map(str.upper, ['hi', 'bye']) produces 'HI', 'BYE'. It's a concise alternative to a for loop." },
    { id: "cs50p-w9-q30", q: "What does filter() expect its first argument to do?", options: ["Transform each element into a different value or type", "Return True or False to decide whether each element is included", "Sort the elements into ascending or descending order", "Count the elements that match a given pattern string"], answer: 1, explanation: "filter(function, iterable) calls the function on each element and keeps only those for which it returns True. The function acts as a test, deciding which elements pass through." },
    { id: "cs50p-w9-q31", q: "What is a list comprehension?", options: ["A way to document a list's contents and purpose", "A concise syntax using [] to construct a new list from a loop (and optional condition) in one expression", "A method that lists all attributes of an object for inspection", "A debugging tool for lists that displays their internal state"], answer: 1, explanation: "A list comprehension like [x*2 for x in range(5)] creates a new list by applying an expression to each element of an iterable. It's a more Pythonic and readable alternative to using map() or a manual loop." },
    { id: "cs50p-w9-q32", q: "What distinguishes a dictionary comprehension from a list comprehension syntactically?", options: ["Dictionary comprehensions use parentheses instead of brackets", "Dictionary comprehensions use curly braces {} and specify key: value pairs", "There is no difference between the two comprehension forms", "Dictionary comprehensions require the dict() function to work"], answer: 1, explanation: "A dict comprehension uses {key: value for ...} with curly braces and a colon between key and value. A list comprehension uses [value for ...] with square brackets. The loop syntax is otherwise identical." },
    { id: "cs50p-w9-q33", q: "In '[word.upper() for word in words if len(word) > 3]', what does the 'if' clause do?", options: ["Raises an error for short words that don't meet the condition", "Filters — only words longer than 3 characters are included in the result", "Transforms words shorter than 3 characters to uppercase too", "It's invalid syntax and will cause a comprehension error"], answer: 1, explanation: "The if clause at the end of a comprehension acts as a filter. Only elements that satisfy the condition are processed and included in the output list. Short words are simply skipped." },

    // enumerate
    { id: "cs50p-w9-q34", q: "What does enumerate() give you that a plain 'for x in list' loop does not?", options: ["The ability to modify elements in the original list", "Both the index and the value on each iteration", "Automatic sorting of the list before iteration begins", "Faster execution speed compared to a regular for loop"], answer: 1, explanation: "enumerate() wraps an iterable and yields (index, value) pairs. This eliminates the need for a manual counter variable. For example, for i, name in enumerate(names) gives you both the position and the element." },

    // Generators & yield
    { id: "cs50p-w9-q35", q: "What problem does yield solve compared to building a huge list and returning it all at once?", options: ["It makes the code shorter and easier to read overall", "It generates values one at a time, avoiding the memory cost of storing everything at once", "It runs the loop in parallel across multiple processor cores", "It caches results to disk for persistence between program runs"], answer: 1, explanation: "A generator with yield produces values lazily, one at a time. If you need a billion values, a list would consume massive memory, but a generator only holds one value at a time, computing the next on demand." },
    { id: "cs50p-w9-q36", q: "What is the key behavioral difference between return and yield inside a loop?", options: ["return is faster to execute than yield during each iteration", "return exits the function immediately; yield pauses it, returning one value while remembering where it left off", "yield exits the function entirely; return pauses it until called again", "There is no difference between them inside a loop structure"], answer: 1, explanation: "return terminates the function permanently and sends back one value. yield pauses the function, sends back one value, and resumes from exactly that point when the next value is requested." },
    { id: "cs50p-w9-q37", q: "What is the term for the object that yield returns?", options: ["A list of all the yielded values collected together", "An iterator (or generator)", "A coroutine running in a separate execution context", "A thread running in parallel with the main program"], answer: 1, explanation: "A function containing yield returns a generator object, which is a type of iterator. It doesn't compute all values upfront; instead, it produces the next value each time next() is called (or each pass through a for loop)." },
    { id: "cs50p-w9-q38", q: "If a function uses yield instead of return, can you still loop over its output with a for loop?", options: ["No — you must call next() manually each time you want a value", "Yes — the for loop consumes the iterator one value at a time", "Only if you convert it to a list first before iterating over it", "Only with a while loop — for loops don't support generators"], answer: 1, explanation: "for loops work perfectly with generators because they automatically call next() on each iteration. The loop ends when the generator is exhausted (StopIteration is raised internally). This is the most common way to use generators." },

    // Docstrings
    { id: "cs50p-w9-q39", q: "Where does a docstring go relative to the function it documents?", options: ["Above the function definition on the preceding line", "Below the function, after all code has been written", "Inside the function, as the first statement, using triple quotes", "In a separate .txt file alongside the Python source code"], answer: 2, explanation: "A docstring is a triple-quoted string placed as the very first statement inside a function (or class, or module). Python stores it in the __doc__ attribute, making it accessible to help() and documentation tools." },
    { id: "cs50p-w9-q40", q: "What is the practical benefit of using docstrings over regular # comments for functions?", options: ["They run faster than comments during code execution", "Tools can automatically extract them to generate documentation (web pages, PDFs)", "They prevent bugs from occurring in the documented functions", "They are required by Python for all function definitions"], answer: 1, explanation: "Unlike comments, docstrings are stored as data at runtime and accessible via __doc__. Tools like Sphinx, pydoc, and help() can extract and format them into documentation automatically, without reading source code." },
  ],
},

];
